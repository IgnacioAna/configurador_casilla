---
phase: 06-resumen-y-exportaci-n
plan: 02
type: execute
wave: 1
depends_on: ["06-01"]
files_modified:
  - src/data/contacto.js
  - src/utils/exportWhatsApp.js
  - src/utils/exportWhatsApp.test.js
autonomous: true
requirements: [EXPORT-01, RESUMEN-03]
must_haves:
  truths:
    - "Existe una constante única CONTACTO con el número wa.me activo (pruebas) y el de producción documentado, más web/IG/ciudad"
    - "mensajeWhatsApp(estado) arma el resumen en texto (trato de usted) con modelo, total formateado, nota orientativa y recordatorio del PDF"
    - "El mensaje de WhatsApp no contiene voseo (gate anti-voseo, patrón Fase 3)"
    - "linkWhatsApp(estado) = https://wa.me/<dígitos>?text=... con el número desde CONTACTO y \\n→%0A vía encodeURIComponent"
    - "El link con todos los extras seleccionados queda por debajo del límite práctico de URL (~2000 chars)"
    - "FINANCIACION tiene las 3 opciones (contado/financiado/permuta) con nombre+detalle, listas para RESUMEN-03"
  artifacts:
    - path: "src/data/contacto.js"
      provides: "Fuente única de contacto: wa.me + web + IG + ciudad (D-08/D-13)"
      exports: ["CONTACTO"]
      contains: "5492954555113"
    - path: "src/utils/exportWhatsApp.js"
      provides: "mensajeWhatsApp + linkWhatsApp (puro, testeable)"
      exports: ["mensajeWhatsApp", "linkWhatsApp"]
  key_links:
    - from: "src/utils/exportWhatsApp.js linkWhatsApp"
      to: "src/data/contacto.js CONTACTO.whatsapp"
      via: "número desde la constante, nunca literal en código"
      pattern: "CONTACTO\\.whatsapp"
    - from: "src/utils/exportWhatsApp.js"
      to: "encodeURIComponent (output encoding)"
      via: "encodeURIComponent(mensajeWhatsApp(estado)) en el text del wa.me"
      pattern: "encodeURIComponent"
---

<objective>
Construir la **fuente única de contacto** (`data/contacto.js`, D-08/D-13) y la utilidad pura de
exportación por WhatsApp (`exportWhatsApp.js`, EXPORT-01), más verificar que `FINANCIACION`
(ya existente, RESUMEN-03) cubre las 3 opciones que el resumen va a renderizar.

`mensajeWhatsApp(estado)` arma el resumen en texto multilínea (trato de usted, termina con el
recordatorio del PDF porque `wa.me` no adjunta archivos, D-10) y `linkWhatsApp(estado)` produce el
deep-link `wa.me` con el número leído de `CONTACTO` y todo el `text` pasado por `encodeURIComponent`
(output encoding, V5; `\n`→`%0A`).

Purpose: aislar el armado del mensaje/link como lógica pura testeable (mensaje, anti-voseo, encoding,
longitud) — interface-first — para que el botón "Enviar por WhatsApp" del Plan 06-03 sea un simple
`<a href={linkWhatsApp(estado)}>`. La constante de contacto la consumen tanto el link wa.me como el
contacto del PDF (06-03).

Output: `data/contacto.js`, `exportWhatsApp.js` + su test en verde.

> **Coordinación de wave (file ownership):** este plan NO toca `package.json`. El registro del test
> `src/utils/exportWhatsApp.test.js` en el script `test` lo hace el Plan 06-01 (dueño único de
> `package.json` en Wave 1) — 06-01 registra los 3 test files nuevos de la fase, incluido el de este
> plan. Así 06-01 y 06-02 quedan con `files_modified` disjuntos y corren en paralelo sin conflicto.
> Dependencia de datos: `exportWhatsApp.js` importa `detallePresupuesto` y `resumenCampos` (de 06-01)
> y `CONTACTO` (de este plan). El runner `node --test` del test individual funciona en cuanto los
> archivos de 06-01 existan; `npm test` completo verde se confirma al cierre de la wave.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-resumen-y-exportaci-n/06-CONTEXT.md
@.planning/phases/06-resumen-y-exportaci-n/06-RESEARCH.md
@.planning/phases/06-resumen-y-exportaci-n/06-PATTERNS.md

<interfaces>
<!-- Contratos exactos. exportWhatsApp DEPENDE de detallePresupuesto y resumenCampos (Plan 06-01). -->

From src/data/financiacion.js (RESUMEN-03 — ya existe, solo verificar):
```javascript
export const FINANCIACION = [
  { id: 'contado',    nombre: 'Pago contado',           detalle: 'Precio de lista, sujeto a confirmación del asesor.' },
  { id: 'financiado', nombre: 'Financiación en cuotas', detalle: 'Planes de pago a convenir con el asesor comercial.' },
  { id: 'permuta',    nombre: 'Permuta / entrega',      detalle: 'Se aceptan permutas a evaluar por la fábrica.' },
]
```

From src/utils/motorPrecios.js (lo crea/extiende el Plan 06-01 — disponible al ejecutar este plan):
```javascript
export function detallePresupuesto(estado) // → { items, neto, iva, total }
```

From src/utils/resumenCampos.js (lo crea el Plan 06-01 — disponible al ejecutar este plan):
```javascript
export function resumenCampos(estado)
// → { uso, ocupantes, modelo, largo, bano, camas, cocina, extras }
//   extras = array de nombres reales (string[]); el resto strings legibles o 'Sin selección'
```

From src/utils/formato.js:
```javascript
export function formatPrecio(n) // → '$38.971.845' (argentino). NUNCA formatear a mano.
```

CONTACTO a crear (valores fijados en CONTEXT D-08/D-13 + RESEARCH Code Examples):
```javascript
export const CONTACTO = {
  // wa.me: SOLO dígitos, código país (54) + 9, sin '+' ni espacios.
  whatsapp: '5492954555113',          // ACTIVO (pruebas, Ignacio). PRODUCCIÓN antes del go-live: 5492302468754 (Impacar)
  whatsappDisplay: '+54 9 2954 55-5113', // legible para el PDF
  web: 'industriaimpacar.com',
  instagram: '@industriasimpacar',
  ciudad: 'General Pico, La Pampa',
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: data/contacto.js — fuente única de contacto + verificación de FINANCIACION (D-08, D-13, RESUMEN-03)</name>
  <files>src/data/contacto.js</files>
  <read_first>
    - src/data/financiacion.js (análogo de shape: objeto/array de constantes con comentario de contexto arriba; verificar que tiene las 3 opciones para RESUMEN-03)
    - .planning/phases/06-resumen-y-exportaci-n/06-CONTEXT.md § D-08 + D-13 (números de WhatsApp activo vs producción; contacto completo del PDF)
    - .planning/phases/06-resumen-y-exportaci-n/06-RESEARCH.md § Code Examples (cuerpo verbatim de contacto.js)
    - .planning/phases/06-resumen-y-exportaci-n/06-PATTERNS.md § contacto.js
  </read_first>
  <action>
    Crear `src/data/contacto.js` como fuente ÚNICA de contacto (la consumen el link wa.me y el
    contacto del PDF). Copiar el cuerpo del bloque <interfaces> arriba VERBATIM:
    ```javascript
    // src/data/contacto.js — fuente única de contacto (wa.me + PDF). CONTEXT D-08/D-13.
    export const CONTACTO = {
      // wa.me: SOLO dígitos, código país (54) + 9, sin '+' ni espacios.
      whatsapp: '5492954555113', // ACTIVO (pruebas, Ignacio). PRODUCCIÓN antes del go-live: 5492302468754 (Impacar)
      whatsappDisplay: '+54 9 2954 55-5113', // para mostrar legible en el PDF
      web: 'industriaimpacar.com',
      instagram: '@industriasimpacar',
      ciudad: 'General Pico, La Pampa',
    }
    ```
    CRÍTICO (D-08): documentar AMBOS números — el activo `5492954555113` en la constante Y el de
    producción `5492302468754` en el comentario (es el swap previo al go-live; queda como nota, NO se
    activa ahora, para que las pruebas de la demo no le lleguen a Impacar).

    Verificar (NO modificar) `src/data/financiacion.js`: debe tener las 3 opciones contado/financiado/
    permuta con `nombre` + `detalle` para RESUMEN-03. Si por algún motivo faltara alguna, detenerse y
    reportarlo (no debería — ya existe de Fase 1). No hay cambios de código en financiacion.js.
  </action>
  <verify>
    <automated>node -e "import('./src/data/contacto.js').then(m=>{const c=m.CONTACTO; if(c.whatsapp!=='5492954555113')throw new Error('whatsapp activo incorrecto'); ['whatsappDisplay','web','instagram','ciudad'].forEach(k=>{if(!c[k])throw new Error('falta '+k)}); console.log('OK')})"</automated>
  </verify>
  <acceptance_criteria>
    - `src/data/contacto.js` exporta `const CONTACTO` (grep: `export const CONTACTO`)
    - `src/data/contacto.js` contiene la subcadena `5492954555113` (número activo de pruebas)
    - `src/data/contacto.js` contiene la subcadena `5492302468754` (número de producción documentado en comentario)
    - `src/data/contacto.js` contiene `industriaimpacar.com`, `@industriasimpacar` y `General Pico, La Pampa`
    - `src/data/financiacion.js` exporta `FINANCIACION` con 3 elementos (verificación: `grep -c "id: '" src/data/financiacion.js` ≥ 3) — RESUMEN-03
    - El comando de verify imprime `OK` y sale 0
  </acceptance_criteria>
  <done>contacto.js creado con número activo + producción documentado y contacto completo; FINANCIACION verificada con 3 opciones para RESUMEN-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: exportWhatsApp.js (mensajeWhatsApp + linkWhatsApp) + tests (EXPORT-01)</name>
  <files>src/utils/exportWhatsApp.js, src/utils/exportWhatsApp.test.js</files>
  <read_first>
    - src/utils/formato.js (estilo pure-util: imports al tope, funciones puras; formatPrecio para la cifra)
    - src/utils/motorPrecios.js (detallePresupuesto — creada por 06-01; se usa para el total)
    - src/utils/resumenCampos.js (resumenCampos — creada por 06-01; LEER su shape real antes de armar el template del mensaje)
    - src/data/contacto.js (CONTACTO.whatsapp — Task 1 de este plan)
    - src/utils/motorPrecios.test.js (patrón node:test + asserts de estado adulterado)
    - .planning/phases/06-resumen-y-exportaci-n/06-RESEARCH.md § Pattern 2 (cuerpo verbatim mensajeWhatsApp/linkWhatsApp) + Pitfall 6 (límite ~2000 chars)
    - .planning/phases/06-resumen-y-exportaci-n/06-UI-SPEC.md § Copywriting Contract (estructura del mensaje + trato de usted + línea final del PDF)
  </read_first>
  <behavior>
    mensajeWhatsApp(estado):
    - Test: con `{ modeloId:'N4', uso:'ganadero', ocupantes:4, bano:{tamano:'ampliado'}, dormitorio:{camas:[{tipo:'C'},{tipo:'M'}]}, extras:['cocina-horno','heladera-220','calefactor-4000'] }` el texto contiene: el nombre del modelo ('N4'), `formatPrecio(total)` (el total c/IVA formateado, ej. '$...'), la nota 'Presupuesto orientativo, sujeto a confirmación.', y la línea final 'Le envío aparte el PDF con el plano y el detalle.' (D-10).
    - Test (anti-voseo, gate Fase 3): el mensaje NO contiene `tenés`, `querés`, `podés`, `elegí`, `mirá`, `enviá` ni otras formas de voseo — está en trato de usted.
    - Test: el total que aparece en el mensaje proviene de `detallePresupuesto(estado).total` formateado con `formatPrecio` (no un número crudo, no `.toLocaleString` ad-hoc).
    - Test (degradación): `mensajeWhatsApp(null)` y con extras no-array no crashean; el modelo cae a algo legible ('Modelo no disponible' vía resumenCampos), el total a `$0` sin `$NaN`.
    linkWhatsApp(estado):
    - Test: empieza con `https://wa.me/5492954555113?text=` (número desde CONTACTO).
    - Test: el `text` está URL-encoded — contiene `%0A` (los `\n` codificados) y NO contiene saltos de línea crudos ni espacios sin codificar.
    - Test (Pitfall 6): con TODOS los extras reales seleccionados (`EXTRAS.map((e) => e.id)`), `linkWhatsApp(estado).length` < 2000.
  </behavior>
  <action>
    Crear `src/utils/exportWhatsApp.js` (puro, sin React/DOM). Copiar el cuerpo de RESEARCH Pattern 2:
    ```javascript
    import { CONTACTO } from '../data/contacto.js'
    import { detallePresupuesto } from './motorPrecios.js'
    import { formatPrecio } from './formato.js'
    import { resumenCampos } from './resumenCampos.js'

    export function mensajeWhatsApp(estado) {
      const { total } = detallePresupuesto(estado)
      const c = resumenCampos(estado)
      return [
        'Hola, le comparto la configuración de mi casilla Impacar:',
        '',
        `Modelo: ${c.modelo}${c.largo && c.largo !== 'Sin selección' ? ` (${c.largo})` : ''}`,
        `Uso: ${c.uso} · Ocupantes: ${c.ocupantes}`,
        `Baño: ${c.bano}`,
        `Dormitorio: ${c.camas}`,
        `Cocina: ${c.cocina}`,
        c.extras.length ? `Extras: ${c.extras.join(', ')}` : 'Extras: Sin selección',
        '',
        `Total c/IVA: ${formatPrecio(total)}`,
        'Presupuesto orientativo, sujeto a confirmación.',
        '',
        'Le envío aparte el PDF con el plano y el detalle.', // D-10: wa.me no adjunta archivos
      ].join('\n')
    }

    export function linkWhatsApp(estado) {
      return `https://wa.me/${CONTACTO.whatsapp}?text=${encodeURIComponent(mensajeWhatsApp(estado))}`
    }
    ```
    Reglas CRÍTICAS (anti-patterns de RESEARCH):
    - El número SIEMPRE de `CONTACTO.whatsapp` — NUNCA literal en el código.
    - `formatPrecio(total)` para la cifra — NUNCA `.toLocaleString` ad-hoc.
    - `encodeURIComponent` sobre TODO el `text` (V5 output encoding; `\n`→`%0A`).
    - Última línea = recordatorio del PDF (D-10), trato de usted en el 100% del copy.
    - Adaptá las claves a lo que devuelve `resumenCampos` (06-01): leé primero su shape real
      (`{ uso, ocupantes, modelo, largo, bano, camas, cocina, extras }`); si una clave o su formato
      difiere, alineá el template a la salida real. El mensaje debe quedar conciso (Pitfall 6: sin
      desglose ítem-por-ítem con precios — el detalle completo va en el PDF, D-10).

    Crear `src/utils/exportWhatsApp.test.js` con node:test (header de motorPrecios.test.js:
    `import { test } from 'node:test'` + `import assert from 'node:assert/strict'`) cubriendo el bloque
    <behavior>, incluyendo el **gate anti-voseo** (array de tokens de voseo + assert de que ninguno
    aparece en el mensaje) y el test de longitud con `EXTRAS.map((e) => e.id)`.

    NO tocar `package.json` — el registro de este test en el script `test` lo hace el Plan 06-01
    (dueño único de package.json en Wave 1). Ejecutá el test directo con `node --test`.
  </action>
  <verify>
    <automated>node --test src/utils/exportWhatsApp.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/utils/exportWhatsApp.js` exporta `function mensajeWhatsApp` y `function linkWhatsApp` (grep ambas)
    - `src/utils/exportWhatsApp.js` contiene `CONTACTO.whatsapp` (número desde la constante, no literal)
    - `src/utils/exportWhatsApp.js` contiene `encodeURIComponent`
    - `src/utils/exportWhatsApp.js` contiene `formatPrecio` (cifra formateada, no cruda)
    - `src/utils/exportWhatsApp.js` contiene la subcadena `Le envío aparte el PDF con el plano y el detalle.`
    - `grep -nE 'tenés|querés|podés|elegí|mirá|enviá|tené ' src/utils/exportWhatsApp.js` NO devuelve coincidencias (anti-voseo en el copy fuente)
    - `node --test src/utils/exportWhatsApp.test.js` sale 0, incluyendo el test anti-voseo y el de longitud < 2000
  </acceptance_criteria>
  <done>mensajeWhatsApp/linkWhatsApp puros, con número desde CONTACTO, total formateado, nota+recordatorio PDF, trato de usted (gate anti-voseo verde), encoding correcto y link < 2000 chars con todos los extras; test en verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage (`impacar_config_v1`) → exportWhatsApp | El estado puede venir adulterado; el contenido se inserta en el `text` del deep-link. |
| App → wa.me (target externo) | El navegador abre un dominio externo en una pestaña nueva (lo cablea 06-03 con `rel="noopener noreferrer"`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering / Injection | `linkWhatsApp` (estado adulterado inyecta saltos de línea / `&` / caracteres raros en la URL) | mitigate | `encodeURIComponent` sobre TODO el `text` (codifica `\n`→`%0A`, `&`, acentos, ñ). El número viene de la constante `CONTACTO`, NUNCA del estado del usuario. Test verifica que no quedan saltos crudos en la URL. ASVS V5 (output encoding). |
| T-06-05 | Tampering | `mensajeWhatsApp` con estado vacío/adulterado | mitigate | Compone sobre `detallePresupuesto` (devuelve 0, nunca `$NaN`) y `resumenCampos` (devuelve 'Sin selección'/'Modelo no disponible'). Test cubre `mensajeWhatsApp(null)` y extras no-array sin crash. |
| T-06-06 | Information Disclosure | número de WhatsApp en el código fuente | accept | Es un número comercial público (y en pruebas, el personal de Ignacio por decisión explícita D-08). Sin secreto. El swap a producción queda documentado en la constante. |

**Nivel de riesgo: BAJO.** Reverse-tabnabbing del `<a target="_blank">` se mitiga en 06-03 con
`rel="noopener noreferrer"` (UI-SPEC S6). Ningún threat `high` que bloquee.
</threat_model>

<verification>
- `node --test src/utils/exportWhatsApp.test.js` en verde (mensaje, anti-voseo, link, encoding, longitud).
- `node -e` de contacto.js confirma número activo + campos completos.
- `grep -c` confirma FINANCIACION con 3 opciones (RESUMEN-03).
- grep anti-voseo sobre el archivo fuente = 0 coincidencias.
- `npm test` completo en verde tras esta wave (con el test registrado por 06-01).
</verification>

<success_criteria>
- `data/contacto.js` con número activo (pruebas) + producción documentado + web/IG/ciudad (D-08, D-13).
- `mensajeWhatsApp(estado)`: resumen en texto, trato de usted, total formateado, nota orientativa,
  recordatorio del PDF (EXPORT-01, D-09/D-10), sin voseo.
- `linkWhatsApp(estado)`: `https://wa.me/<CONTACTO.whatsapp>?text=<encoded>`, < 2000 chars (EXPORT-01).
- `FINANCIACION` verificada con 3 opciones para RESUMEN-03.
</success_criteria>

<output>
After completion, create `.planning/phases/06-resumen-y-exportaci-n/06-02-SUMMARY.md`
</output>
