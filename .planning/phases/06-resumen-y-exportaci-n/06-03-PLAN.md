---
phase: 06-resumen-y-exportaci-n
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - src/App.jsx
  - src/components/ConfiguratorWizard.jsx
  - src/components/Resumen.jsx
  - src/components/resumen/ConfigSecciones.jsx
  - src/components/resumen/PresupuestoDesglosado.jsx
  - src/components/resumen/BloqueFinanciacion.jsx
  - src/components/resumen/AccionesExport.jsx
  - src/utils/exportPDF.js
autonomous: false
requirements: [RESUMEN-01, RESUMEN-02, RESUMEN-03, EXPORT-01, EXPORT-02]
must_haves:
  truths:
    - "Existe una tercera vista 'resumen' en App.jsx; el estado lo comparte un único usePersistedConfig izado a App"
    - "El Paso 6 del wizard muestra 'Ver resumen y presupuesto' que lleva a vista='resumen' sin resetear el estado"
    - "El resumen muestra el plano final (FloorPlan con configDesdeEstado) + todas las secciones de configuración traducidas"
    - "El resumen muestra el presupuesto desglosado (base + accesorios agrupados, neto+IVA+total) con la nota orientativa"
    - "El resumen lista las 3 opciones de financiación (texto, sin montos)"
    - "El botón 'Enviar por WhatsApp' es un <a href={linkWhatsApp(estado)} target=_blank rel='noopener noreferrer'>"
    - "El botón 'Descargar PDF' genera un PDF A4 de 1 página con el plano vectorial, logo IMPACAR, presupuesto, config y contacto"
    - "Cada sección del resumen tiene 'Editar' que dispara IR_A_PASO al paso correcto y vuelve a vista='wizard'"
    - "Estado vacío (modeloId/uso/ocupantes null) muestra el empty state, sin crashear ni $NaN"
  artifacts:
    - path: "src/App.jsx"
      provides: "Ruteo landing/wizard/resumen + usePersistedConfig izado, estado/dispatch a ambas vistas"
      contains: "vista === 'resumen'"
    - path: "src/components/Resumen.jsx"
      provides: "Shell del resumen (S1) + ref al SVG para el PDF + empty state"
      min_lines: 40
    - path: "src/components/resumen/PresupuestoDesglosado.jsx"
      provides: "Desglose por ítem agrupado + Neto/IVA/Total c/IVA (markup verbatim de BarraPrecio)"
      contains: "Total c/IVA"
    - path: "src/components/resumen/ConfigSecciones.jsx"
      provides: "Secciones por paso con 'Editar' (IR_A_PASO)"
      contains: "IR_A_PASO"
    - path: "src/components/resumen/BloqueFinanciacion.jsx"
      provides: "FINANCIACION.map (3 opciones)"
      contains: "FINANCIACION"
    - path: "src/components/resumen/AccionesExport.jsx"
      provides: "Botones WhatsApp (<a>) + PDF (<button> con estado generando)"
      contains: "rel=\"noopener noreferrer\""
    - path: "src/utils/exportPDF.js"
      provides: "generarPDF(svgNode, estado) browser-side (jsPDF + svg2pdf.js)"
      exports: ["nombreArchivoPDF", "generarPDF"]
  key_links:
    - from: "src/App.jsx"
      to: "Resumen + ConfiguratorWizard"
      via: "usePersistedConfig izado: { estado, dispatch, reiniciar } a ambas vistas"
      pattern: "usePersistedConfig\\(\\)"
    - from: "src/components/resumen/ConfigSecciones.jsx"
      to: "wizardReducer IR_A_PASO"
      via: "dispatch({ type: ACCIONES.IR_A_PASO, paso }) + onVolverEditar()"
      pattern: "ACCIONES\\.IR_A_PASO"
    - from: "src/components/resumen/AccionesExport.jsx"
      to: "exportWhatsApp.linkWhatsApp / exportPDF.generarPDF"
      via: "<a href={linkWhatsApp(estado)}> y onClick={() => generarPDF(svgNode, estado)}"
      pattern: "(linkWhatsApp|generarPDF)\\("
    - from: "src/utils/exportPDF.js generarPDF"
      to: "FloorPlan SVG node vivo"
      via: "await doc.svg(svgNode, {x,y,width,height}) antes de doc.save"
      pattern: "doc\\.svg\\("
---

<objective>
Ensamblar la pantalla de cierre (RESUMEN-01/02/03, EXPORT-01/02) a partir de las piezas puras ya
verificadas en Wave 1 (06-01: detallePresupuesto, resumenCampos, nombreArchivoPDF; 06-02: contacto,
linkWhatsApp). Es el ~70% de reuso de la fase: plano = `FloorPlan` sin cambios, totales = markup
verbatim de `BarraPrecio`, deep-links = acción `IR_A_PASO` existente, botones = estilos del wizard.

Tres trabajos:
1. **Estado compartido + entrada al resumen (D-01, D-02):** izar `usePersistedConfig` a `App.jsx` y
   sumar `vista === 'resumen'`; el wizard y el resumen comparten el MISMO `estado`/`dispatch` (así
   `IR_A_PASO` desde el resumen muta el estado que lee el wizard). Botón "Ver resumen y presupuesto"
   en el Paso 6.
2. **Componentes del resumen (S1-S6):** shell `Resumen.jsx` + 4 sub-componentes + el handler browser
   `generarPDF` (jsPDF + svg2pdf.js, async, nodo SVG vivo).
3. **Checkpoint visual** (no autónomo): verificar el PDF vectorial, los deep-links "Editar", el envío
   por WhatsApp y la financiación en el dev server — lo que no se puede automatizar sin headless browser.

Purpose: cerrar el flujo del configurador de punta a punta. Después de este plan, un contratista
configura su casilla y envía el presupuesto por WhatsApp o lo descarga en PDF.

Output: App.jsx + ConfiguratorWizard.jsx modificados, 5 componentes nuevos del resumen, exportPDF.js
con el handler browser, y el checkpoint visual aprobado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-resumen-y-exportaci-n/06-CONTEXT.md
@.planning/phases/06-resumen-y-exportaci-n/06-RESEARCH.md
@.planning/phases/06-resumen-y-exportaci-n/06-PATTERNS.md
@.planning/phases/06-resumen-y-exportaci-n/06-UI-SPEC.md

<interfaces>
<!-- Contratos exactos del codebase + de las utilidades de Wave 1. Sin necesidad de explorar. -->

From src/App.jsx (HOY — el estado vive DENTRO del wizard; hay que izarlo):
```jsx
import { useState } from 'react'
import Landing from './components/Landing.jsx'
import ConfiguratorWizard from './components/ConfiguratorWizard.jsx'
import { STORAGE_KEY } from './hooks/usePersistedConfig.js'

function vistaInicial() {
  try { return localStorage.getItem(STORAGE_KEY) ? 'wizard' : 'landing' } catch { return 'landing' }
}
export default function App() {
  const [vista, setVista] = useState(vistaInicial)
  return (
    <div className="min-h-screen bg-impacar-fondo text-impacar-texto font-sans">
      {vista === 'landing'
        ? <Landing onComenzar={() => setVista('wizard')} />
        : <ConfiguratorWizard onVolverInicio={() => setVista('landing')} />}
    </div>
  )
}
```

From src/hooks/usePersistedConfig.js:
```jsx
export const STORAGE_KEY = 'impacar_config_v1'
export function usePersistedConfig() { /* ... */ return { estado, dispatch, reiniciar } }
```

From src/components/ConfiguratorWizard.jsx (HOY llama usePersistedConfig adentro — línea 12):
```jsx
export default function ConfiguratorWizard({ onVolverInicio }) {
  const { estado, dispatch, reiniciar } = usePersistedConfig()
  ...
  const esUltimo = estado.pasoActual === TOTAL_PASOS - 1
  ...
  <button type="button" onClick={siguiente} disabled={esUltimo}
    className="min-h-[44px] rounded bg-impacar-campo px-6 py-2 text-sm font-semibold text-white transition-colors hover:bg-impacar-campo/90 focus:outline-none focus:ring-2 focus:ring-impacar-campo/40 disabled:cursor-not-allowed disabled:opacity-40">
    Siguiente
  </button>
```

From src/state/wizardReducer.js:
```jsx
export const ACCIONES = { SET_CAMPO, SIGUIENTE, ANTERIOR, IR_A_PASO, RESET }  // IR_A_PASO: { paso }
export const TOTAL_PASOS = 6
export function configDesdeEstado(estado) // → { modeloId, largo, bano, dormitorio, cocina:{horno,heladera}, estar:{mesa} }
```

From src/components/wizard/pasosRegistro.jsx (índices de paso para IR_A_PASO):
```jsx
// 0 uso · 1 dimensiones · 2 bano · 3 dormitorio · 4 cocina · 5 extras
export const PASOS = [ { id:'uso', titulo:'Uso y ocupantes' }, { id:'dimensiones', ... }, ... ]
```

From src/components/wizard/PlanoPanel.jsx (patrón de montaje del plano — card + FloorPlan):
```jsx
<div className="rounded border border-impacar-texto/10 bg-impacar-fondo p-4">
  <FloorPlan config={config} />
</div>
```

From src/components/wizard/BarraPrecio.jsx (markup VERBATIM de las 3 líneas — UI-SPEC S4 lo exige idéntico):
```jsx
<dl className="mt-2 space-y-1">
  <div className="flex items-center justify-between">
    <dt className="text-sm">Neto</dt><dd className="text-sm">{formatPrecio(neto)}</dd>
  </div>
  <div className="flex items-center justify-between">
    <dt className="text-sm text-impacar-texto/70">IVA 21%</dt><dd className="text-sm text-impacar-texto/70">{formatPrecio(iva)}</dd>
  </div>
  <div className="flex items-center justify-between">
    <dt className="text-sm font-semibold text-impacar-campo">Total c/IVA</dt><dd className="text-sm font-semibold text-impacar-campo">{formatPrecio(total)}</dd>
  </div>
</dl>
```

Utilidades de Wave 1 (disponibles al ejecutar este plan):
```jsx
// src/utils/motorPrecios.js
export function detallePresupuesto(estado) // → { items:[{id,label,precioNeto,categoria,subgrupo?}], neto, iva, total }
// src/utils/resumenCampos.js
export function resumenCampos(estado) // → { uso, ocupantes, modelo, largo, bano, camas, cocina, extras:string[] }
// src/utils/exportWhatsApp.js
export function linkWhatsApp(estado)  // → 'https://wa.me/<num>?text=<encoded>'
// src/utils/exportPDF.js (parte pura ya creada en 06-01; generarPDF se AÑADE en este plan)
export function nombreArchivoPDF(estado) // → 'presupuesto-impacar-N4.pdf'
// src/data/contacto.js
export const CONTACTO // { whatsapp, whatsappDisplay, web, instagram, ciudad }
// src/data/financiacion.js
export const FINANCIACION // [{ id, nombre, detalle } x3]
```

GRUPOS para el desglose (RESEARCH Code Examples — data-driven sobre categoria/subgrupo):
```jsx
const GRUPOS = [
  { match: (i) => i.categoria === 'bano',        titulo: 'Baño' },
  { match: (i) => i.categoria === 'dormitorio',  titulo: 'Dormitorio' },
  { match: (i) => i.categoria === 'cocina',      titulo: 'Cocina y estar' },
  { match: (i) => i.categoria === 'extras' && i.subgrupo === 'confort', titulo: 'Confort' },
  { match: (i) => i.categoria === 'extras' && i.subgrupo === 'energia', titulo: 'Energía' },
]
```

Copy exacto (UI-SPEC Copywriting Contract — VERBATIM):
- Título: `Su casilla, lista para enviar`
- Subtítulo: `Revise su configuración y el presupuesto estimado. Envíelo por WhatsApp o descárguelo en PDF.`
- CTA WhatsApp: `Enviar por WhatsApp` · CTA PDF: `Descargar PDF`
- Entrada en Paso 6: `Ver resumen y presupuesto` · Global: `Volver a editar` · Por sección: `Editar`
- Header presupuesto: `Presupuesto estimado` · líneas `Neto` / `IVA 21%` / `Total c/IVA`
- Nota orientativa: `Presupuesto orientativo, sujeto a confirmación del asesor comercial.`
- Header financiación: `Opciones de financiación`
- Headers de sección: `Uso y ocupantes` · `Modelo` · `Baño` · `Dormitorio` · `Cocina y estar` · `Extras`
- Empty heading: `Todavía no hay una configuración completa`
- Empty body: `Vuelva al configurador para elegir su modelo y opciones, y vea acá el resumen y el presupuesto.`
- Valor de sección vacía: `Sin selección` · Modelo corrupto: `Modelo no disponible`
- Pie de contacto del PDF: `IMPACAR` · `WhatsApp: {CONTACTO.whatsappDisplay}` · `{CONTACTO.web}` · `{CONTACTO.instagram}` · `{CONTACTO.ciudad}`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Izar usePersistedConfig a App.jsx + vista 'resumen' + entrada "Ver resumen y presupuesto" en el Paso 6 (D-01)</name>
  <files>src/App.jsx, src/components/ConfiguratorWizard.jsx</files>
  <read_first>
    - src/App.jsx (ruteo actual landing/wizard a extender — el archivo a modificar)
    - src/components/ConfiguratorWizard.jsx (línea 12 usePersistedConfig adentro; línea 18 esUltimo; líneas 77-84 botón Siguiente; firma de props)
    - src/hooks/usePersistedConfig.js (firma { estado, dispatch, reiniciar } + STORAGE_KEY)
    - src/state/wizardReducer.js (TOTAL_PASOS, ACCIONES)
    - .planning/phases/06-resumen-y-exportaci-n/06-PATTERNS.md § App.jsx + § ConfiguratorWizard.jsx (decisión de lifting state)
    - .planning/phases/06-resumen-y-exportaci-n/06-CONTEXT.md § D-01 (tercera vista, full-width, onVerResumen)
  </read_first>
  <action>
    ARQUITECTURA (decisión locked de este plan — lifting state up): hoy `usePersistedConfig` vive
    DENTRO de `ConfiguratorWizard` (línea 12). Para que `Resumen` lea/escriba el MISMO estado que el
    wizard (y que `IR_A_PASO` desde el resumen funcione), izar el hook a `App.jsx` y pasar
    `{ estado, dispatch, reiniciar }` como props a ambas vistas. Una sola instancia del hook = una
    sola fuente de verdad; sin riesgo de dos useReducer divergentes.

    1) `src/App.jsx`:
       - Importar `usePersistedConfig` (además de STORAGE_KEY) y `Resumen` (lo crea Task 2).
       - Llamar `const { estado, dispatch, reiniciar } = usePersistedConfig()` en App.
       - Mantener `vistaInicial()` tal cual (landing/wizard según localStorage).
       - Ruteo de 3 ramas:
         ```jsx
         {vista === 'landing' && <Landing onComenzar={() => setVista('wizard')} />}
         {vista === 'wizard' && (
           <ConfiguratorWizard
             estado={estado} dispatch={dispatch} reiniciar={reiniciar}
             onVolverInicio={() => setVista('landing')}
             onVerResumen={() => setVista('resumen')}
           />
         )}
         {vista === 'resumen' && (
           <Resumen
             estado={estado} dispatch={dispatch}
             onVolverEditar={() => setVista('wizard')}
           />
         )}
         ```
       - Conservar el wrapper `<div className="min-h-screen bg-impacar-fondo text-impacar-texto font-sans">`.
         (Resumen y el wizard ya traen su propio chrome con header; mantené la estructura actual donde
         cada vista renderiza su `min-h-screen` — o dejá el wrapper de App y que las vistas no dupliquen
         el `min-h-screen`. Elegí UNA y dejá el render consistente: lo más simple es que App tenga el
         wrapper y cada vista su header band, como hoy.)

    2) `src/components/ConfiguratorWizard.jsx`:
       - Cambiar la firma a `export default function ConfiguratorWizard({ estado, dispatch, reiniciar, onVolverInicio, onVerResumen })`.
       - ELIMINAR la línea `const { estado, dispatch, reiniciar } = usePersistedConfig()` (ahora llegan
         por props) y el import de `usePersistedConfig` si queda sin uso.
       - Sustituir `volverAEmpezar` para usar el `reiniciar` recibido por props (mismo comportamiento).
       - En el Paso 6 (`esUltimo === true`): donde hoy el botón "Siguiente" queda `disabled`, mostrar en
         su lugar un botón `Ver resumen y presupuesto` que llama `onVerResumen()`. Reusar EXACTAMENTE el
         className del botón primario "Siguiente" (relleno verde). En los pasos 1-5 (`!esUltimo`) sigue
         el botón "Siguiente" normal. Patrón:
         ```jsx
         {esUltimo ? (
           <button type="button" onClick={onVerResumen}
             className="min-h-[44px] rounded bg-impacar-campo px-6 py-2 text-sm font-semibold text-white transition-colors hover:bg-impacar-campo/90 focus:outline-none focus:ring-2 focus:ring-impacar-campo/40">
             Ver resumen y presupuesto
           </button>
         ) : (
           <button type="button" onClick={siguiente} className="...Siguiente igual que hoy...">Siguiente</button>
         )}
         ```
       - NO tocar BarraProgreso, PlanoPanel, BarraPrecio ni la lógica de navegación restante.

    Persistencia: como el hook ya guarda en cada cambio de estado y vive en App, navegar a 'resumen'
    NO resetea nada (D-02: el estado sigue vivo). Verificá que F5 en el resumen sigue cayendo a 'wizard'
    (vistaInicial no conoce 'resumen' — es aceptable; el usuario retoma el wizard con su estado intacto).
  </action>
  <verify>
    <automated>npx esbuild src/App.jsx src/components/ConfiguratorWizard.jsx --bundle --format=esm --outfile=/dev/null --loader:.js=jsx --loader:.jsx=jsx --external:react --external:react-dom 2>&1 | head -5; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/App.jsx` contiene `usePersistedConfig()` (hook izado a App)
    - `src/App.jsx` contiene la subcadena `vista === 'resumen'`
    - `src/App.jsx` pasa `onVerResumen` a ConfiguratorWizard y `onVolverEditar` a Resumen (grep ambas)
    - `src/components/ConfiguratorWizard.jsx` recibe `estado`, `dispatch` por props (NO llama `usePersistedConfig()` internamente) — grep `usePersistedConfig` en ConfiguratorWizard.jsx devuelve 0
    - `src/components/ConfiguratorWizard.jsx` contiene la subcadena `Ver resumen y presupuesto`
    - `npm run build` completa sin errores (Vite transpila el JSX nuevo)
  </acceptance_criteria>
  <done>App.jsx izó el estado y rutea las 3 vistas; el wizard recibe estado/dispatch por props y ofrece "Ver resumen y presupuesto" en el Paso 6; build OK.</done>
</task>

<task type="auto">
  <name>Task 2: Resumen.jsx (S1-S2) + ConfigSecciones/PresupuestoDesglosado/BloqueFinanciacion/AccionesExport (S3-S6) + generarPDF en exportPDF.js</name>
  <files>src/components/Resumen.jsx, src/components/resumen/ConfigSecciones.jsx, src/components/resumen/PresupuestoDesglosado.jsx, src/components/resumen/BloqueFinanciacion.jsx, src/components/resumen/AccionesExport.jsx, src/utils/exportPDF.js</files>
  <read_first>
    - .planning/phases/06-resumen-y-exportaci-n/06-UI-SPEC.md § Phase 6 Surface Specs S1-S6 (contrato visual completo) + § Copywriting Contract (copy verbatim)
    - .planning/phases/06-resumen-y-exportaci-n/06-PATTERNS.md § Resumen.jsx, § PresupuestoDesglosado.jsx, § ConfigSecciones.jsx, § BloqueFinanciacion.jsx, § AccionesExport.jsx, § exportPDF.js (clases/markup verbatim + análogos con líneas)
    - .planning/phases/06-resumen-y-exportaci-n/06-RESEARCH.md § Pattern 3 (cuerpo verbatim de generarPDF + cableado del ref) + Pitfalls 1-4
    - src/components/wizard/BarraPrecio.jsx (markup verbatim Neto/IVA/Total c/IVA para S4)
    - src/components/wizard/PlanoPanel.jsx (montaje del FloorPlan en card)
    - src/components/wizard/pasos/PasoExtras.jsx (patrón .map data-driven + subheaders de grupo)
    - src/components/FloorPlan.jsx (el <svg viewBox width="100%"> que alimenta tanto pantalla como PDF; EstadoEmpty como patrón de empty state)
    - src/components/ConfiguratorWizard.jsx (className de botones primario/outline y del link "Volver a empezar")
    - src/utils/motorPrecios.js, src/utils/resumenCampos.js, src/utils/exportWhatsApp.js, src/utils/exportPDF.js (parte pura), src/data/contacto.js, src/data/financiacion.js (las APIs que consumen los componentes)
    - src/state/wizardReducer.js (ACCIONES.IR_A_PASO, configDesdeEstado) + src/components/wizard/pasosRegistro.jsx (índices de paso)
  </read_first>
  <action>
    Crear el shell del resumen y sus 4 sub-componentes (en `src/components/resumen/`) + el handler
    browser del PDF. TODO el copy verbatim del Copywriting Contract; TODAS las clases verbatim de las
    Surface Specs S1-S6; reuso total de FloorPlan/BarraPrecio markup/botones del wizard.

    A) `src/components/resumen/PresupuestoDesglosado.jsx` (S4 — D-03/D-04/D-05):
       - `export default function PresupuestoDesglosado({ estado })`.
       - `const { items, neto, iva, total } = detallePresupuesto(estado)`.
       - Card `rounded border border-impacar-texto/10 bg-white/40 p-4`; header `Presupuesto estimado`
         en `text-xl font-semibold`.
       - Línea base del modelo: `items.find((i) => i.categoria === 'modelo')` → `flex justify-between`,
         label izq `text-sm`, `formatPrecio(precioNeto)` der `text-sm`.
       - Accesorios agrupados por el array `GRUPOS` (del bloque <interfaces>): por cada grupo con ≥1
         ítem, subheader `text-xs font-semibold text-impacar-campo` + sus ítems `pl-3 flex justify-between`
         (`text-sm` nombre / `text-sm` precio). OMITIR grupos vacíos.
       - Divisor `border-t border-impacar-texto/10`, luego las 3 líneas Neto/IVA 21%/Total c/IVA con el
         markup VERBATIM de BarraPrecio (Total c/IVA = `font-semibold text-impacar-campo`).
       - Nota orientativa bajo el total: `<p className="text-xs text-impacar-texto/70">Presupuesto orientativo, sujeto a confirmación del asesor comercial.</p>`.
       - Nunca `$NaN`: todo vía `formatPrecio` (el motor ya devuelve 0 en corrupto).

    B) `src/components/resumen/ConfigSecciones.jsx` (S3 — D-02/D-07):
       - `export default function ConfigSecciones({ estado, onEditar })` donde `onEditar(indice)` lo
         provee Resumen.jsx (dispatch IR_A_PASO + onVolverEditar).
       - `const c = resumenCampos(estado)`.
       - Array data-driven de secciones con su índice de paso e id (del bloque <interfaces>):
         `[{ titulo:'Uso y ocupantes', paso:0, valor: `${c.uso} · ${c.ocupantes}` }, { titulo:'Modelo', paso:1, valor: `${c.modelo} — ${c.largo}` }, { titulo:'Baño', paso:2, valor:c.bano }, { titulo:'Dormitorio', paso:3, valor:c.camas }, { titulo:'Cocina y estar', paso:4, valor:c.cocina }, { titulo:'Extras', paso:5, valor: c.extras.length ? c.extras.join(', ') : 'Sin selección' }]`.
         (Alineá las claves a la salida real de `resumenCampos` que dejó 06-01.)
       - Cada sección = card `rounded border border-impacar-texto/10 bg-white/40 p-4 space-y-2`. Header
         row: título `text-xl font-semibold` izq + link `Editar` der (`ml-auto`):
         ```jsx
         <button type="button" onClick={() => onEditar(s.paso)}
           className="min-h-[44px] text-sm font-medium text-impacar-campo underline-offset-2 hover:underline focus:outline-none focus:ring-2 focus:ring-impacar-campo/40">Editar</button>
         ```
       - Valor en `text-sm`; faltante → `Sin selección` (ya viene así de resumenCampos).

    C) `src/components/resumen/BloqueFinanciacion.jsx` (S5 — D-06/RESUMEN-03):
       - Card header `Opciones de financiación` (`text-xl font-semibold`).
       - `FINANCIACION.map((o) => ...)` (import de `../../data/financiacion.js`): `nombre` en
         `text-sm font-semibold`, `detalle` en `text-sm text-impacar-texto/70` debajo; `space-y-3`.
         Solo texto, sin montos. `key={o.id}`.

    D) `src/utils/exportPDF.js` — AÑADIR `generarPDF(svgNode, estado)` (NO tocar nombreArchivoPDF de
       06-01). Copiar el cuerpo de RESEARCH Pattern 3 verbatim:
       ```javascript
       export async function generarPDF(svgNode, estado) {
         const { jsPDF } = await import('jspdf')   // import dinámico: la dep pesada no infla el bundle inicial
         await import('svg2pdf.js')                // side-effect: parchea doc.svg
         const doc = new jsPDF('p', 'mm', 'a4')
         const M = 15, anchoUtil = 210 - M * 2
         doc.setFont('helvetica', 'bold'); doc.setFontSize(22); doc.text('IMPACAR', M, 20)
         doc.setFont('helvetica', 'normal'); doc.setFontSize(10); doc.text('Configurador de casillas rurales', M, 26)
         // Plano vectorial: derivar el alto del viewBox real para NO deformar (Pitfall 3)
         const vb = svgNode?.viewBox?.baseVal
         const planoY = 32
         const planoAlto = vb && vb.width ? anchoUtil * (vb.height / vb.width) : anchoUtil * 0.30
         await doc.svg(svgNode, { x: M, y: planoY, width: anchoUtil, height: planoAlto }) // AWAIT antes de save (Pitfall 1)
         // Presupuesto + config condensada + contacto al pie: bloques doc.text con cursor Y creciente.
         // Usar detallePresupuesto + formatPrecio para las cifras; resumenCampos para la config;
         // CONTACTO (whatsappDisplay/web/instagram/ciudad) al pie. doc.splitTextToSize(t, anchoUtil) para wrap.
         doc.save(nombreArchivoPDF(estado))
       }
       ```
       Importar al tope (estáticos, no dinámicos): `detallePresupuesto` de motorPrecios, `formatPrecio`
       de formato, `resumenCampos`, `CONTACTO`. Reglas (Pitfalls 1-4): `await doc.svg` ANTES de
       `doc.save`; nodo SVG vivo (no string); alto derivado del viewBox; Inter NO se registra (Helvetica
       default es aceptable, Pitfall 4); cifras con `formatPrecio`; contacto desde `CONTACTO`. 1 sola
       página A4 (D-12). Si `svgNode` es null (plano no montado), saltar el bloque del plano sin
       crashear (degradación elegante) — pero en el flujo normal el plano está montado en S2.

    E) `src/components/resumen/AccionesExport.jsx` (S6 — EXPORT-01/02):
       - `export default function AccionesExport({ estado, svgNode, onVolverEditar })`.
       - WhatsApp = `<a href={linkWhatsApp(estado)} target="_blank" rel="noopener noreferrer" ...>` con
         glyph SVG inline (WhatsApp) + `Enviar por WhatsApp`. className primario relleno verde (verbatim
         de ConfiguratorWizard Siguiente). NO usar el verde de marca WhatsApp (#25D366) — usar
         `bg-impacar-campo` (UI-SPEC locked).
       - PDF = `<button type="button" disabled={generando} onClick={onDescargar}>` con glyph download
         inline + `Descargar PDF`. className OUTLINE (verbatim de ConfiguratorWizard Anterior, con
         `disabled:opacity-40 disabled:cursor-not-allowed`). Handler:
         ```jsx
         const [generando, setGenerando] = useState(false)
         const onDescargar = async () => { setGenerando(true); try { await generarPDF(svgNode, estado) } finally { setGenerando(false) } }
         ```
       - Layout botones: `flex flex-col gap-3 sm:flex-row`.
       - Glyphs SVG inline (`viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth={1.5}`,
         patrón IconoUso de PasoUso.jsx — no librería de iconos).
       - Debajo, link global `Volver a editar`: `onClick={onVolverEditar}`, className del link "Volver a
         empezar" del wizard (`text-sm font-medium text-impacar-texto/60 ... hover:text-impacar-cobre hover:underline`, `min-h-[44px]`).

    F) `src/components/Resumen.jsx` (S1-S2 — shell + ref al SVG):
       - `export default function Resumen({ estado, dispatch, onVolverEditar })`.
       - Empty state (degradación, UI-SPEC): si `!estado?.modeloId || !estado?.uso || estado?.ocupantes == null`
         → render heading `Todavía no hay una configuración completa` + body `Vuelva al configurador...`
         + un botón `Volver a editar` (onClick onVolverEditar). (Nota: modeloId arranca en 'N4' por
         defecto en estadoInicial, así que el disparador real del empty es uso/ocupantes null — ajustá
         la condición a uso/ocupantes para no ocultar un resumen con modelo default; documentá la
         elección en un comentario.)
       - Chrome: reusar el header band del wizard (`<header className="bg-impacar-campo px-6 py-4 text-white">` con `IMPACAR` + subtítulo). Container S1: `<main className="mx-auto max-w-3xl px-6 py-12">`.
       - Ritmo vertical (bloques separados por `mt-8`): título `Su casilla, lista para enviar`
         (`text-3xl font-bold leading-tight`) → subtítulo (`text-base text-impacar-texto/70` o similar)
         → **S2 plano** → S3 ConfigSecciones → S4 PresupuestoDesglosado → S5 BloqueFinanciacion →
         S6 AccionesExport.
       - S2 plano (ref para el PDF):
         ```jsx
         const planoRef = useRef(null)
         <div ref={planoRef} className="rounded border border-impacar-texto/10 bg-white/40 p-4">
           <FloorPlan config={configDesdeEstado(estado)} />
         </div>
         ```
         Pasar `svgNode={planoRef.current?.querySelector('svg')}` a AccionesExport. (Como el ref se
         resuelve tras el montaje, AccionesExport debe leer el nodo en el momento del click, no en el
         render: pasá `getSvgNode={() => planoRef.current?.querySelector('svg')}` y que el handler lo
         invoque, O pasá el ref y resolvé dentro del handler. Elegí una y dejala consistente.)
       - `onEditar(indice)` para ConfigSecciones: `dispatch({ type: ACCIONES.IR_A_PASO, paso: indice }); onVolverEditar()`.
       - Sin barra de progreso, sin plano sticky lateral (S1: full-width, una columna).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Existen los 5 archivos: `src/components/Resumen.jsx`, `src/components/resumen/ConfigSecciones.jsx`, `PresupuestoDesglosado.jsx`, `BloqueFinanciacion.jsx`, `AccionesExport.jsx`
    - `Resumen.jsx` contiene `Su casilla, lista para enviar`, `useRef`, `configDesdeEstado` y `Todavía no hay una configuración completa`
    - `PresupuestoDesglosado.jsx` contiene `detallePresupuesto`, `Total c/IVA` y `Presupuesto orientativo, sujeto a confirmación del asesor comercial.`
    - `ConfigSecciones.jsx` contiene `ACCIONES.IR_A_PASO` y `resumenCampos` y la subcadena `Editar`
    - `BloqueFinanciacion.jsx` contiene `FINANCIACION` y `Opciones de financiación` (grep)
    - `AccionesExport.jsx` contiene `linkWhatsApp`, `rel="noopener noreferrer"`, `Enviar por WhatsApp`, `Descargar PDF` y `generando`
    - `src/utils/exportPDF.js` contiene `export async function generarPDF`, `await import('jspdf')`, `await import('svg2pdf.js')`, `doc.svg(` y `viewBox` (alto derivado)
    - `grep -nE 'tenés|querés|podés|elegí|mirá|enviá|revisá|descargá' src/components/Resumen.jsx src/components/resumen/*.jsx` NO devuelve coincidencias (anti-voseo)
    - `npm run build` completa sin errores (jsPDF/svg2pdf resuelven, JSX transpila)
    - `npm test` sigue en verde (sin regresiones de la lógica pura)
  </acceptance_criteria>
  <done>Pantalla de resumen completa: plano + secciones con "Editar" + presupuesto desglosado agrupado + financiación + acciones WhatsApp/PDF; generarPDF embebe el plano vectorial en 1 página A4; build y tests en verde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint visual — PDF vectorial, deep-links "Editar", WhatsApp y financiación en el dev server</name>
  <what-built>
    El flujo de cierre completo: tercera vista 'resumen' con estado compartido, plano final, secciones
    de configuración con "Editar" (IR_A_PASO), presupuesto desglosado agrupado con nota orientativa,
    3 opciones de financiación, y exportación por WhatsApp (link wa.me al número de pruebas
    5492954555113) + descarga de PDF vectorial A4 (jsPDF + svg2pdf.js). Toda la lógica pura está
    cubierta por `npm test`; lo que sigue NO se puede automatizar sin un headless browser (dep nueva
    contraria al patrón del proyecto), por eso se verifica a mano.
  </what-built>
  <how-to-verify>
    1. `npm run dev` y abrir la app en el navegador (idealmente con DevTools en ~375px, target Samsung).
    2. Completar el wizard hasta el Paso 6 (elegir uso, ocupantes, modelo, baño, algunas camas, cocina/
       heladera y un par de extras de confort/energía) y hacer click en **"Ver resumen y presupuesto"**.
    3. **RESUMEN-01:** confirmar que el resumen muestra el **plano final** completo + una sección por
       cada paso (Uso y ocupantes / Modelo / Baño / Dormitorio / Cocina y estar / Extras) con los
       valores traducidos a nombres reales (no ids), y "Sin selección" donde no se eligió nada.
    4. **RESUMEN-02:** confirmar el presupuesto desglosado: línea base del modelo + accesorios agrupados
       por categoría (Baño/Dormitorio/Cocina y estar/Confort/Energía, sin grupos vacíos) + Neto / IVA 21%
       / Total c/IVA, con la nota "Presupuesto orientativo, sujeto a confirmación del asesor comercial.".
       Verificar que el **Total c/IVA coincide** con el que mostraba la BarraPrecio del wizard (misma suma).
    5. **RESUMEN-03:** confirmar las **3 opciones de financiación** (Pago contado / Financiación en
       cuotas / Permuta) con su detalle, sin montos.
    6. **EXPORT-01:** click en **"Enviar por WhatsApp"** → debe abrir WhatsApp (web/app) con el mensaje
       pre-armado en trato de usted (modelo, ocupantes, baño, camas, cocina, extras, Total c/IVA, nota
       orientativa y la línea final "Le envío aparte el PDF con el plano y el detalle."), al número de
       PRUEBAS 5492954555113. No enviar; solo verificar el texto.
    7. **EXPORT-02:** click en **"Descargar PDF"** → el botón se deshabilita mientras genera; se descarga
       `presupuesto-impacar-Nx.pdf`. Abrir el PDF: confirmar **1 sola página A4**, logo IMPACAR arriba,
       el **plano como vector** (hacer ZOOM fuerte: NO debe pixelar — si pixela, es html2canvas/raster,
       falla el constraint), el presupuesto (total + nota), la config condensada y el contacto al pie
       (WhatsApp + industriaimpacar.com + @industriasimpacar + General Pico, La Pampa).
    8. **Deep-links (RESUMEN-01/D-02):** en cada sección del resumen, click en **"Editar"** → debe
       abrir el paso correspondiente del wizard con el estado intacto. Volver al resumen (Paso 6 →
       "Ver resumen y presupuesto") y verificar que los cambios se reflejan.
    9. **Volver a editar global:** el link "Volver a editar" regresa al wizard sin resetear el estado.
    10. **Degradación:** (opcional) en una pestaña limpia / con "Volver a empezar", entrar al resumen sin
        completar uso/ocupantes → debe mostrar el empty state "Todavía no hay una configuración completa"
        y NUNCA `$NaN` ni un crash.
  </how-to-verify>
  <resume-signal>Escriba "approved" si todo pasa, o describa los problemas (p.ej. "el plano pixela en el PDF", "el total del resumen no coincide", "Editar de Baño abre el paso equivocado").</resume-signal>
  <files>(verificación visual — ninguno; los archivos los crearon las Tasks 1-2)</files>
  <action>Checkpoint de verificación humana (no autónomo). Antes de pausar, Claude deja el dev server listo: confirma que `npm run build` y `npm test` están en verde, arranca `npm run dev` y deja la app en la vista resumen alcanzable desde el Paso 6. Luego presenta los pasos de <how-to-verify> al usuario y espera su confirmación. NO hay trabajo de implementación nuevo en esta task: solo verificar lo construido en las Tasks 1-2 (PDF vectorial, deep-links "Editar", WhatsApp pre-armado, financiación, empty state).</action>
  <verify>
    <automated>npm test &amp;&amp; npm run build</automated>
  </verify>
  <done>El usuario respondió "approved" tras verificar en el dev server: PDF vectorial de 1 página A4 (no pixela al zoom) con logo+plano+presupuesto+config+contacto; "Editar" por sección abre el paso correcto con estado intacto; WhatsApp abre el mensaje pre-armado al número de pruebas; financiación lista las 3 opciones; total del resumen coincide con BarraPrecio; empty state sin $NaN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage (`impacar_config_v1`) → Resumen/componentes | Estado restaurado no confiable; el resumen solo LEE (no escribe estado nuevo salvo IR_A_PASO/navegación). |
| App → wa.me (`target="_blank"`) | Apertura de un dominio externo en pestaña nueva. |
| Estado interno → SVG → PDF | El SVG lo genera la propia app desde datos internos (no HTML user-supplied). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Tampering / tab hijack | `AccionesExport` `<a target="_blank">` a wa.me (reverse tabnabbing) | mitigate | `rel="noopener noreferrer"` en el `<a>` (UI-SPEC S6, acceptance criteria lo grep-verifica). |
| T-06-08 | Tampering | `Resumen`/`ConfigSecciones`/`PresupuestoDesglosado` leen estado adulterado de localStorage | mitigate | Consumen `detallePresupuesto`/`resumenCampos`/`configDesdeEstado`, todos tolerantes (0, 'Sin selección', 'Modelo no disponible'); empty state cuando uso/ocupantes null. Sin `$NaN`, sin crash. Verificado en checkpoint paso 10. |
| T-06-09 | Tampering (DoS) | `generarPDF` con `svgNode` null o estado corrupto | mitigate | Guard `svgNode?.viewBox?.baseVal`; si no hay nodo, salta el bloque del plano sin crashear. El botón queda `disabled` durante el await (no doble-click). |
| T-06-10 | — (no aplica) | XSS al embeber el SVG en el PDF | accept | El SVG es generado por la app desde datos internos (no HTML externo); svg2pdf no ejecuta scripts. Sin acción. |

**Nivel de riesgo: BAJO.** Sin auth, sin datos sensibles, sin inputs de red. Ningún threat `high`.
</threat_model>

<verification>
- `npm run build` sin errores (Tasks 1-2): App izado, 3 vistas, jsPDF/svg2pdf resuelven, JSX transpila.
- `npm test` en verde (sin regresiones de la lógica pura de Wave 1).
- grep de acceptance criteria: copy verbatim presente, `rel="noopener noreferrer"`, `IR_A_PASO`,
  `doc.svg`, `await import('jspdf')`, anti-voseo = 0.
- Checkpoint humano (Task 3): PDF vectorial 1 página A4, deep-links "Editar", WhatsApp pre-armado al
  número de pruebas, financiación 3 opciones, total coincidente con BarraPrecio, empty state sin $NaN.
</verification>

<success_criteria>
- Tercera vista 'resumen' con estado compartido vía usePersistedConfig izado a App.jsx (D-01).
- Plano final + secciones traducidas con "Editar" (IR_A_PASO) por sección (RESUMEN-01, D-02/D-07).
- Presupuesto desglosado agrupado + Neto/IVA/Total c/IVA + nota orientativa (RESUMEN-02, D-03/D-04/D-05).
- 3 opciones de financiación, solo texto (RESUMEN-03, D-06).
- WhatsApp: `<a href={linkWhatsApp(estado)} target=_blank rel=noopener noreferrer>` (EXPORT-01, D-08/D-09/D-10).
- PDF: plano vectorial + logo + presupuesto + config + contacto en 1 página A4 (EXPORT-02, D-11/D-12/D-13).
- Degradación elegante (empty state, sin $NaN); checkpoint visual aprobado.
</success_criteria>

<output>
After completion, create `.planning/phases/06-resumen-y-exportaci-n/06-03-SUMMARY.md`
</output>
