---
phase: 06-resumen-y-exportaci-n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/utils/motorPrecios.js
  - src/utils/motorPrecios.test.js
  - src/utils/resumenCampos.js
  - src/utils/resumenCampos.test.js
  - src/utils/exportPDF.js
  - src/utils/exportPDF.test.js
autonomous: true
requirements: [RESUMEN-01, RESUMEN-02, EXPORT-02]
must_haves:
  truths:
    - "detallePresupuesto(estado) devuelve { items, neto, iva, total } y sus totales son idénticos a calcularPresupuesto (una sola fuente de la suma)"
    - "Cada ítem del desglose lleva categoria/subgrupo para agruparlo (Baño/Dormitorio/Cocina y estar/Confort/Energía)"
    - "resumenCampos(estado) traduce ids a nombres reales (uso, modelo+largo, baño, camas C/S/M, cocina/heladera/mesa, extras) y degrada a 'Sin selección' sin undefined"
    - "nombreArchivoPDF(estado) devuelve presupuesto-impacar-<modeloId>.pdf y ...-sin-modelo.pdf cuando no hay modelo"
    - "Estado adulterado (modeloId inexistente, extras no-array, null) nunca produce $NaN ni crashea; el modelo cae a 'Modelo no disponible'"
    - "jspdf@4.2.1 y svg2pdf.js@2.7.0 quedan en dependencies con versión exacta"
  artifacts:
    - path: "src/utils/motorPrecios.js"
      provides: "detallePresupuesto(estado) compuesto sobre calcularPresupuesto"
      exports: ["calcularPresupuesto", "detallePresupuesto"]
    - path: "src/utils/resumenCampos.js"
      provides: "Traducción estado→labels legibles para el resumen y el mensaje WhatsApp"
      exports: ["resumenCampos"]
    - path: "src/utils/exportPDF.js"
      provides: "Parte pura nombreArchivoPDF (el handler browser generarPDF llega en 06-03)"
      exports: ["nombreArchivoPDF"]
    - path: "package.json"
      provides: "deps jspdf+svg2pdf.js exactas + 3 test files nuevos en el script test"
      contains: "\"jspdf\": \"4.2.1\""
  key_links:
    - from: "src/utils/motorPrecios.js detallePresupuesto"
      to: "calcularPresupuesto (mismo archivo)"
      via: "const { neto, iva, total } = calcularPresupuesto(estado)"
      pattern: "calcularPresupuesto\\(estado\\)"
    - from: "src/utils/resumenCampos.js"
      to: "MODELOS / EXTRAS (/data)"
      via: ".find / .filter sobre /data (anti-hardcodeo)"
      pattern: "(MODELOS|EXTRAS)\\.(find|filter|map)"
---

<objective>
Construir la lógica pura testeable de la pantalla de cierre — **antes** que cualquier componente
(interface-first, patrón de Fases 4-5) — e instalar las dependencias de runtime del PDF.

Tres utilidades puras:
1. `detallePresupuesto(estado)` (extiende `motorPrecios.js`, D-05): desglose ítem-por-ítem que
   **compone sobre** `calcularPresupuesto` sin re-sumar (RESUMEN-02).
2. `resumenCampos(estado)` (nuevo, RESUMEN-01): traduce ids→nombres reales para las secciones del
   resumen y el cuerpo del mensaje WhatsApp.
3. `nombreArchivoPDF(estado)` (parte pura de `exportPDF.js`, EXPORT-02): nombre del archivo descargado.

Más la instalación de `jspdf@4.2.1` + `svg2pdf.js@2.7.0` (D-11) con versiones exactas, y el registro
de los nuevos test files en el script `test`.

Purpose: aislar toda la lógica con I/O determinístico para testearla con `node:test` (sin deps de
test nuevas) y dejar las dependencias del PDF listas, de modo que el Plan 06-03 (componentes +
handler browser del PDF) ensamble piezas ya verificadas.

Output: `motorPrecios.js` extendido + 3 utilidades puras con sus tests en verde, `package.json` con
las 2 deps de runtime y los 3 test files nuevos en el script `test`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-resumen-y-exportaci-n/06-CONTEXT.md
@.planning/phases/06-resumen-y-exportaci-n/06-RESEARCH.md
@.planning/phases/06-resumen-y-exportaci-n/06-PATTERNS.md

<interfaces>
<!-- Contratos exactos extraídos del codebase. El executor NO necesita explorar para conocerlos. -->

From src/utils/motorPrecios.js (la función sobre la que se COMPONE — NO reescribir):
```javascript
import { MODELOS } from '../data/models.js'
import { EXTRAS } from '../data/extras.js'
import { calcularIVA, calcularTotal } from './formato.js'

export function calcularPresupuesto(estado) {
  const modelo = MODELOS.find((m) => m.id === estado?.modeloId)
  const base = modelo?.precioNeto ?? 0          // modeloId inválido/ausente → base 0
  const ids = Array.isArray(estado?.extras) ? estado.extras : []  // no-array → suma 0
  const sumaExtras = EXTRAS.filter((e) => ids.includes(e.id)).reduce((acc, e) => acc + e.precioNeto, 0)
  const neto = base + sumaExtras
  return { neto, iva: calcularIVA(neto), total: calcularTotal(neto) }
}
```

From src/data/models.js (MODELOS — para itemBase y para resumenCampos):
```javascript
{ id: 'N1', nombre: 'N1', largo: 4.5,  camasBase: 2,    precioNeto: 29108976, ocupantesIdeal: 2, personalizable: false }
{ id: 'N4', nombre: 'N4', largo: 6.6,  camasBase: 4,    precioNeto: 38971845, ocupantesIdeal: 4, personalizable: false }
{ id: 'N7', nombre: 'N7', largo: 9.6,  camasBase: null, precioNeto: 46180786, ocupantesIdeal: 8, personalizable: true  }
// largo es Number; mostrar con coma decimal (6.6 → "6,6 m"), igual que FloorPlan.metros (toFixed→replace('.', ','))
```

From src/data/extras.js (EXTRAS — labels reales + categoria/subgrupo para agrupar):
```javascript
{ id: 'inodoro-septica',  nombre: 'Inodoro con depósito y cámara séptica', precioNeto: 495000,  categoria: 'bano' }
{ id: 'cajonera-bajo-cama', nombre: 'Cajonera bajo cama (x4)',             precioNeto: 322000,  categoria: 'dormitorio' }
{ id: 'cocina-horno',     nombre: 'Cocina con horno industrial',          precioNeto: 585800,  categoria: 'cocina' }
{ id: 'heladera-220',     nombre: 'Heladera con freezer 220V cat A++',    precioNeto: 1699000, categoria: 'cocina' }
{ id: 'heladera-12v',     nombre: 'Heladera con freezer 12V + pantalla + regulador', precioNeto: 1690000, categoria: 'cocina' }
{ id: 'mesa-cano',        nombre: 'Mesa de caño + melamina + aluminio',   precioNeto: 218000,  categoria: 'cocina' }
{ id: 'calefactor-4000',  nombre: 'Calefactor 4000 CAL', precioNeto: 342000, categoria: 'extras', subgrupo: 'confort' }
{ id: 'panel-solar-160',  nombre: 'Panel solar 160W + regulador 30A (12V)', precioNeto: 410000, categoria: 'extras', subgrupo: 'energia' }
// categoria ∈ {bano, dormitorio, cocina, extras}; subgrupo ∈ {confort, energia} SOLO en categoria 'extras'.
```

From src/state/wizardReducer.js (estadoInicial + configDesdeEstado — patrón de proyección tolerante):
```javascript
export const estadoInicial = {
  pasoActual: 0, uso: null, ocupantes: null, modeloId: 'N4',
  bano: { tamano: 'estandar' },            // 'estandar' | 'ampliado'
  dormitorio: { camas: [] },               // Array<{ tipo: 'C' | 'S' | 'M' }>
  extras: [],                              // Array<string> ids de accesorios
}
// configDesdeEstado deriva cocina/heladera/mesa de extras[] (una sola fuente, D-14):
//   heladera = extras.includes('heladera-220') ? 'heladera-220' : extras.includes('heladera-12v') ? 'heladera-12v' : null
//   cocina = { horno: extras.includes('cocina-horno'), heladera };  estar = { mesa: extras.includes('mesa-cano') }
```

From src/components/wizard/pasos/PasoUso.jsx (USOS — conjunto de UI, labels de uso):
```javascript
const USOS = [
  { id: 'contratista', label: 'Contratista rural' },
  { id: 'ganadero', label: 'Ganadero' },
  { id: 'agricola', label: 'Agrícola' },
  { id: 'vivienda', label: 'Vivienda' },
  { id: 'otro', label: 'Otro' },
]
```

From src/components/wizard/pasos/PasoDormitorio.jsx (TIPOS — labels de camas + helper contar):
```javascript
const TIPOS = [
  { tipo: 'C', label: 'Cucheta marinera (C)', singular: 'cucheta' },
  { tipo: 'S', label: 'Cama simple (S)', singular: 'cama simple' },
  { tipo: 'M', label: 'Matrimonial (M)', singular: 'matrimonial' },
]
const contar = (tipo) => camas.filter((c) => c.tipo === tipo).length
```

From src/utils/formato.js:
```javascript
export function formatPrecio(n)   // → '$38.971.845' (argentino)
export function calcularIVA(neto) // (Number(neto) || 0) * IVA
export function calcularTotal(neto)
```

From src/utils/motorPrecios.test.js (constantes reales para asserts, sin hardcodear):
```javascript
const N4_NETO = 38971845
// N4 + los 17 extras reales → neto 55977868, total 67733220 (Math.round)
const todosLosIds = EXTRAS.map((e) => e.id) // patrón anti-hardcodeo
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar jspdf + svg2pdf.js (versiones exactas) y registrar tests nuevos en package.json</name>
  <files>package.json</files>
  <read_first>
    - package.json (el archivo a modificar — script `test` actual y `dependencies`)
    - .planning/phases/06-resumen-y-exportaci-n/06-RESEARCH.md § Standard Stack + Environment Availability (versiones verificadas jspdf@4.2.1 / svg2pdf.js@2.7.0, Node v24.14.1, npm 11.11.0)
    - .planning/phases/06-resumen-y-exportaci-n/06-PATTERNS.md § package.json (cambios exactos)
  </read_first>
  <action>
    1. Instalar las dependencias de runtime con versiones EXACTAS (sin caret), D-11:
       `npm install jspdf@4.2.1 svg2pdf.js@2.7.0`
       Esto deja en `dependencies` exactamente `"jspdf": "4.2.1"` y `"svg2pdf.js": "2.7.0"` (npm
       fija la versión instalada; si quedara con `^`, editá package.json para dejar la versión exacta).
       Son las primeras deps de runtime más allá de React — es la "única dependencia pesada
       justificada" de PROJECT.md (EXPORT-02, plano vectorial).
    2. Extender el script `test` de package.json añadiendo AL FINAL los 3 archivos nuevos de esta
       fase (el runner `node --test` los lista explícitamente):
       `src/utils/resumenCampos.test.js src/utils/exportPDF.test.js src/utils/exportWhatsApp.test.js`
       NO tocar los archivos ya listados; `motorPrecios.test.js` ya está en el script (se extiende en
       Task 2, no se re-agrega). **Este plan es el ÚNICO dueño de `package.json` en Wave 1** (06-02 NO
       toca package.json): por eso registra los 3 test files acá, incluido `exportWhatsApp.test.js` que
       crea 06-02 en la misma wave. `npm test` lo encontrará una vez que 06-02 cree ese archivo (ambos
       planes terminan antes de Wave 2).
    3. Vetar las deps en install (V14): confirmar que `npm install` no reportó vulnerabilidades de
       severidad alta/crítica (`npm audit --omit=dev` no debe listar `high`/`critical` para estas dos).
       Si las hubiera, detenerse y reportarlo (security_block_on: high).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.dependencies.jspdf!=='4.2.1') throw new Error('jspdf no es 4.2.1'); if(p.dependencies['svg2pdf.js']!=='2.7.0') throw new Error('svg2pdf.js no es 2.7.0'); if(!p.scripts.test.includes('resumenCampos.test.js')||!p.scripts.test.includes('exportPDF.test.js')||!p.scripts.test.includes('exportWhatsApp.test.js')) throw new Error('faltan test files en script test'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies contiene `"jspdf": "4.2.1"` (versión exacta, sin `^`)
    - `package.json` dependencies contiene `"svg2pdf.js": "2.7.0"` (versión exacta, sin `^`)
    - `package.json` scripts.test contiene la subcadena `src/utils/resumenCampos.test.js`
    - `package.json` scripts.test contiene la subcadena `src/utils/exportPDF.test.js`
    - `package.json` scripts.test contiene la subcadena `src/utils/exportWhatsApp.test.js`
    - El comando de verify arriba imprime `OK` y sale 0
    - `node_modules/jspdf` y `node_modules/svg2pdf.js` existen tras el install
  </acceptance_criteria>
  <done>jspdf@4.2.1 y svg2pdf.js@2.7.0 instalados con versión exacta; los 3 test files nuevos registrados en el script test; sin vulnerabilidades high/critical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: detallePresupuesto(estado) en motorPrecios.js + tests (RESUMEN-02)</name>
  <files>src/utils/motorPrecios.js, src/utils/motorPrecios.test.js</files>
  <read_first>
    - src/utils/motorPrecios.js (el archivo a extender — calcularPresupuesto se REUSA, NO se reescribe)
    - src/utils/motorPrecios.test.js (el archivo de tests a extender — header node:test, constantes N4_NETO=38971845, patrón todosLosIds=EXTRAS.map)
    - src/data/models.js (MODELOS: id/nombre/largo/precioNeto para el ítem base)
    - src/data/extras.js (EXTRAS: id/nombre/precioNeto/categoria/subgrupo para los ítems de accesorios)
    - .planning/phases/06-resumen-y-exportaci-n/06-RESEARCH.md § Pattern 1 (cuerpo verbatim de detallePresupuesto)
    - .planning/phases/06-resumen-y-exportaci-n/06-PATTERNS.md § motorPrecios.js (patrón clave a replicar)
  </read_first>
  <behavior>
    - Test: `detallePresupuesto({ modeloId:'N4', extras:[] })` → `items[0] = { id:'N4', label:'Casilla N4 — 6,6 m', precioNeto:38971845, categoria:'modelo' }` y `{neto,iva,total}` == `calcularPresupuesto({modeloId:'N4',extras:[]})` (paridad byte a byte).
    - Test: con `extras:['inodoro-septica','calefactor-4000']` los ítems de accesorios aparecen en el ORDEN de EXTRAS, cada uno con `{ id, label:e.nombre, precioNeto:e.precioNeto, categoria:e.categoria, subgrupo:e.subgrupo }`.
    - Test: los ítems son agrupables por categoria/subgrupo — al menos un ítem por categoria 'bano' / 'dormitorio' / 'cocina' / 'extras'+subgrupo 'confort' / 'extras'+subgrupo 'energia' cuando se seleccionan los extras correspondientes.
    - Test (paridad total): `detallePresupuesto` con N4 + los 17 extras reales → neto 55977868, total Math.round 67733220 (idéntico a calcularPresupuesto).
    - Test (estado adulterado): `modeloId:'XX'` → `items[0] = { id:'sin-modelo', label:'Modelo no disponible', precioNeto:0, categoria:'modelo' }`, neto 0, sin $NaN.
    - Test (estado adulterado): `extras:'no-array'` → solo ítem base, sin crashear. `detallePresupuesto(null)` → ítem base 'Modelo no disponible', neto/iva/total 0.
  </behavior>
  <action>
    Añadir a `src/utils/motorPrecios.js` (NO reescribir calcularPresupuesto) la función
    `detallePresupuesto(estado)`. Importar `import { calcularPresupuesto }` NO es necesario — está en
    el mismo archivo; llamarla directamente. Importar `MODELOS` y `EXTRAS` (ya importados arriba).

    Cuerpo exacto (de RESEARCH Pattern 1 — copiar verbatim, ajustando el largo a coma decimal):
    ```javascript
    export function detallePresupuesto(estado) {
      const modelo = MODELOS.find((m) => m.id === estado?.modeloId)
      const ids = Array.isArray(estado?.extras) ? estado.extras : []
      const itemBase = modelo
        ? { id: modelo.id, label: `Casilla ${modelo.nombre} — ${String(modelo.largo).replace('.', ',')} m`, precioNeto: modelo.precioNeto, categoria: 'modelo' }
        : { id: 'sin-modelo', label: 'Modelo no disponible', precioNeto: 0, categoria: 'modelo' }
      const itemsExtras = EXTRAS
        .filter((e) => ids.includes(e.id))
        .map((e) => ({ id: e.id, label: e.nombre, precioNeto: e.precioNeto, categoria: e.categoria, subgrupo: e.subgrupo }))
      const { neto, iva, total } = calcularPresupuesto(estado) // ÚNICA fuente de la suma — NO re-sumar
      return { items: [itemBase, ...itemsExtras], neto, iva, total }
    }
    ```
    Reglas críticas (anti-patterns de RESEARCH): NO re-`reduce` los totales (divergiría de BarraPrecio);
    el largo se muestra con coma decimal (`6.6` → `'6,6 m'`); modelo ausente → 'Modelo no disponible'
    (degradación elegante, UI-SPEC error fallback). Mantené el comentario explicativo de "una sola
    fuente de la suma" arriba de la función, en español.

    Extender `src/utils/motorPrecios.test.js` con los tests del bloque <behavior> (header e imports
    ya están; agregar `import { detallePresupuesto } from './motorPrecios.js'`). Reusar las constantes
    existentes (N4_NETO, el patrón `EXTRAS.map((e) => e.id)`) — sin hardcodear los 17 strings.
  </action>
  <verify>
    <automated>node --test src/utils/motorPrecios.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/utils/motorPrecios.js` exporta `function detallePresupuesto` (grep: `export function detallePresupuesto`)
    - `src/utils/motorPrecios.js` NO contiene un segundo `.reduce(` para totales en detallePresupuesto (la suma se delega) — grep `detallePresupuesto` seguido del cuerpo NO incluye `.reduce`
    - `src/utils/motorPrecios.js` contiene la subcadena `Modelo no disponible`
    - `src/utils/motorPrecios.js` contiene `calcularPresupuesto(estado)` dentro de detallePresupuesto (paridad de totales)
    - `node --test src/utils/motorPrecios.test.js` sale 0 con todos los tests nuevos + existentes en verde
    - El test de paridad asserta `detalle.total === calcularPresupuesto(estado).total`
  </acceptance_criteria>
  <done>detallePresupuesto devuelve items[] (base+extras con categoria/subgrupo) + neto/iva/total idénticos a calcularPresupuesto; tolera estado adulterado sin $NaN; suite de motorPrecios en verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: resumenCampos.js + nombreArchivoPDF (exportPDF.js parte pura) + tests (RESUMEN-01, EXPORT-02)</name>
  <files>src/utils/resumenCampos.js, src/utils/resumenCampos.test.js, src/utils/exportPDF.js, src/utils/exportPDF.test.js</files>
  <read_first>
    - src/state/wizardReducer.js (configDesdeEstado: patrón de proyección pura tolerante a tampering; estadoInicial: uso/ocupantes arrancan null)
    - src/data/models.js (MODELOS: nombre + largo con coma decimal)
    - src/data/extras.js (EXTRAS: nombres reales de cocina-horno/heladera-220/heladera-12v/mesa-cano + confort/energia)
    - src/components/wizard/pasos/PasoUso.jsx (array USOS con id→label de uso)
    - src/components/wizard/pasos/PasoDormitorio.jsx (array TIPOS con tipo→label de camas + helper contar)
    - src/utils/formato.test.js (header node:test a copiar para los tests nuevos)
    - .planning/phases/06-resumen-y-exportaci-n/06-PATTERNS.md § resumenCampos.js + § exportPDF.js (mapeos id→label, degradación 'Sin selección')
    - .planning/phases/06-resumen-y-exportaci-n/06-UI-SPEC.md § Copywriting Contract (Empty section value = 'Sin selección'; PDF filename)
  </read_first>
  <behavior>
    resumenCampos(estado):
    - Test: `resumenCampos({ modeloId:'N4', uso:'ganadero', ocupantes:4, bano:{tamano:'ampliado'}, dormitorio:{camas:[{tipo:'C'},{tipo:'C'},{tipo:'M'}]}, extras:['cocina-horno','heladera-220','mesa-cano','calefactor-4000'] })` devuelve un objeto con: `modelo` = 'N4', `largo` = '6,6 m', `uso` = 'Ganadero', `ocupantes` = 4 (o '4'), `bano` = 'Ampliado', `camas` describe 2 cuchetas + 1 matrimonial (texto legible, ej. '2 cucheta marinera, 1 matrimonial' o C/S/M con cantidades), `cocina` menciona horno + heladera 220V, `extras` = array de nombres reales de confort/energía (ej. incluye 'Calefactor 4000 CAL').
    - Test: estado vacío (`uso:null, ocupantes:null, modeloId:'XX'`, sin camas, extras []) → cada campo cae a 'Sin selección' (o 'Modelo no disponible' para el modelo), NUNCA undefined / NUNCA '' / NUNCA 'null'.
    - Test: `resumenCampos(null)` y `resumenCampos({ extras:'no-array' })` no crashean (Array.isArray + optional chaining); extras → 'Sin selección'.
    nombreArchivoPDF(estado):
    - Test: `nombreArchivoPDF({ modeloId:'N4' })` === 'presupuesto-impacar-N4.pdf'.
    - Test: `nombreArchivoPDF({})` === 'presupuesto-impacar-sin-modelo.pdf'; `nombreArchivoPDF(null)` === 'presupuesto-impacar-sin-modelo.pdf'.
  </behavior>
  <action>
    Crear `src/utils/resumenCampos.js` — función pura `resumenCampos(estado)` que traduce el estado a
    labels legibles para las secciones del resumen (06-03) y el cuerpo del mensaje WhatsApp (otro plan).
    Anti-hardcodeo: leer de `/data` (`MODELOS.find`, `EXTRAS.filter`) y reusar los conjuntos de UI USOS
    (de PasoUso) y TIPOS (de PasoDormitorio). Como USOS/TIPOS hoy son `const` locales NO exportados,
    DUPLICÁ los arrays dentro de resumenCampos.js con un comentario `// Conjunto de UI duplicado de
    PasoUso.jsx / PasoDormitorio.jsx (no están exportados) — mantener sincronizados` (no muevas los
    originales para no compartir archivos entre planes de esta wave). Mapeos exactos:
      - uso: `USOS.find((u) => u.id === estado?.uso)?.label ?? 'Sin selección'`
      - modelo: `MODELOS.find((m) => m.id === estado?.modeloId)?.nombre ?? 'Modelo no disponible'`
      - largo: del mismo modelo → `String(largo).replace('.', ',') + ' m'`, o 'Sin selección' si no hay modelo
      - ocupantes: `estado?.ocupantes ?? 'Sin selección'`
      - bano: `estado?.bano?.tamano === 'ampliado' ? 'Ampliado' : estado?.bano?.tamano === 'estandar' ? 'Estándar' : 'Sin selección'`
      - camas: `const camas = Array.isArray(estado?.dormitorio?.camas) ? estado.dormitorio.camas : []`; contar por tipo con `camas.filter((c) => c.tipo === t).length` usando TIPOS; armar texto legible (las que tengan count>0, con su `label` o `singular` + cantidad); `[] → 'Sin selección'`.
      - cocina/heladera/mesa: derivar de `extras` como configDesdeEstado (`extras.includes('cocina-horno')`, heladera 220/12v, `mesa-cano`) y traducir a nombres de EXTRAS; nada elegido → 'Sin selección'.
      - extras (confort/energía): `EXTRAS.filter((e) => e.categoria === 'extras' && extras.includes(e.id)).map((e) => e.nombre)`; array vacío representado para que el consumidor muestre 'Sin selección'.
    Devolvé un objeto con claves estables: `{ uso, ocupantes, modelo, largo, bano, camas, cocina, extras }`
    (extras como array de nombres; el resto strings ya legibles). Documentá el shape arriba de la función.

    Crear `src/utils/exportPDF.js` con la PARTE PURA `nombreArchivoPDF(estado)` (el handler browser
    `generarPDF` se agrega en 06-03 — dejá un comentario `// generarPDF(svgNode, estado): browser-side,
    se implementa en el Plan 06-03 (jsPDF + svg2pdf.js).`):
    ```javascript
    export function nombreArchivoPDF(estado) {
      return `presupuesto-impacar-${estado?.modeloId ?? 'sin-modelo'}.pdf`
    }
    ```

    Crear `src/utils/resumenCampos.test.js` y `src/utils/exportPDF.test.js` con node:test (header de
    formato.test.js: `import { test } from 'node:test'` + `import assert from 'node:assert/strict'`),
    cubriendo el bloque <behavior>. Replicar el patrón de estado-adulterado de motorPrecios.test.js
    (modeloId inexistente, extras no-array, estado null).
  </action>
  <verify>
    <automated>node --test src/utils/resumenCampos.test.js src/utils/exportPDF.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/utils/resumenCampos.js` exporta `function resumenCampos` (grep: `export function resumenCampos`)
    - `src/utils/resumenCampos.js` contiene la subcadena `Sin selección`
    - `src/utils/resumenCampos.js` contiene `MODELOS.find` y `EXTRAS.filter` (anti-hardcodeo, lee de /data)
    - `src/utils/exportPDF.js` exporta `function nombreArchivoPDF` (grep: `export function nombreArchivoPDF`)
    - `src/utils/exportPDF.js` contiene la subcadena `sin-modelo`
    - `node --test src/utils/resumenCampos.test.js src/utils/exportPDF.test.js` sale 0
    - Un test asserta `nombreArchivoPDF({ modeloId:'N4' }) === 'presupuesto-impacar-N4.pdf'`
    - Un test asserta que `resumenCampos` con estado vacío no produce ningún valor `undefined`
  </acceptance_criteria>
  <done>resumenCampos traduce ids→nombres reales con 'Sin selección' en faltantes; nombreArchivoPDF produce el filename correcto; ambos test files en verde; `npm test` completo en verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage (`impacar_config_v1`) → utilidades puras | El estado restaurado es entrada NO confiable (el usuario puede editar localStorage). `usePersistedConfig` ya valida la forma mínima al cargar, pero las utilidades igual se defienden. |
| npm registry → bundle (deps nuevas) | jspdf/svg2pdf.js son código de terceros incorporado al runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `detallePresupuesto` / `resumenCampos` (estado de localStorage adulterado) | mitigate | `Array.isArray(estado?.extras)`, optional chaining (`estado?.modeloId`, `estado?.bano?.tamano`, `estado?.dormitorio?.camas`), defaults seguros ('Sin selección' / 'Modelo no disponible'); los totales se delegan a `calcularPresupuesto` que ya devuelve 0, nunca `$NaN`. Patrón Fases 4-5 verificado en codebase. Tests del bloque <behavior> cubren modeloId inexistente, extras no-array y estado null. |
| T-06-02 | Tampering (supply chain, ASVS V14) | `package.json` deps `jspdf`/`svg2pdf.js` | mitigate | Versiones EXACTAS pinneadas (`4.2.1` / `2.7.0`, par coordinado verificado en RESEARCH). Vetar en install: `npm audit` no debe reportar high/critical (security_block_on: high). jsPDF v4.x es línea de releases de seguridad. |
| T-06-03 | Information Disclosure | nombreArchivoPDF (filename con modeloId) | accept | El modeloId es un id de catálogo público (N1-N7), sin PII ni dato sensible. Riesgo nulo. |

**Nivel de riesgo: BAJO.** Sin auth, sin datos sensibles, sin inputs de red. Ningún threat `high` que bloquee.
</threat_model>

<verification>
- `npm test` (suite completa de lógica pura) en verde, incluyendo los tests nuevos de detallePresupuesto,
  resumenCampos y nombreArchivoPDF, más los existentes (regresión).
- `node -e` de package.json confirma versiones exactas y test files registrados.
- Paridad de totales: el test asserta `detallePresupuesto(estado).total === calcularPresupuesto(estado).total`
  (no hay segunda fuente de la suma).
- Anti-hardcodeo: grep confirma `.find`/`.filter` sobre MODELOS/EXTRAS (no ids ni precios literales).
</verification>

<success_criteria>
- jspdf@4.2.1 + svg2pdf.js@2.7.0 en dependencies con versión exacta (D-11).
- `detallePresupuesto(estado)` devuelve { items, neto, iva, total } con totales idénticos a
  `calcularPresupuesto` y items con categoria/subgrupo para agrupar (RESUMEN-02, D-03/D-04/D-05).
- `resumenCampos(estado)` traduce ids→nombres reales con 'Sin selección' en faltantes (RESUMEN-01, D-07).
- `nombreArchivoPDF(estado)` → presupuesto-impacar-<modelo|sin-modelo>.pdf (EXPORT-02, D-discretion).
- Toda la lógica tolera estado adulterado sin $NaN ni crash (degradación elegante).
- `npm test` en verde.
</success_criteria>

<output>
After completion, create `.planning/phases/06-resumen-y-exportaci-n/06-01-SUMMARY.md`
</output>
