---
phase: 02-motor-de-plano-svg
plan: 02
type: execute
wave: 2
depends_on: [1]
files_modified:
  - src/components/FloorPlan.jsx
  - src/components/FloorPlanElements/Door.jsx
  - src/components/FloorPlanElements/Window.jsx
  - src/components/FloorPlanElements/Bathroom.jsx
  - src/components/FloorPlanElements/Bed.jsx
  - src/components/FloorPlanElements/Kitchen.jsx
  - src/components/FloorPlanElements/Table.jsx
autonomous: true
requirements: [PLANO-01, PLANO-04]
user_setup: []

must_haves:
  truths:
    - "Dado un modelo, el plano dibuja paredes (perímetro), puerta con arco, ventanas y acotaciones en metros"
    - "El <svg> escala al contenedor (viewBox + preserveAspectRatio xMidYMid meet, width 100%)"
    - "Las 5 zonas se dibujan con sus colores (baño celeste, dormitorio marrón, cocina verde, baulera/estar neutro)"
    - "Las camas se dibujan como rectángulos con etiqueta C/S/M contra las paredes largas y pasillo central"
    - "Config nula muestra estado Empty; geometría inválida muestra estado Error"
  artifacts:
    - path: "src/components/FloorPlan.jsx"
      provides: "Componente principal: <svg> role=img + <title>, paredes, zonas, puerta, ventanas, cotas, estados"
      min_lines: 90
    - path: "src/components/FloorPlanElements/Bed.jsx"
      provides: "Rectángulo de cama con etiqueta C/S/M y <title> accesible"
    - path: "src/components/FloorPlanElements/Door.jsx"
      provides: "Interrupción de pared + arco de barrido de apertura"
  key_links:
    - from: "src/components/FloorPlan.jsx"
      to: "src/utils/floorplanLayout.js"
      via: "import { calcularLayout, M_A_U }"
      pattern: "import.*calcularLayout.*from.*floorplanLayout"
    - from: "src/components/FloorPlan.jsx"
      to: "config.valido / layout.zonas / layout.camas"
      via: "render condicional de estados + map de zonas y camas"
      pattern: "\\.zonas\\.map|\\.camas\\.map"
---

<objective>
Construir el componente `FloorPlan.jsx` y sus subcomponentes de equipamiento. El FloorPlan recibe
un prop `config`, llama a `calcularLayout` (Plan 01), y dibuja un `<svg>` cenital con: paredes
exteriores, las 5 zonas con color, la puerta (arco de barrido), ventanas, las camas con etiqueta
C/S/M, los módulos de baño y cocina, y las acotaciones en metros (capa de medición en cobre).
Maneja los 3 estados: Normal, Empty (config nula) y Error (geometría inválida).

Purpose: este es el corazón visual del producto. Implementa el CONTRATO de render del UI-SPEC
(sistema de coordenadas 100u/m, paleta 60/30/10, anchos de trazo, tipografía, accesibilidad) sobre
las coordenadas ya calculadas por el layout puro. Las transiciones se agregan en el Plan 03.
Output: FloorPlan.jsx + 6 subcomponentes SVG en src/components/FloorPlanElements/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-motor-de-plano-svg/02-UI-SPEC.md

<read_first>
- .planning/phases/02-motor-de-plano-svg/02-UI-SPEC.md — LOAD-BEARING. Implementar al pie:
  * "Color" → fills de zona (zona.bano #BFE3EE, zona.dormitorio #C9A66B, zona.cocina #A7C796,
    baulera/estar #ECECE4), tinta de croquis (pared #1A1A1A ancho 3, divisor fino 1, arco 1.25,
    cruz de ventana 0.75), cota en cobre #8B6914 ancho 0.75, cama #B5915A.
  * "Typography" → etiqueta de zona 12u semibold uppercase, cota 10u regular, etiqueta de cama 14u.
  * "Sistema de coordenadas SVG" → viewBox, preserveAspectRatio xMidYMid meet, orientación, puerta/ventanas.
  * "Copywriting Contract" → etiquetas BAULERA/BAÑO/DORMITORIO/ESTAR/COCINA, cotas "2,60 m" con coma,
    estados Empty ("Sin configuración" / "Elegí un modelo...") y Error.
  * "Jerarquía visual y accesibilidad" → <title> en cada ícono de equipamiento; role="img" + <title> raíz.
  * "Estados del componente" → Normal / Empty / Error.
- src/utils/floorplanLayout.js (Plan 01) — `calcularLayout(config)` devuelve { valido, zonas, camas, viewBox, totalU, anchoU, pad }; `M_A_U` = 100.
- src/data/mockConfig.js (Plan 01) — CONFIG_MOCK_N4 para probar el render.
- src/App.jsx — patrón de componente del proyecto (función default export, clases Tailwind impacar-*/zona-*).
- tailwind.config.js — tokens disponibles (impacar.cobre, impacar.texto, zona.bano/dormitorio/cocina).
</read_first>

<interfaces>
<!-- Contrato del layout que el render consume directamente (Plan 01). -->
```js
// src/utils/floorplanLayout.js
export const M_A_U = 100
export function calcularLayout(config): {
  valido: boolean,
  zonas: Array<{ id, etiqueta, x, anchoU, largoM, fill }>,  // unidades de viewBox
  camas: Array<{ x, y, w, h, tipo }>,                        // tipo: 'C' | 'S' | 'M'
  viewBox: string,        // "0 0 W H"
  totalU: number, anchoU: number, pad: number,               // pad = 12
}
```
Forma del prop `config`: ver src/data/mockConfig.js (modeloId, largo, bano, dormitorio, cocina, estar).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Subcomponentes SVG de equipamiento (FloorPlanElements/)</name>
  <files>src/components/FloorPlanElements/Door.jsx, src/components/FloorPlanElements/Window.jsx, src/components/FloorPlanElements/Bathroom.jsx, src/components/FloorPlanElements/Bed.jsx, src/components/FloorPlanElements/Kitchen.jsx, src/components/FloorPlanElements/Table.jsx</files>
  <action>
    Crear 6 subcomponentes presentacionales, cada uno recibe coordenadas/medidas ya calculadas (en
    unidades de viewBox) por props y devuelve primitivas SVG. TODOS los íconos de equipamiento llevan
    `<title>` dentro de su `<g>` para accesibilidad (UI-SPEC "Jerarquía visual y accesibilidad").

    Anchos de trazo y colores EXACTOS del UI-SPEC (sección "Color"):
    - `Door.jsx` (props x, y, length, side): interrupción de pared + `<path>` de cuarto de círculo
      (arco de barrido). Stroke `#1A1A1A` opacidad 0.7, ancho 1.25. `<title>Puerta de entrada</title>`.
    - `Window.jsx` (props x, y, length): hueco con dos líneas finas cruzadas (X) o doble línea paralela.
      Stroke `#1A1A1A` opacidad 0.6, ancho 0.75. `<title>Ventana</title>`.
    - `Bathroom.jsx` (props x, y, w, h): primitivas minimalistas de ducha/inodoro/lavatorio dentro del
      bloque del baño. Contorno `#1A1A1A` opacidad 0.7, ancho 1. `<title>Baño: ducha, inodoro, lavatorio</title>`.
    - `Bed.jsx` (props x, y, w, h, tipo): `<rect>` fill `#B5915A` (marrón saturado), contorno `#1A1A1A`
      opacidad 0.7 ancho 1, y un `<text text-anchor="middle">` centrado con la letra `tipo` (C/S/M) a 14u
      semibold. `<title>` que expande el tipo: C→"Cucheta marinera", S→"Cama simple", M→"Cama matrimonial".
    - `Kitchen.jsx` (props x, y, w, h, opciones): mesada + cocina + heladera como primitivas. `<title>Cocina</title>`.
    - `Table.jsx` (props x, y, w, h, rebatible): `<rect>` con borde; si `rebatible` usar
      `stroke-dasharray="3 2"` (punteado = rebatible), sino sólido. Stroke `#1A1A1A` opacidad 0.7 ancho 1.
      `<title>Mesa</title>`.

    Cada componente es un `export default function`. NO usar librerías de íconos (constraint: SVG nativo
    inline). Mantener el número de nodos acotado (croquis claro, no render arquitectónico).
  </action>
  <verify>
    <automated>cd "C:/Users/Usuario/OneDrive/Desktop/Proyectos/Impacar" && for f in Door Window Bathroom Bed Kitchen Table; do test -f "src/components/FloorPlanElements/$f.jsx" || { echo "FALTA $f.jsx"; exit 1; }; grep -q '<title>' "src/components/FloorPlanElements/$f.jsx" || { echo "FALTA <title> en $f.jsx"; exit 1; }; done && echo "OK 6 subcomponentes con <title> accesible"</automated>
  </verify>
  <done>Los 6 subcomponentes existen, cada uno con `<title>` accesible, con los anchos de trazo y colores del UI-SPEC. Commit: `feat(02-02): floorplan equipment subcomponents`.</done>
</task>

<task type="auto">
  <name>Task 2: FloorPlan.jsx — svg, paredes, zonas, estados</name>
  <files>src/components/FloorPlan.jsx</files>
  <action>
    Crear `FloorPlan.jsx` (`export default function FloorPlan({ config })`):
    - `const layout = calcularLayout(config)` (import de '../utils/floorplanLayout.js').
    - Estado Empty: si `!config` → render de un bloque centrado con heading "Sin configuración" y body
      "Elegí un modelo para ver el plano de tu casilla." (copy EXACTO del UI-SPEC). Clases Tailwind del
      proyecto (text-impacar-texto, etc.), centrado en el área del plano.
    - Estado Error: si `layout.valido === false` → bloque con "No se pudo dibujar el plano. Revisá el
      modelo seleccionado e intentá de nuevo." (copy EXACTO).
    - Estado Normal: render del `<svg>`:
      * `viewBox={layout.viewBox}`, `preserveAspectRatio="xMidYMid meet"`, `width="100%"`,
        `role="img"`, y un `<title>` que resume el plano (ej.: `Plano en planta de la casilla modelo
        ${config.modeloId}, ${formato} m`). Fondo `#F5F5F0` (impacar.fondo).
      * Aplicar un `transform="translate(pad, pad)"` (pad=12) al `<g>` de contenido para dejar el padding
        de cotas alrededor (UI-SPEC: 12u a cada lado).
      * Pared exterior: `<rect>` del perímetro (ancho `layout.totalU`, alto `layout.anchoU`) con stroke
        `#1A1A1A` ancho 3, fill `none` (la pared más gruesa, anclaje visual).
      * Zonas: `layout.zonas.map(z => <rect x={z.x} y=... width={z.anchoU} height={layout.anchoU}
        fill={z.fill} />)` con divisores finos entre zonas (stroke `#1A1A1A` opacidad 0.55, ancho 1) y la
        etiqueta de zona (`<text>` 12u semibold uppercase text-anchor=middle, color `#1A1A1A`): BAULERA,
        BAÑO, DORMITORIO, ESTAR, COCINA.
    - Importar y NO romper el build; usar formato de coma decimal para los metros en las etiquetas (helper
      inline o `String(n).replace('.', ',')`).
    Dejar HOOKS para Task 3 (puerta, ventanas, camas, cotas) — esta tarea entrega svg+paredes+zonas+estados.
  </action>
  <verify>
    <automated>cd "C:/Users/Usuario/OneDrive/Desktop/Proyectos/Impacar" && npm run build 2>&1 | grep -qi 'built in' && grep -Eq 'role="img"|role=\{?.img' src/components/FloorPlan.jsx && grep -q 'preserveAspectRatio="xMidYMid meet"' src/components/FloorPlan.jsx && grep -q 'Sin configuración' src/components/FloorPlan.jsx && grep -q 'No se pudo dibujar el plano' src/components/FloorPlan.jsx && echo "OK build + svg + estados"</automated>
  </verify>
  <done>FloorPlan.jsx compila (`npm run build` pasa), tiene role="img", preserveAspectRatio meet, dibuja paredes + 5 zonas con color, y maneja los estados Empty/Error con los copys exactos. Commit: `feat(02-02): FloorPlan svg, walls, zones, states`.</done>
</task>

<task type="auto">
  <name>Task 3: Camas, puerta, ventanas y acotaciones</name>
  <files>src/components/FloorPlan.jsx</files>
  <action>
    Completar el render Normal de FloorPlan.jsx montando los subcomponentes (Task 1) y la capa de cotas:
    - Camas: `layout.camas.map(c => <Bed x={c.x} y={c.y} w={c.w} h={c.h} tipo={c.tipo} />)` dentro de la
      zona dormitorio (2 filas contra paredes largas + pasillo central, ya posicionadas por el layout).
    - Puerta: `<Door>` sobre una pared larga, recomendación UI-SPEC: en la zona de estar/comedor (calcular
      x desde la zona 'estar' del layout).
    - Ventanas: `<Window>` — una en el dormitorio y una en cocina/estar (recomendación UI-SPEC), posicionadas
      sobre las paredes largas.
    - Módulos: `<Bathroom>` en la zona baño, `<Kitchen>` en la zona cocina, `<Table>` en estar si
      `config.estar?.mesa`.
    - Acotaciones (capa de medición, cobre `#8B6914` ancho 0.75) — UI-SPEC "Acotaciones":
      * (1) Largo total: línea de cota con ticks abajo del dibujo, número centrado "X,XX m" (10u regular).
      * (2) Ancho total 2,60 m: cota vertical a un lado.
      * (3) Largo de cada zona: cotas segmentadas a lo largo de X (al menos baulera/dormitorio/cocina).
      Las cotas viven en el padding de 12u reservado. Números en metros con COMA decimal y sufijo " m".
    - Mantener jerarquía de capas (UI-SPEC): paredes → zonas → módulos/etiquetas → cotas en cobre encima.
  </action>
  <verify>
    <automated>cd "C:/Users/Usuario/OneDrive/Desktop/Proyectos/Impacar" && npm run build 2>&1 | grep -qi 'built in' && grep -q '\.camas\.map' src/components/FloorPlan.jsx && grep -Eq '<Door|<Window|<Bathroom|<Kitchen' src/components/FloorPlan.jsx && grep -Eq '#8B6914|impacar-cobre|impacar\.cobre' src/components/FloorPlan.jsx && echo "OK build + camas + modulos + cotas en cobre"</automated>
  </verify>
  <done>El render Normal dibuja camas (C/S/M), puerta con arco, ventanas, módulos de baño/cocina y las 3 acotaciones en cobre; `npm run build` pasa. Cubre PLANO-01 (paredes/puerta/ventanas/cotas + escala) y PLANO-04 (zonas + geometría). Commit: `feat(02-02): beds, door, windows, dimension lines`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config prop → FloorPlan | Único input; en Fase 2 es config mock. Puede venir nula o con geometría inválida. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Denial of Service | FloorPlan con config nula/incompleta crashea el render | mitigate | Render condicional: `!config` → estado Empty; `layout.valido === false` → estado Error. Nunca se intenta mapear zonas/camas indefinidas. Verificado por grep de los copys de estado. |
| T-02-05 | Tampering | text de etiqueta inyectado vía config (si en fases futuras viene de input) | accept | En Fase 2 la config es mock local; React escapa el contenido de `<text>` por defecto. Sin renderizado de HTML crudo. Revisar al integrar wizard (Fase 4). |
| T-02-06 | Information Disclosure | modelo/medidas visibles en el SVG | accept | El plano es para mostrarse al cliente; sin PII. Sin cambios. |
</threat_model>

<verification>
- `npm run build` pasa limpio (todos los imports resuelven, JSX compila).
- FloorPlan con CONFIG_MOCK_N4 dibuja paredes, 5 zonas con color, camas C/S/M, puerta, ventanas, cotas en cobre.
- `<svg>` tiene role="img" + `<title>`, viewBox y preserveAspectRatio="xMidYMid meet" (escala al contenedor).
- config nula → estado Empty; layout inválido → estado Error (copys exactos del UI-SPEC).
- Cada subcomponente de equipamiento tiene `<title>` accesible.
</verification>

<success_criteria>
- Dado un modelo, el plano dibuja paredes, puerta (arco), ventanas y acotaciones en metros, escalando al contenedor (PLANO-01).
- Muestra la estructura fija de zonas con sus colores (baño celeste, dormitorio marrón, cocina verde) (PLANO-04).
- Respeta la geometría real al ubicar camas (consumida del layout del Plan 01) (PLANO-04).
- Maneja los 3 estados del UI-SPEC (Normal / Empty / Error).
</success_criteria>

<output>
After completion, create `.planning/phases/02-motor-de-plano-svg/02-02-SUMMARY.md`
</output>
