---
phase: 02-motor-de-plano-svg
plan: 03
type: execute
wave: 3
depends_on: [2]
files_modified:
  - src/styles/index.css
  - src/components/FloorPlan.jsx
  - src/App.jsx
autonomous: false
requirements: [PLANO-02]
user_setup: []

must_haves:
  truths:
    - "Al cambiar el config (largo, zonas, módulos), los elementos del plano transicionan ~300ms ease-in-out sin saltos"
    - "Las camas/módulos que aparecen o desaparecen hacen fade (opacity), no parpadean"
    - "Con prefers-reduced-motion: reduce las transiciones se desactivan (cambio instantáneo)"
    - "La demo en App.jsx renderiza el FloorPlan con una config mock y un control 'Cambiar modelo' que la cambia"
  artifacts:
    - path: "src/styles/index.css"
      provides: "Clases de transición SVG (300ms ease-in-out sobre x/y/width/height/opacity) + bloque prefers-reduced-motion"
      contains: "prefers-reduced-motion"
    - path: "src/App.jsx"
      provides: "Demo: estado de config con useState, render del FloorPlan, botón 'Cambiar modelo' que cicla configs mock"
  key_links:
    - from: "src/App.jsx"
      to: "src/components/FloorPlan.jsx"
      via: "import FloorPlan + <FloorPlan config={config} />"
      pattern: "<FloorPlan\\s+config="
    - from: "src/App.jsx"
      to: "src/data/mockConfig.js"
      via: "import { CONFIGS_MOCK } + useState para ciclar"
      pattern: "CONFIGS_MOCK"
    - from: "src/styles/index.css"
      to: "elementos SVG del FloorPlan"
      via: "clase .fp-anim con transition + media prefers-reduced-motion"
      pattern: "prefers-reduced-motion"
---

<objective>
Cerrar el motor del plano: agregar las transiciones suaves (~300ms ease-in-out) sobre posición,
tamaño y opacidad de los elementos del SVG cuando cambia el config (PLANO-02), con respeto a
`prefers-reduced-motion`, y wirear en App.jsx una demo con una config mock y un control "Cambiar
modelo" que cicle entre modelos para demostrar la transición en vivo.

Purpose: PLANO-02 ("el plano se actualiza con transición suave ~300ms al cambiar una opción") es el
criterio de éxito #4 de la fase y la prueba visible de que el motor funciona. El wizard real llega
en fases posteriores; acá la demo lo simula cambiando entre configs mock. Incluye un checkpoint
human-verify porque la suavidad de la animación es una cualidad visual no verificable por CLI.
Output: clases de transición en index.css, hooks de clase en FloorPlan.jsx, demo en App.jsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-motor-de-plano-svg/02-UI-SPEC.md

<read_first>
- .planning/phases/02-motor-de-plano-svg/02-UI-SPEC.md — sección "Animación / Transición (PLANO-02)":
  ~300ms ease-in-out (cubic-bezier(0.4,0,0.2,1)); animar x/y/width/height/opacity de zonas/camas/módulos
  y reposición de cotas; NO animar el viewBox ni el ancho de trazo de paredes; respetar
  prefers-reduced-motion: reduce → cambio instantáneo. Sección "Copywriting Contract": CTA de la demo
  "Cambiar modelo" (verbo + sustantivo).
- src/components/FloorPlan.jsx (Plan 02) — componente a anotar con la clase de transición.
- src/data/mockConfig.js (Plan 01) — CONFIGS_MOCK (array con N1, N4...) para ciclar en la demo.
- src/styles/index.css — archivo de estilos actual (@tailwind + body); acá van las clases de transición.
- src/App.jsx — shell actual; se reemplaza el smoke render por la demo del FloorPlan.
</read_first>

<interfaces>
<!-- Lo que la demo y las transiciones consumen. -->
```js
// src/components/FloorPlan.jsx (Plan 02)
export default function FloorPlan({ config }) // dibuja el plano; config nula → Empty; inválido → Error
// src/data/mockConfig.js (Plan 01)
export const CONFIGS_MOCK // Array de configs (al menos N1 y N4) para ciclar en la demo
```
Easing del UI-SPEC: `cubic-bezier(0.4, 0, 0.2, 1)`, duración `300ms`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clases de transición SVG + prefers-reduced-motion</name>
  <files>src/styles/index.css, src/components/FloorPlan.jsx</files>
  <action>
    En `src/styles/index.css` (después del bloque body), agregar:
    - `.fp-anim { transition: x 300ms cubic-bezier(0.4,0,0.2,1), y 300ms cubic-bezier(0.4,0,0.2,1),
      width 300ms cubic-bezier(0.4,0,0.2,1), height 300ms cubic-bezier(0.4,0,0.2,1),
      opacity 300ms cubic-bezier(0.4,0,0.2,1), transform 300ms cubic-bezier(0.4,0,0.2,1); }`
      (transición sobre los atributos de presentación SVG x/y/width/height + opacity/transform).
    - Bloque `@media (prefers-reduced-motion: reduce) { .fp-anim { transition: none; } }` para desactivar
      las transiciones (cambio instantáneo) cuando el usuario lo pide (UI-SPEC accesibilidad).

    En `src/components/FloorPlan.jsx`: agregar `className="fp-anim"` a los `<rect>` de zonas, a los `<g>`/
    rects de camas y módulos, y a los `<g>` de cotas — es decir, a TODO lo que reposiciona/redimensiona al
    cambiar el config. NO agregar la clase a la `<rect>` de pared exterior cuyo ancho de trazo no debe
    animar (UI-SPEC: "El trazo de las paredes no cambia de ancho con la animación"); la pared puede animar
    su width/height pero su stroke-width queda fijo (no se toca por la transición). El viewBox del `<svg>`
    NO lleva la clase (no se anima por frame).

    Para que las camas/módulos que aparecen o desaparecen hagan fade (no parpadeo), asegurar que el render
    mantenga keys estables y aplique opacity (la clase fp-anim ya transiciona opacity).
  </action>
  <verify>
    <automated>cd "C:/Users/Usuario/OneDrive/Desktop/Proyectos/Impacar" && grep -q 'prefers-reduced-motion' src/styles/index.css && grep -q '300ms' src/styles/index.css && grep -q 'cubic-bezier(0.4,0,0.2,1)' src/styles/index.css && grep -q 'fp-anim' src/components/FloorPlan.jsx && npm run build 2>&1 | grep -qi 'built in' && echo "OK transiciones + reduced-motion + build"</automated>
  </verify>
  <done>index.css tiene `.fp-anim` con transición 300ms ease-in-out sobre x/y/width/height/opacity y el bloque `prefers-reduced-motion: reduce` que la desactiva; FloorPlan.jsx aplica la clase a zonas/camas/módulos/cotas; `npm run build` pasa. Commit: `feat(02-03): 300ms transitions + prefers-reduced-motion`.</done>
</task>

<task type="auto">
  <name>Task 2: Demo en App.jsx (config mock + "Cambiar modelo")</name>
  <files>src/App.jsx</files>
  <action>
    Reemplazar el smoke render actual de App.jsx por una demo del FloorPlan:
    - `import FloorPlan from './components/FloorPlan.jsx'` y `import { CONFIGS_MOCK } from './data/mockConfig.js'`.
    - `const [idx, setIdx] = useState(0)` y `const config = CONFIGS_MOCK[idx]`.
    - Mantener el header existente (IMPACAR / "Configurador de casillas rurales", clases impacar-campo).
    - En `<main>`: un contenedor del plano (wrapper con padding md=16px del UI-SPEC, bg impacar-fondo) que
      renderiza `<FloorPlan config={config} />` escalando al ancho del contenedor (max-width razonable para
      desktop; en mobile ocupa el ancho — el SVG ya escala con width 100%).
    - Un botón "Cambiar modelo" (copy EXACTO del UI-SPEC, verbo+sustantivo) que hace
      `setIdx((idx + 1) % CONFIGS_MOCK.length)` — cada click cambia el config y dispara la transición de 300ms.
      Touch target ≥44px (UI-SPEC). Mostrar al lado el modeloId actual (ej.: "Modelo N4").
    - Debajo del plano, una leyenda HTML simple (UI-SPEC "Leyenda"): swatches de color → zona (Baño /
      Dormitorio / Cocina / Estar/comedor / Baulera) y la expansión "C cucheta · S simple · M matrimonial",
      usando la escala de spacing de 8pt (gap sm=8px, separación lg=24px del plano).
    - Todo en español argentino, trato de usted, identidad sobria.
  </action>
  <verify>
    <automated>cd "C:/Users/Usuario/OneDrive/Desktop/Proyectos/Impacar" && grep -Eq '<FloorPlan\s+config=' src/App.jsx && grep -q 'CONFIGS_MOCK' src/App.jsx && grep -q 'Cambiar modelo' src/App.jsx && grep -q 'useState' src/App.jsx && npm run build 2>&1 | grep -qi 'built in' && echo "OK demo + build"</automated>
  </verify>
  <done>App.jsx renderiza `<FloorPlan config={config} />` con una config mock, un botón "Cambiar modelo" que cicla CONFIGS_MOCK, y una leyenda; `npm run build` pasa. Cubre PLANO-02 a nivel de demo (el cambio dispara la transición). Commit: `feat(02-03): floorplan demo with model switcher`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificación visual de la transición</name>
  <what-built>
    El motor del plano completo con transiciones de 300ms: App.jsx muestra el FloorPlan con una config
    mock y un botón "Cambiar modelo" que cicla entre modelos. Al cambiar, las zonas, camas, módulos y cotas
    transicionan suavemente (~300ms ease-in-out) sin saltos. Con prefers-reduced-motion el cambio es
    instantáneo. El plano escala al contenedor y es legible en mobile (~340px).
  </what-built>
  <how-to-verify>
    1. Corré `npm run dev` y abrí la URL que imprime Vite (típicamente http://localhost:5173).
    2. Verificá que se dibuja la casilla N1 (o el primer modelo): paredes, las 5 zonas con color (baño
       celeste, dormitorio marrón, cocina verde, baulera/estar neutro), camas con C/S/M, puerta con arco,
       ventanas, y las acotaciones en cobre con números en metros (coma decimal, ej. "2,60 m").
    3. Hacé click en "Cambiar modelo" varias veces. Confirmá que el plano TRANSICIONA suavemente (las zonas
       y camas se reacomodan en ~300ms, las cotas reposicionan, los módulos que aparecen/desaparecen hacen
       fade) — sin saltos bruscos ni parpadeo.
    4. Achicá la ventana del navegador a ~340px de ancho (o usá DevTools modo responsive, Samsung). Confirmá
       que el SVG escala, no se desborda y los números de cota siguen legibles.
    5. (Opcional, accesibilidad) Activá "Reducir movimiento" en el SO o vía DevTools → Rendering →
       "Emulate prefers-reduced-motion: reduce" y confirmá que el cambio de modelo es instantáneo (sin animación).
    6. Confirmá el estado Empty: temporalmente la demo puede tener un modelo que muestre el plano; si querés,
       pasá `config={null}` para ver "Sin configuración" (opcional).
  </how-to-verify>
  <resume-signal>Escribí "aprobado" si la transición es suave y el plano se ve correcto, o describí qué falla (saltos, colores, desbordes, ilegibilidad en mobile).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| interacción de usuario (botón) → estado de config | El click cambia el índice de CONFIGS_MOCK; data local, no input libre. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Denial of Service | transiciones costosas degradan el render en mobile gama media | mitigate | Animar solo x/y/width/height/opacity/transform (propiedades baratas), número de nodos SVG acotado (croquis, no render arquitectónico), sin librería de animación. Verificación de fluidez en el checkpoint human-verify (paso 4, ~340px). |
| T-02-08 | Tampering | usuario fuerza idx fuera de rango | mitigate | `(idx + 1) % CONFIGS_MOCK.length` mantiene el índice acotado; nunca accede a un config undefined. |
| T-02-09 | Information Disclosure | n/a presentacional | accept | Sin PII; el plano es para mostrar al cliente. Sin cambios. |
</threat_model>

<verification>
- `npm run build` pasa limpio.
- index.css tiene transición 300ms ease-in-out (cubic-bezier(0.4,0,0.2,1)) y bloque prefers-reduced-motion.
- App.jsx renderiza `<FloorPlan config={...} />` y el botón "Cambiar modelo" cicla CONFIGS_MOCK.
- Checkpoint human-verify: la transición es visualmente suave, el plano escala a ~340px, reduced-motion desactiva la animación.
</verification>

<success_criteria>
- Al cambiar las props (largo, zonas, módulos), el plano transiciona suavemente (~300ms) sin saltos bruscos (PLANO-02, Success Criteria #4).
- Las transiciones respetan prefers-reduced-motion (UI-SPEC accesibilidad).
- La demo demuestra el motor end-to-end: config mock → FloorPlan → cambio de modelo → transición.
- El plano escala al viewport y es legible en mobile (~340px).
</success_criteria>

<output>
After completion, create `.planning/phases/02-motor-de-plano-svg/02-03-SUMMARY.md`
</output>
