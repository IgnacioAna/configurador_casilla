---
phase: 02-motor-de-plano-svg
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/utils/floorplanLayout.js
  - src/utils/floorplanLayout.test.js
  - src/data/mockConfig.js
autonomous: true
requirements: [PLANO-04]
user_setup: []

must_haves:
  truths:
    - "Dada una config de modelo, el layout devuelve 5 zonas en el orden fijo baulera|baño|dormitorio|estar|cocina a lo largo de X"
    - "Baulera y cocina miden 0.60m; baño/dormitorio/estar reparten el largo restante"
    - "El dormitorio ubica camas de 0.80m contra cada pared larga con pasillo central de 0.92m (suma 2.52m interior)"
    - "Una config con geometría imposible (largo insuficiente) se marca inválida en vez de devolver zonas negativas"
  artifacts:
    - path: "src/utils/floorplanLayout.js"
      provides: "Helper puro de layout: zonas a lo largo de X y posiciones de camas, derivadas de GEOMETRIA"
      exports: ["calcularLayout", "M_A_U"]
      min_lines: 60
    - path: "src/data/mockConfig.js"
      provides: "Configs mock (forma del prop config) para N1 y N4 que el FloorPlan y la demo consumen"
      exports: ["CONFIG_MOCK_N4", "CONFIGS_MOCK"]
    - path: "src/utils/floorplanLayout.test.js"
      provides: "Tests node:test que codifican el contrato del layout (RED→GREEN)"
  key_links:
    - from: "src/utils/floorplanLayout.js"
      to: "src/data/geometry.js"
      via: "import { GEOMETRIA }"
      pattern: "import.*GEOMETRIA.*from.*geometry"
    - from: "src/utils/floorplanLayout.js"
      to: "GEOMETRIA.pasilloCentral / anchoCama / zonaBaulera / zonaCocina"
      via: "lectura de constantes (sin hardcodear 0.92 / 0.80 / 0.60)"
      pattern: "GEOMETRIA\\.(pasilloCentral|anchoCama|zonaBaulera|zonaCocina|anchoInterior)"
---

<objective>
Definir el contrato del prop `config` que consumirá el FloorPlan y construir el helper puro de
layout que, dado ese config + las constantes de `src/data/geometry.js`, calcula:
1. Las 5 zonas a lo largo de X en el orden fijo (baulera 0.60 | baño | dormitorio | estar | cocina 0.60).
2. Las posiciones de las camas del dormitorio (2 contra cada pared larga + pasillo central 0.92).
3. La conversión metros→unidades de viewBox (factor único 100u/m).

Es la base de la fase: el FloorPlan (Plan 02) y la demo (Plan 03) consumen estas salidas. Se construye
primero, como funciones puras y testeables, para que el render no tenga que inventar geometría.

Purpose: aislar TODA la matemática de medidas en un módulo puro y probado, leyendo la geometría real
de geometry.js (nunca hardcodeada). Así el render queda declarativo y la transición (Plan 03) anima
sobre coordenadas ya calculadas.
Output: `src/utils/floorplanLayout.js` (con `calcularLayout` y la constante de escala), `src/data/mockConfig.js`
(forma del prop config + configs mock N1/N4) y su test node:test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-motor-de-plano-svg/02-UI-SPEC.md

<read_first>
- .planning/phases/02-motor-de-plano-svg/02-UI-SPEC.md — sección "Sistema de coordenadas SVG":
  factor 100u/m, orientación (largo en X, ancho en Y), estructura fija de zonas, geometría del dormitorio.
- src/data/geometry.js — constantes REALES a leer (NO hardcodear): anchoExterior 2.60, anchoInterior 2.52,
  anchoCama 0.80, largoCama 2.00, pasilloCentral 0.92, zonaBaulera 0.60, zonaCocina 0.60.
- src/data/models.js — los 7 modelos (id, largo). El config mock referencia un modeloId real.
- src/utils/formato.test.js — patrón de test del proyecto (node:test, ESM-native, `node --test`).
</read_first>

<interfaces>
<!-- Constantes que el executor consume directamente. NO explorar el codebase. -->

De src/data/geometry.js:
```js
export const GEOMETRIA = {
  anchoExterior: 2.6, anchoInterior: 2.52, anchoCama: 0.8, largoCama: 2.0,
  pasilloCentral: 0.92, zonaBaulera: 0.6, zonaCocina: 0.6,
}
```

De src/data/models.js:
```js
export const MODELOS = [ { id: 'N1', largo: 4.5, ... }, { id: 'N4', largo: 6.6, ... }, ... ]
```

Forma del prop `config` a DEFINIR y documentar en mockConfig.js (las fases del wizard la producirán):
```js
{
  modeloId: 'N4',          // referencia a MODELOS[].id; el largo total se deriva del modelo
  largo: 6.6,              // largo total en metros (redundante con el modelo, pero explícito para el plano)
  bano:       { tamano: 'estandar' },          // 'estandar' | 'ampliado'
  dormitorio: { camas: [{ tipo: 'C' }, { tipo: 'S' }] },  // tipo: 'C' cucheta | 'S' simple | 'M' matrimonial
  cocina:     { horno: false, heladera: null },
  estar:      { mesa: false },
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test RED del contrato de layout</name>
  <files>src/utils/floorplanLayout.test.js</files>
  <behavior>
    - `calcularLayout(CONFIG_MOCK_N4)` devuelve `{ valido: true, zonas, camas, viewBox, totalU, anchoU }`.
    - Hay exactamente 5 zonas en `zonas` y su orden por `x` ascendente es: baulera, bano, dormitorio, estar, cocina.
    - La zona baulera tiene `largoM === GEOMETRIA.zonaBaulera` (0.60) y la cocina `=== GEOMETRIA.zonaCocina` (0.60).
    - La suma de `largoM` de las 5 zonas === `config.largo` (6.60 para N4), con tolerancia 1e-9.
    - Las zonas no se solapan: para cada par consecutivo, `zona[i].x + zona[i].anchoU === zona[i+1].x` (en unidades).
    - El factor de escala convierte metros→unidades: `M_A_U === 100` y `zonaBaulera` mide `0.60 * 100 = 60` unidades de ancho.
    - `camas` ubica las camas del dormitorio: hay 2 filas (superior e inferior); la fila superior arranca en `y` del borde interior superior y la inferior termina en el borde interior inferior; el hueco entre ambas filas === `GEOMETRIA.pasilloCentral * 100` (92u). Cada cama tiene alto `GEOMETRIA.anchoCama * 100` (80u).
    - Una config con `largo` menor a (baulera + cocina + mínimos) devuelve `{ valido: false }` sin zonas de ancho negativo.
  </behavior>
  <action>
    Crear `src/utils/floorplanLayout.test.js` siguiendo EXACTAMENTE el patrón de src/utils/formato.test.js
    (`import { test } from 'node:test'`, `import assert from 'node:assert/strict'`, ESM, corrible con
    `node --test`). Importar `calcularLayout` y `M_A_U` de `./floorplanLayout.js` y `CONFIG_MOCK_N4` de
    `../data/mockConfig.js`.

    Escribir los asserts que codifican cada bullet de <behavior>. Para verificar que las medidas se LEEN
    de geometry.js y no se hardcodean, importar `GEOMETRIA` en el test y comparar contra
    `GEOMETRIA.zonaBaulera * M_A_U`, `GEOMETRIA.pasilloCentral * M_A_U`, `GEOMETRIA.anchoCama * M_A_U` (no
    contra literales 60/92/80).

    Para el caso inválido usar un config con `largo: 1.0` (imposible: baulera 0.60 + cocina 0.60 ya deja
    solo 0.40 para baño+dormitorio+estar). NO crear floorplanLayout.js todavía — este test debe FALLAR
    (RED) porque el módulo no existe.
  </action>
  <verify>
    <automated>cd "C:/Users/Usuario/OneDrive/Desktop/Proyectos/Impacar" && node --test src/utils/floorplanLayout.test.js; test $? -ne 0 && echo "RED OK (falla como se espera)"</automated>
  </verify>
  <done>El test existe, importa calcularLayout/M_A_U/CONFIG_MOCK_N4, y falla en RED porque floorplanLayout.js (y mockConfig.js) aún no existen. Commit: `test(02-01): add failing test for floorplan layout`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: mockConfig.js + floorplanLayout.js (GREEN)</name>
  <files>src/data/mockConfig.js, src/utils/floorplanLayout.js</files>
  <action>
    PRIMERO crear `src/data/mockConfig.js`:
    - Documentar con un comentario de cabecera la FORMA del prop `config` (ver <interfaces>): modeloId,
      largo, bano:{tamano}, dormitorio:{camas:[{tipo}]}, cocina:{horno,heladera}, estar:{mesa}.
    - Exportar `CONFIG_MOCK_N4` (modeloId 'N4', largo 6.6, bano estandar, dormitorio con 2 cuchetas + 2 simples
      o lo que refleje las 4 camas base de N4, cocina con horno false, estar mesa false).
    - Exportar `CONFIGS_MOCK` (array con al menos N1 y N4) para que la demo del Plan 03 cicle entre modelos.
    - Importar `MODELOS` de './models.js' para derivar `largo` del modeloId si hace falta, manteniendo
      coherencia con el modelo real (NO inventar largos).

    LUEGO crear `src/utils/floorplanLayout.js`:
    - `import { GEOMETRIA } from '../data/geometry.js'`.
    - `export const M_A_U = 100` (100 unidades de viewBox = 1 metro; comentario que cita el UI-SPEC).
    - `export function calcularLayout(config)`:
      * Validar config: si es nula/incompleta o si `largo < GEOMETRIA.zonaBaulera + GEOMETRIA.zonaCocina + 0.4`
        (mínimo plausible para los 3 ambientes centrales) → devolver `{ valido: false }`.
      * Calcular `totalU = config.largo * M_A_U`, `anchoU = GEOMETRIA.anchoExterior * M_A_U` (260u alto).
      * Repartir X: baulera = `GEOMETRIA.zonaBaulera`, cocina = `GEOMETRIA.zonaCocina` (fijas); el largo
        restante `restante = config.largo - zonaBaulera - zonaCocina` se reparte entre baño/dormitorio/estar.
        Reparto recomendado: baño ~22%, dormitorio ~45%, estar ~33% del restante (ajustable; documentar los
        ratios como constantes nombradas). El baño SIEMPRE va entre baulera y dormitorio (orden inmutable).
      * Construir `zonas` = array de 5 objetos `{ id, etiqueta, x, anchoU, largoM, fill }` en unidades de viewBox,
        con x acumulado e incluyendo el padding lateral de cota (ver nota viewBox abajo). Mapear fill a los
        tokens del UI-SPEC: bano '#BFE3EE', dormitorio '#C9A66B', cocina '#A7C796', baulera/estar '#ECECE4'.
      * Calcular `camas`: dentro de la zona dormitorio, ubicar las camas de `config.dormitorio.camas` en 2
        filas. Borde interior superior Y = `((GEOMETRIA.anchoExterior - GEOMETRIA.anchoInterior)/2) * M_A_U`
        (espesor de pared 0.04m → 4u). Fila superior: alto `GEOMETRIA.anchoCama * M_A_U`; fila inferior pegada
        al borde interior inferior; entre ambas el `GEOMETRIA.pasilloCentral * M_A_U` (92u). Largo de cada
        cama sobre X = `GEOMETRIA.largoCama * M_A_U` (200u), corriendo desde el inicio de la zona dormitorio.
        Repartir las camas de la config alternando fila superior/inferior. Cada cama: `{ x, y, w, h, tipo }`.
      * Calcular `viewBox` con padding de 12u a cada lado para las cotas (UI-SPEC): `"0 0 ${totalU + 24} ${anchoU + 24}"`.
      * Devolver `{ valido: true, zonas, camas, viewBox, totalU, anchoU, pad: 12 }`.
    - NO usar literales 0.60/0.92/0.80/2.52/2.60 — leer SIEMPRE de GEOMETRIA. Es verificable por grep.
  </action>
  <verify>
    <automated>cd "C:/Users/Usuario/OneDrive/Desktop/Proyectos/Impacar" && node --test src/utils/floorplanLayout.test.js</automated>
  </verify>
  <done>Los tests pasan en verde (suite completa OK). `calcularLayout` lee toda la geometría de GEOMETRIA. Commit: `feat(02-01): floorplan layout helper + mock config`.</done>
</task>

<task type="auto">
  <name>Task 3: Gate anti-hardcodeo + suma de control de geometría</name>
  <files>src/utils/floorplanLayout.test.js</files>
  <action>
    Agregar al test existente dos checks defensivos que blindan el contrato:
    1. Suma de control geométrica: assert que `GEOMETRIA.anchoCama * 2 + GEOMETRIA.pasilloCentral` ===
       `GEOMETRIA.anchoInterior` (0.80×2 + 0.92 = 2.52), garantizando que el layout de camas cierra contra
       el interior útil real.
    2. Test de transición de config (prepara PLANO-02 del Plan 03): `calcularLayout` para N1 (largo 4.5) y
       N4 (largo 6.6) deben devolver `totalU` distintos (450 vs 660) y AMBOS válidos, probando que el layout
       reacciona al cambio de modelo de forma determinística (mismas zonas, distinto reparto).

    El gate anti-hardcodeo se verifica por grep en el comando de verify (abajo): floorplanLayout.js NO debe
    contener los literales de medida 0.92 / 0.80 / 2.52 / 0.6 fuera de comentarios.
  </action>
  <verify>
    <automated>cd "C:/Users/Usuario/OneDrive/Desktop/Proyectos/Impacar" && node --test src/utils/floorplanLayout.test.js && grep -v '^\s*//' src/utils/floorplanLayout.js | grep -Eq '(0\.92|2\.52|0\.80|[^0-9]0\.6[^0-9])' && echo "FALLO: medida hardcodeada" && exit 1 || echo "OK: sin medidas hardcodeadas"</automated>
  </verify>
  <done>El test pasa con la suma de control y el test de transición N1/N4; el grep confirma que no hay medidas hardcodeadas (todo se lee de GEOMETRIA). Commit: `test(02-01): add geometry checksum + transition guard`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config mock → calcularLayout | Único input; en Fase 2 es data mock local, no input de usuario. Aún así puede traer largo inválido. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | calcularLayout(config) con largo imposible | mitigate | Validación de rango: largo < baulera+cocina+mínimo → `{ valido: false }`, nunca zonas de ancho negativo. Cubierto por test del caso inválido. |
| T-02-02 | Tampering | drift entre geometría real y layout | mitigate | Suma de control `anchoCama*2 + pasilloCentral === anchoInterior` en el test; todas las medidas leídas de GEOMETRIA (gate grep), una sola fuente de verdad. |
| T-02-03 | Information Disclosure | datos del config (modelo, precio) en cliente | accept | El plano es presentacional y el catálogo es público; sin PII. Sin cambios (heredado de T-01-07). |
</threat_model>

<verification>
- `node --test src/utils/floorplanLayout.test.js` pasa toda la suite.
- El grep anti-hardcodeo confirma que floorplanLayout.js no contiene literales de medida.
- `calcularLayout(CONFIG_MOCK_N4).zonas` tiene 5 zonas en orden fijo; `.camas` ubica camas con pasillo 0.92m.
- Config inválida devuelve `{ valido: false }`.
</verification>

<success_criteria>
- El helper de layout (puro, testeado) traduce un `config` a zonas + camas + viewBox en unidades de viewBox.
- Toda la geometría se lee de src/data/geometry.js (verificable por grep — sin medidas hardcodeadas).
- La forma del prop `config` está documentada en mockConfig.js y hay configs mock para al menos N1 y N4.
- Cubre PLANO-04 (estructura fija de zonas + geometría real) a nivel de cálculo; el render lo consume en Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/02-motor-de-plano-svg/02-01-SUMMARY.md`
</output>
