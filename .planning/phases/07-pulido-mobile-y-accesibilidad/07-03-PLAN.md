---
phase: 07-pulido-mobile-y-accesibilidad
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified: []
autonomous: false
requirements: [UX-01, UX-02]

must_haves:
  truths:
    - "El UAT manual completo (375px / labels / contraste / teclado / reduced-motion) pasa en las 8 pantallas"
    - "Se puede completar el flujo de punta a punta (landing -> wizard 6 pasos -> resumen -> WhatsApp/PDF) solo con teclado"
    - "El usuario firma que se ve y se opera bien a 375px + teclado (cierre subjetivo/visual de la fase)"
  artifacts: []
  key_links:
    - from: "checkpoint human-verify"
      to: "UAT checklist de 07-VALIDATION.md"
      via: "recorrido manual en npm run dev + DevTools"
      pattern: "manual"
---

<objective>
Cierre de la fase (y del milestone v1.0): un checkpoint de verificacion HUMANA que ejecuta el UAT manual
completo de 07-VALIDATION.md sobre la app ya pulida por los Planes 01 y 02. La verificacion de esta fase es
100% manual por D-02 (sin tests automatizados de a11y, sin deps nuevas); este plan es el gate que firma que
UX-01 y UX-02 se cumplen de verdad en el target real.

No modifica codigo. Antes del checkpoint, una tarea auto corre el smoke automatico (npm test + npm run build)
como guarda de regresion; luego el checkpoint humano recorre las 8 pantallas a 375px, por teclado, con el
contrast checker y reduced-motion emulado.

Purpose: Firmar que la interfaz es comoda y usable a ~375px y operable por teclado de punta a punta (el
criterio es subjetivo/visual y requiere ojo humano).
Output: UAT firmado; requirements UX-01/UX-02 listos para marcar Complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-pulido-mobile-y-accesibilidad/07-CONTEXT.md
@.planning/phases/07-pulido-mobile-y-accesibilidad/07-RESEARCH.md
@.planning/phases/07-pulido-mobile-y-accesibilidad/07-VALIDATION.md
@.planning/phases/07-pulido-mobile-y-accesibilidad/07-01-PLAN.md
@.planning/phases/07-pulido-mobile-y-accesibilidad/07-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smoke automatico de regresion (npm test + build) antes del UAT</name>
  <files></files>
  <read_first>
    - 07-VALIDATION.md "Manual-Only Verifications" ultima fila (app monta sin errores de consola tras los edits).
    - package.json (script test: 12 archivos node:test; build: vite build).
  </read_first>
  <action>
    Correr la guarda de regresion antes de pasar al checkpoint humano:
    - `npm test` (debe pasar el set completo, 12 archivos de logica pura — confirma que los edits de
      markup/CSS/foco de los Planes 01-02 no rompieron ningun import).
    - `npm run build` (vite build — debe terminar sin error).
    - `npm run dev` y abrir la consola del navegador recorriendo landing -> wizard -> resumen: smoke visual
      de que la app monta SIN errores ni warnings nuevos en consola.
    Si algo falla, NO avanzar al checkpoint: registrar el fallo y volver al plan correspondiente.
  </action>
  <verify>
    <automated>npm test && npm run build</automated>
    Manual: `npm run dev`, consola del navegador limpia recorriendo el flujo completo.
  </verify>
  <acceptance_criteria>
    - `npm test` sale 0 (12 archivos de test verdes).
    - `npm run build` termina sin error.
    - Manual: la app monta y se recorre landing->wizard->resumen sin errores/warnings nuevos en consola.
  </acceptance_criteria>
  <done>npm test y npm run build verdes; la app monta sin errores de consola — listo para el UAT humano.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT manual completo a 375px + teclado + contraste + reduced-motion (D-02)</name>
  <action>Checkpoint de verificacion humana (no edita codigo). Ejecutar el UAT manual completo de 07-VALIDATION.md sobre la app ya pulida por los Planes 01-02, siguiendo los pasos de <how-to-verify>. La verificacion de esta fase es 100% manual por D-02; este es el gate que firma UX-01/UX-02.</action>
  <what-built>
    Los Planes 01-02 aplicaron: reduced-motion global (D-04), foco al cambiar de vista/paso (D-03),
    contraste /60->/70 en los 2 links muted (D-01), role=group en el selector de heladera (D-01),
    anti-overflow a 375px en las filas de precio/valor (UX-01) y labels de grupo en los pasos (UX-02).
    Esta es la verificacion REAL de la fase (manual por D-02).
  </what-built>
  <how-to-verify>
    Ejecutar `npm run dev` y abrir DevTools. Recorrer el UAT checklist de 07-VALIDATION.md:

    A) SIN DESBORDES A 375px (UX-01) — DevTools responsive (Ctrl+Shift+M), ancho 375. Para Landing,
       Paso 1, Paso 2, Paso 3, Paso 4, Paso 5, Paso 6 y Resumen:
       1. Sin scroll horizontal en el viewport.
       2. Ningun texto/precio/control cortado o saliendose de su card.
       3. Barra de precio sticky (Pasos 4-6) legible y dentro del ancho.
       4. Plano colapsable: al abrirlo a 375px, el SVG y las cotas caben; la leyenda hace wrap.

    B) LABELS Y CONTRASTE (UX-02) — Accessibility tree + contrast checker del inspector:
       5. Cada checkbox/chip/card/stepper/link/boton tiene nombre accesible; cada GRUPO de controles
          aparece como "group" con su nombre.
       6. Contraste >=4.5:1 en cuerpo y en las 3 lineas de presupuesto (Neto/IVA/Total).
       7. Links "Volver a empezar" (wizard) y "Volver a editar" (resumen) PASAN (tras /60->/70).
       8. Advertencia cobre del dormitorio PASA (>=4.5:1).

    C) TECLADO END-TO-END SIN MOUSE (UX-02, D-03) — Tab/Shift+Tab/Enter/Espacio/flechas:
       9. Landing -> "Comenzar" con Enter mueve el foco al inicio del wizard.
       10. Cada control del wizard alcanzable por Tab, con foco VISIBLE y orden logico.
       11. Cards/chips operables con Enter/Espacio; steppers +/-; checkboxes con Espacio.
       12. "Siguiente"/"Anterior" mueven el foco al inicio del nuevo paso.
       13. Plano colapsable: el toggle se opera por teclado (Enter/Espacio), aria-expanded refleja el estado.
       14. Resumen: "Editar" por seccion, "Enviar por WhatsApp" (<a>) y "Descargar PDF" (<button>)
           operables por teclado; el foco al entrar al resumen arranca arriba.
       15. Los controles disabled (Anterior en Paso 1, Ampliado en N1/N2) se saltan limpiamente (no atrapan el foco).

    D) MOVIMIENTO REDUCIDO (UX-02, D-04) — DevTools Rendering -> emular prefers-reduced-motion: reduce:
       16. Cambiar de modelo/paso NO anima el plano (.fp-anim neutralizado).
       17. Hover de cards/botones y la barra de progreso tampoco animan.

    Si TODO pasa: aprobar. Si algo falla: describir la pantalla + el item exacto que fallo para gap closure.
  </how-to-verify>
  <resume-signal>Escriba "aprobado" si las 4 secciones (A/B/C/D) del UAT pasan en las 8 pantallas, o describa los items que fallaron (pantalla + numero de item)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuario -> UI (cliente) | Verificacion manual de UI client-side; sin red, sin backend, sin entrada de servidor |

**Este plan NO modifica codigo** (files_modified: []). Es un gate de verificacion. No agrega NI superficie
de ataque NI dependencias. La unica accion automatizada es correr npm test/build (sin instalar nada).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | (n/a) | UAT / smoke | accept | Plan de solo verificacion; sin cambios de codigo ni de dependencias. El UAT incluye confirmar que la app monta sin errores de consola (no regresion funcional) |

Sin amenazas (no hay cambios de codigo). El UAT ademas confirma indirectamente que los edits de los Planes
01-02 no regresaron comportamiento (smoke de consola limpia + npm test verde).
</threat_model>

<verification>
- `npm test && npm run build` verdes (guarda de regresion antes del UAT).
- UAT manual completo (secciones A/B/C/D de 07-VALIDATION.md) firmado por el humano en las 8 pantallas.
- Recorrido de teclado end-to-end (landing -> wizard -> resumen -> WhatsApp/PDF) sin mouse, exitoso.
</verification>

<success_criteria>
- UAT manual (375px / labels / contraste / teclado / reduced-motion) verde en las 8 pantallas (D-02). [UX-01, UX-02]
- Flujo completable solo con teclado de punta a punta (D-03). [UX-02]
- Checkpoint humano aprobado: "se ve y se opera bien a 375px + teclado".
- npm test/build verdes; sin deps nuevas; app monta sin errores de consola.
</success_criteria>

<output>
After completion, create `.planning/phases/07-pulido-mobile-y-accesibilidad/07-03-SUMMARY.md`
(registrar el resultado del UAT por seccion y el sign-off humano).
</output>
