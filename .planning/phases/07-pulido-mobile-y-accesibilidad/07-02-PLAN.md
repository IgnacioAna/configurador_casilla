---
phase: 07-pulido-mobile-y-accesibilidad
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/components/Landing.jsx
  - src/components/Resumen.jsx
  - src/components/FloorPlan.jsx
  - src/components/wizard/PlanoPanel.jsx
  - src/components/wizard/BarraPrecio.jsx
  - src/components/wizard/BarraProgreso.jsx
  - src/components/resumen/PresupuestoDesglosado.jsx
  - src/components/resumen/ConfigSecciones.jsx
  - src/components/resumen/BloqueFinanciacion.jsx
  - src/components/wizard/pasos/PasoUso.jsx
  - src/components/wizard/pasos/PasoDimensiones.jsx
  - src/components/wizard/pasos/PasoBano.jsx
  - src/components/wizard/pasos/PasoDormitorio.jsx
  - src/components/wizard/pasos/PasoExtras.jsx
autonomous: true
requirements: [UX-01, UX-02]

must_haves:
  truths:
    - "En un viewport de 375px no hay scroll horizontal en ninguna pantalla (Landing + Pasos 1-6 + Resumen)"
    - "Ningun nombre largo de extra ni precio se sale de su card en las filas flex justify-between (desglose, BarraPrecio, ConfigSecciones)"
    - "La barra de precio sticky a 375px queda dentro del ancho y legible"
    - "El plano colapsable a 375px cabe (SVG + cotas) y la leyenda hace wrap"
    - "Cada grupo de controles relacionados tiene un nombre accesible (role=group + aria-labelledby donde faltaba)"
  artifacts:
    - path: "src/components/resumen/PresupuestoDesglosado.jsx"
      provides: "Filas del desglose con min-w-0 + precio shrink-0 (sin overflow a 375px)"
      contains: "min-w-0"
    - path: "src/components/wizard/pasos/PasoUso.jsx"
      provides: "Grupos uso/ocupantes etiquetados (role=group + aria-labelledby)"
      contains: "role=\"group\""
    - path: "src/components/Landing.jsx"
      provides: "Landing usable a 375px sin desbordes"
      contains: "min-h-[44px]"
  key_links:
    - from: "filas flex justify-between con texto variable"
      to: "el precio/valor a la derecha"
      via: "min-w-0 en el texto izquierdo + shrink-0/whitespace-nowrap en el precio"
      pattern: "min-w-0"
    - from: "grupos de botones (cards/chips) sin label"
      to: "su titulo <p>"
      via: "role=group + aria-labelledby al id del <p>"
      pattern: "aria-labelledby"
---

<objective>
Auditar y corregir, POR SUPERFICIE, los desbordes a ~375px (UX-01, Pitfall 5) y completar los labels de
GRUPO que faltan (UX-02, Pattern 3) sobre las pantallas existentes: Landing, los 6 pasos del wizard, el plano
colapsable, la barra de precio, y las secciones/desglose/financiacion del resumen.

Solo se usan utilidades de Tailwind YA presentes (`min-w-0`, `flex-wrap`, `break-words`, `truncate`,
`shrink-0`, `whitespace-nowrap`) y `role="group"` + `aria-labelledby` para etiquetar grupos. NO hay rediseno,
NO se cambia el layout estructural, NO se toca la paleta, NO se agregan deps (D-02). Los controles individuales
ya tienen nombre accesible (cards/chips con aria-pressed, checkboxes con <label> envolvente, steppers con
aria-label — verificado); el hueco real son los GRUPOS sin etiqueta (los <p> titulo no asociados) y los
desbordes de texto largo.

Purpose: Que cada pantalla sea usable a 375px sin cortes/scroll horizontal y que el arbol de a11y nombre cada grupo.
Output: utilidades de overflow aplicadas donde el texto variable lo exige; role=group+aria-labelledby en los
grupos de controles que hoy solo tienen un <p> titulo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-pulido-mobile-y-accesibilidad/07-CONTEXT.md
@.planning/phases/07-pulido-mobile-y-accesibilidad/07-RESEARCH.md
@.planning/phases/07-pulido-mobile-y-accesibilidad/07-VALIDATION.md

<interfaces>
<!-- Estado real de cada superficie (leido del codigo). El executor edita IN SITU; no explorar. -->

Pitfall 5 (RESEARCH): nombres largos de extras (p.ej. "Sistema solar 220 off grid (...)") en filas
`flex justify-between` empujan el precio fuera del viewport. Fix: `min-w-0` al contenedor de texto +
`flex-wrap`/`break-words`; el precio (corto) con `whitespace-nowrap shrink-0`. Auditar a 375px en DevTools.

Filas flex justify-between con texto variable (candidatas a overflow):
- PresupuestoDesglosado.jsx: linea base modelo (l.36), items por grupo (l.50), "Otros" (l.67) — todas
  `<div className="flex items-center justify-between [pl-3]">` con `<span text-sm>{label}</span>` izq y
  `<span text-sm>{formatPrecio(...)}</span>` der. Tambien las 3 lineas Neto/IVA/Total (l.79-90).
- BarraPrecio.jsx: 3 filas `<div className="flex items-center justify-between">` con dt/dd (Neto/IVA/Total).
- ConfigSecciones.jsx: header `<div className="flex items-center">` con <h2> + boton "Editar" ml-auto (l.32);
  el valor `<p className="text-sm">{s.valor}</p>` (l.42) puede ser largo (extras.join(', ')).

Plano colapsable (PlanoPanel.jsx): la leyenda ya usa `flex flex-wrap gap-x-4 gap-y-2` (l.22) — verificar
que wrapea a 375px. El FloorPlan escala con width="100%". Verificar textos de cota a 375px (FloorPlan.jsx).

BarraPrecio sticky: montada en ConfiguratorWizard (Plan 01 owner del wizard) en un contenedor sticky
`bottom-0 ... p-3 ... lg:hidden`. ESTA tarea solo edita el CONTENIDO de BarraPrecio.jsx (las 3 filas),
no el contenedor sticky (ese vive en ConfiguratorWizard.jsx — NO se toca aca para no pisar Plan 01).

Grupos de controles con <p> titulo NO asociado (huecos de label de GRUPO, Pattern 3 (d)):
- PasoUso.jsx: bloque USO con `<p>...¿Para que va a usar la casilla?</p>` (l.101) seguido del grid de 5
  cards; bloque OCUPANTES con `<p>¿Cuantas personas...?</p>` (l.137) seguido de los chips. Ninguno usa
  role=group/aria-labelledby (los botones tienen aria-pressed individual, pero el GRUPO no tiene nombre).
- PasoDimensiones.jsx, PasoBano.jsx, PasoDormitorio.jsx, PasoExtras.jsx: revisar cada bloque de controles
  precedido por un <p>/<h3> titulo; donde el grupo no tenga nombre accesible, asociarlo.
- PATRON A SEGUIR (ya usado en PasoCocina para heladera): dar id al <p> y envolver el grupo de botones en
  `<div role="group" aria-labelledby="{id}">`. NO usar role="radiogroup". NO convertir a radios reales.

Landing.jsx: una columna centrada (`max-w-xl text-center px-6 py-12`); h1 text-3xl sm:text-4xl. Verificar
que a 375px nada se corta (el h1 hace wrap natural). Probablemente sin cambios o ajuste menor.

Resumen.jsx: contenedor `max-w-3xl px-6 py-12`, una columna, bloques mt-8. El plano en card bg-white/40 p-4.
Verificar a 375px que el plano y las secciones caben. Probablemente sin cambios o ajuste menor.

BarraProgreso.jsx: header `flex items-center justify-between text-sm` con "Paso X de N" y "NN%". A 375px
caben (textos cortos) — verificar; la barra usa `transition-all duration-300` (ya cubierta por reduced-motion global del Plan 01).

BloqueFinanciacion.jsx: lista de opciones (nombre + detalle). Verificar wrap del detalle a 375px.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Anti-overflow a 375px en las filas de precio/valor (UX-01, Pitfall 5)</name>
  <files>src/components/resumen/PresupuestoDesglosado.jsx, src/components/wizard/BarraPrecio.jsx, src/components/resumen/ConfigSecciones.jsx, src/components/resumen/BloqueFinanciacion.jsx</files>
  <read_first>
    - src/components/resumen/PresupuestoDesglosado.jsx (filas flex justify-between: l.36 base, l.50 items grupo, l.67 otros, l.79-90 Neto/IVA/Total).
    - src/components/wizard/BarraPrecio.jsx (3 filas dt/dd flex justify-between).
    - src/components/resumen/ConfigSecciones.jsx (header flex l.32; valor <p> l.42 que puede ser largo).
    - src/components/resumen/BloqueFinanciacion.jsx (opciones nombre+detalle).
    - 07-RESEARCH.md Pitfall 5 (min-w-0 + flex-wrap/break-words/truncate; precio whitespace-nowrap shrink-0).
    - 07-VALIDATION.md UAT seccion A (sin scroll horizontal, nada cortado).
  </read_first>
  <action>
    Para CADA fila `flex ... justify-between` donde el texto IZQUIERDO es variable (nombre de extra/label) y
    el DERECHO es un precio/valor:
    - Anadir `min-w-0 break-words` al `<span>`/`<dt>` de texto izquierdo (permite que se encoja y parta palabras
      largas en vez de empujar el ancho).
    - Anadir `whitespace-nowrap shrink-0 pl-2` al `<span>`/`<dd>` de precio derecho (el precio nunca se parte
      ni se encoge; pl-2 deja aire entre texto y precio).

    Aplicar concretamente:
    (A) PresupuestoDesglosado.jsx: en las filas de l.36 (base), l.50 (items de grupo) y l.67 (otros), y en
        las 3 filas Neto/IVA/Total (l.79-90), aplicar el patron min-w-0/break-words (izq) + whitespace-nowrap
        shrink-0 (der). Mantener `pl-3` de indentacion de los items de grupo.
    (B) BarraPrecio.jsx: en las 3 filas (Neto/IVA/Total), aplicar el mismo patron a dt (izq) / dd (der).
    (C) ConfigSecciones.jsx: el `<p className="text-sm">{s.valor}</p>` (l.42) anadir `break-words` (los valores
        de extras concatenados pueden ser largos). En el header flex (l.32), el <h2> con `min-w-0 break-words`
        y el boton "Editar" con `shrink-0` (ya tiene ml-auto) para que no se solapen a 375px.
    (D) BloqueFinanciacion.jsx: asegurar que el detalle de cada opcion hace wrap (anadir `break-words` si el
        contenedor lo necesita; verificar a 375px).

    NO cambiar formatPrecio, NO cambiar la logica, NO cambiar montos. Solo clases utilitarias de overflow.
  </action>
  <verify>
    <automated>npm test</automated>
    Manual DevTools (D-02): `npm run dev` -> responsive 375px -> Resumen y Pasos 4-6: confirmar 0 scroll
    horizontal; ningun precio "se sale" de su card; los nombres largos de extras parten linea / encogen y el
    precio queda alineado a la derecha dentro del ancho. La barra de precio sticky (Pasos 4-6) queda dentro del ancho.
  </verify>
  <acceptance_criteria>
    - `grep -c 'min-w-0' src/components/resumen/PresupuestoDesglosado.jsx` >= 1 y `grep -c 'whitespace-nowrap' src/components/resumen/PresupuestoDesglosado.jsx` >= 1.
    - `grep -c 'min-w-0' src/components/wizard/BarraPrecio.jsx` >= 1.
    - `grep -c 'break-words' src/components/resumen/ConfigSecciones.jsx` >= 1 y `grep -c 'shrink-0' src/components/resumen/ConfigSecciones.jsx` >= 1.
    - formatPrecio sigue importado/usado sin cambios: `grep -c 'formatPrecio' src/components/resumen/PresupuestoDesglosado.jsx` >= 1.
    - `npm test` sale 0; `npm run build` termina sin error.
    - Manual: a 375px, Resumen + Pasos 4-6 sin scroll horizontal ni precios cortados; sticky BarraPrecio dentro del ancho.
  </acceptance_criteria>
  <done>Las filas de precio/valor con texto variable usan min-w-0/break-words (izq) + whitespace-nowrap/shrink-0 (der); a 375px no hay overflow ni precios cortados; logica/montos intactos; npm test/build verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Audit 375px de Landing / Resumen / plano / barra de progreso (UX-01)</name>
  <files>src/components/Landing.jsx, src/components/Resumen.jsx, src/components/wizard/PlanoPanel.jsx, src/components/FloorPlan.jsx, src/components/wizard/BarraProgreso.jsx</files>
  <read_first>
    - src/components/Landing.jsx (columna centrada max-w-xl; h1 text-3xl sm:text-4xl).
    - src/components/Resumen.jsx (max-w-3xl una columna; plano en card; bloques mt-8).
    - src/components/wizard/PlanoPanel.jsx (leyenda flex flex-wrap l.22; toggle colapsable mobile l.57-71).
    - src/components/FloorPlan.jsx (SVG width="100%"; textos de cota — verificar a 375px).
    - src/components/wizard/BarraProgreso.jsx (header flex "Paso X de N" / "NN%").
    - 07-RESEARCH.md Pitfall 5 + Architecture (el plano escala width:100%; A3: "zoom" = el colapsable existente, NO un control nuevo).
    - 07-VALIDATION.md UAT seccion A (plano + cotas + leyenda caben/wrap a 375px).
  </read_first>
  <action>
    Auditar cada una de estas pantallas a 375px en DevTools y aplicar SOLO el fix puntual que cada desborde
    real requiera (no cambios cosmeticos preventivos):
    - Landing.jsx: confirmar que h1/subtitulo/CTA/datos de contacto caben sin scroll horizontal. Si algun
      texto fuerza overflow, anadir `break-words` al contenedor afectado. Sin rediseno.
    - Resumen.jsx: confirmar que el plano (card p-4) y las secciones caben en una columna a 375px. El padding
      px-6 ya da margen; si el plano fuerza overflow, asegurar que su contenedor permite encoger (el SVG ya es
      width="100%"). Sin cambio de layout.
    - PlanoPanel.jsx: abrir el colapsable a 375px y confirmar que el SVG + cotas caben y la leyenda
      (`flex flex-wrap`) wrapea. La leyenda ya tiene flex-wrap; si algun item no wrapea, ajustar gap/min-w-0.
    - FloorPlan.jsx: confirmar que los textos de cota no se cortan a 375px (el viewBox escala). Si un texto de
      cota desborda visualmente, es aceptable mientras NO genere scroll horizontal del documento (el SVG
      recorta a su viewBox); documentar en el SUMMARY si se observa.
    - BarraProgreso.jsx: confirmar que "Paso X de N" + "NN%" caben (textos cortos). Probablemente sin cambios.

    IMPORTANTE (A3, scope): NO agregar un control de zoom/fullscreen nuevo. "Zoom" en CONTEXT/PLANO-03 se
    refiere al colapsable existente. Solo se audita/ajusta lo que YA existe.

    Si una pantalla NO necesita cambios, dejarla intacta y registrarlo en el SUMMARY (auditada, sin desbordes).
  </action>
  <verify>
    <automated>npm test</automated>
    Manual DevTools (D-02): responsive 375px -> Landing, Resumen, plano colapsable abierto, BarraProgreso:
    confirmar 0 scroll horizontal, plano + cotas + leyenda caben/wrapean, nada cortado.
  </verify>
  <acceptance_criteria>
    - `npm test` sale 0; `npm run build` termina sin error.
    - PlanoPanel.jsx conserva el toggle colapsable: `grep -c 'aria-expanded' src/components/wizard/PlanoPanel.jsx` >= 1.
    - No se agrego un control de zoom nuevo: `grep -ci 'zoom' src/components/wizard/PlanoPanel.jsx` devuelve 0 y `grep -ci 'fullscreen' src/components/wizard/PlanoPanel.jsx` devuelve 0.
    - FloorPlan conserva su accesibilidad: `grep -c 'role="img"' src/components/FloorPlan.jsx` >= 1.
    - Manual: Landing + Resumen + plano colapsable + BarraProgreso sin scroll horizontal ni cortes a 375px (registrado en SUMMARY por pantalla).
  </acceptance_criteria>
  <done>Landing, Resumen, plano colapsable, FloorPlan cotas y BarraProgreso auditadas a 375px; fixes puntuales aplicados donde hubo desborde real; sin control de zoom nuevo; role=img/aria-expanded conservados; npm test/build verdes.</done>
</task>

<task type="auto">
  <name>Task 3: Labels de GRUPO en los pasos del wizard (UX-02, Pattern 3)</name>
  <files>src/components/wizard/pasos/PasoUso.jsx, src/components/wizard/pasos/PasoDimensiones.jsx, src/components/wizard/pasos/PasoBano.jsx, src/components/wizard/pasos/PasoDormitorio.jsx, src/components/wizard/pasos/PasoExtras.jsx</files>
  <read_first>
    - src/components/wizard/pasos/PasoUso.jsx (l.101 <p>¿Para que...?</p> + grid de cards; l.137 <p>¿Cuantas...?</p> + chips — grupos sin nombre accesible).
    - src/components/wizard/pasos/PasoDimensiones.jsx, PasoBano.jsx, PasoDormitorio.jsx, PasoExtras.jsx (cada bloque de controles precedido por un <p>/titulo).
    - 07-RESEARCH.md Pattern 3 (d) (role=group + aria-labelledby al <p>) — PATRON YA USADO en PasoCocina (heladera).
    - 07-CONTEXT.md D-01 (label de grupo donde falte; NO ARIA exhaustivo) + UX-03 (trato de usted en cualquier copy nuevo).
  </read_first>
  <action>
    Para cada GRUPO de controles relacionados (cards de uso, chips de ocupantes, cards de modelo, opciones de
    bano, steppers/checkboxes de dormitorio, checkboxes de extras) que hoy tiene un `<p>` titulo NO asociado:
    - Dar un `id` unico al `<p>` titulo (p.ej. `id="label-uso"`, `id="label-ocupantes"`, `id="label-modelo"`,
      etc.) si no lo tiene.
    - Envolver el contenedor de botones/controles del grupo en `<div role="group" aria-labelledby="{id}">`
      (reusando, si ya existe, el `<div>` contenedor del grid/flex — anadirle los dos atributos, sin agregar
      un nivel extra de div si no hace falta).
    - Usar `role="group"` (NO `radiogroup`). NO convertir a radios reales. NO agregar copy nuevo (reusar el
      texto existente del <p>); si hiciera falta texto, en espanol con trato de usted (UX-03).

    Aplicar concretamente:
    (A) PasoUso.jsx: `<p id="label-uso">¿Para que...?</p>` + el grid de cards en `<div role="group"
        aria-labelledby="label-uso" ...>`; `<p id="label-ocupantes">¿Cuantas...?</p>` + los chips en
        `<div role="group" aria-labelledby="label-ocupantes" ...>`.
    (B) PasoDimensiones.jsx: el grupo de cards de modelo etiquetado por su titulo (el <h2> "Dimensiones" o un
        <p> subtitulo) via role=group + aria-labelledby.
    (C) PasoBano.jsx: el grupo de tamano (estandar/ampliado, botones aria-pressed) y el grupo de equipamiento
        (checkboxes) etiquetados por sus titulos respectivos.
    (D) PasoDormitorio.jsx: el grupo de steppers de camas etiquetado por su titulo (los steppers ya tienen
        aria-label individual; el GRUPO necesita nombre).
    (E) PasoExtras.jsx: los grupos Confort / Energia (checkboxes) etiquetados por sus subheaders.

    Si un control individual quedara SIN nombre accesible (revisar el arbol de a11y), completarlo con el
    mecanismo que ya usa ese paso (Pattern 3 a/b/c) — pero la base ya esta cubierta (verificado), el foco es el GRUPO.
  </action>
  <verify>
    <automated>npm test</automated>
    Manual DevTools (D-02): arbol de accesibilidad de cada paso -> cada grupo de controles aparece como
    "group" con su nombre (el texto del titulo); cada control individual tiene accessible name. Gate anti-voseo:
    sin texto en segunda persona del singular informal nuevo.
  </verify>
  <acceptance_criteria>
    - `grep -c 'role="group"' src/components/wizard/pasos/PasoUso.jsx` >= 2 (uso + ocupantes) y `grep -c 'aria-labelledby' src/components/wizard/pasos/PasoUso.jsx` >= 2.
    - Cada uno de PasoDimensiones.jsx, PasoBano.jsx, PasoDormitorio.jsx, PasoExtras.jsx: `grep -c 'aria-labelledby' {archivo}` >= 1.
    - Ningun paso usa role="radiogroup": `grep -rc 'role="radiogroup"' src/components/wizard/pasos/` total = 0 (PasoCocina ya migrado en Plan 01).
    - No se introdujeron radios reales: `grep -rc 'role="radio"' src/components/wizard/pasos/` total = 0.
    - Gate anti-voseo en copy nuevo (si hubo): sin "elegi/configura/agrega" en imperativo voseante — el copy existente ya esta en usted; no agregar voseo.
    - `npm test` sale 0; `npm run build` termina sin error.
    - Manual: cada grupo de controles tiene nombre accesible en el arbol de a11y.
  </acceptance_criteria>
  <done>Cada grupo de controles de los pasos (uso, ocupantes, modelo, bano, dormitorio, extras) tiene role=group + aria-labelledby a su titulo; sin radiogroup ni radios reales; copy en usted; npm test/build verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuario -> UI (cliente) | Toda la interaccion es client-side; sin red, sin backend, sin entrada de servidor |
| localStorage -> reducer | Estado persistido puede venir adulterado (ya tolerado por el reducer; esta fase no agrega accesos sin guarda) |

**Esta fase NO agrega superficie de ataque nueva:** son utilidades de overflow de Tailwind y atributos
ARIA (role/aria-labelledby) sobre UI existente. NO hay logica nueva, NI inputs nuevos, NI red, NI deps (D-02).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | DoS (cliente) | Edits de markup en pasos/resumen que leen estado adulterado | accept | Solo se anaden clases utilitarias y atributos ARIA; la degradacion elegante existente (Array.isArray en extras, optional chaining) NO se toca |
| T-07-05 | Information Disclosure | aria-labelledby/role nuevos | accept | Los labels reusan texto de UI ya visible (no exponen datos nuevos); no hay PII ni secretos en la demo |
| T-07-06 | Tampering | Atributos de seguridad en componentes editados | accept | Este plan NO toca AccionesExport (owner: Plan 01) ni el `<a>` WhatsApp; ningun atributo rel/target se edita aqui |

No se esperan amenazas high-severity (block_on: high). Superficie de seguridad esencialmente cero.
</threat_model>

<verification>
- `npm test` verde (12 archivos; regresion de logica pura — edits de markup/clases no deben romper imports).
- `npm run build` (vite build) sin error en cada tarea.
- Manual DevTools (D-02): 375px sin scroll horizontal ni cortes en las 8 pantallas; cada grupo de controles
  con nombre accesible en el arbol de a11y.
- Sin deps nuevas. Sin role=radiogroup ni radios reales. Paleta locked intacta. Sin control de zoom nuevo (A3).
</verification>

<success_criteria>
- A 375px no hay scroll horizontal ni elementos cortados en Landing + Pasos 1-6 + Resumen (D-02 manual). [UX-01]
- Los nombres largos de extras no empujan el precio fuera de la card (min-w-0 + shrink-0). [UX-01]
- La barra de precio sticky a 375px queda dentro del ancho y legible. [UX-01]
- El plano colapsable a 375px cabe (SVG + cotas) y la leyenda wrapea; sin control de zoom nuevo (A3). [UX-01]
- Cada grupo de controles tiene nombre accesible (role=group + aria-labelledby) (D-01, Pattern 3). [UX-02]
- npm test/build verdes; sin deps nuevas; sin radiogroup; copy en usted.
</success_criteria>

<output>
After completion, create `.planning/phases/07-pulido-mobile-y-accesibilidad/07-02-SUMMARY.md`
</output>
