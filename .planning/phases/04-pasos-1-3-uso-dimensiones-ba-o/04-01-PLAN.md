---
phase: 04-pasos-1-3-uso-dimensiones-ba-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/banoReglas.js
  - src/utils/banoReglas.test.js
  - src/data/models.test.js
  - src/utils/floorplanLayout.js
  - src/utils/floorplanLayout.test.js
  - package.json
autonomous: true
requirements: [USO-03, BANO-02, BANO-03]
must_haves:
  truths:
    - "El umbral de baño ampliado se deriva de MODELOS.largo >= 6.1 (N1/N2 false, N3-N7 true), sin lista de ids hardcodeada"
    - "SUGERENCIA_OCUPANTES mapea cada ocupante (2/3/4/5/6/8) a un id existente en MODELOS"
    - "calcularLayout con bano.tamano:'ampliado' produce una zona baño mayor que con 'estandar' para el mismo modelo"
    - "Con baño ampliado la suma de largoM de las 5 zonas sigue siendo === config.largo"
    - "Con N3 (6.1m) ampliado, las 3 zonas centrales (bano/dormitorio/estar) tienen largoM positivo"
  artifacts:
    - path: "src/utils/banoReglas.js"
      provides: "Helper puro permiteBanoAmpliado(modeloId) + constante LARGO_MIN_BANO_AMPLIADO"
      exports: ["permiteBanoAmpliado", "LARGO_MIN_BANO_AMPLIADO"]
    - path: "src/utils/banoReglas.test.js"
      provides: "Tests de BANO-02 (umbral data-driven)"
    - path: "src/data/models.test.js"
      provides: "Test de USO-03 (sugerencia mapea a ids reales)"
    - path: "src/utils/floorplanLayout.js"
      provides: "RATIO_BANO parametrizado por bano.tamano (BANO-03)"
      contains: "RATIO_BANO_AMPLIADO"
  key_links:
    - from: "src/utils/banoReglas.js"
      to: "src/data/models.js"
      via: "import MODELOS + find por largo"
      pattern: "MODELOS\\.find"
    - from: "src/utils/floorplanLayout.js"
      to: "config.bano.tamano"
      via: "selección de ratio por tamaño"
      pattern: "bano\\?\\.tamano"
---

<objective>
Construir la capa de lógica pura y testeable de la Fase 4 ANTES de los componentes que la consumen.
Tres piezas: (1) el helper puro `banoReglas.js` con el umbral de baño ampliado derivado de datos
(BANO-02), (2) el test de datos que verifica la sugerencia de modelo (USO-03), y (3) el cambio
quirúrgico en `floorplanLayout.js` para que el plano refleje el tamaño del baño (BANO-03), con sus
tres tests nuevos.

Propósito: estos helpers son el contrato que `PasoDimensiones.jsx` y `PasoBano.jsx` (Waves 2-3)
importan. Construirlos primero (interface-first) evita que los componentes dupliquen lógica de
derivación o exploren el codebase. El cambio de layout es lógica geométrica pura (sin React/DOM),
por eso va acá y no en un componente.

Output: `src/utils/banoReglas.js` + 3 suites de test (`banoReglas.test.js`, `models.test.js`,
`floorplanLayout.test.js` extendido) verdes con `node --test`, y `floorplanLayout.js` modificado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-pasos-1-3-uso-dimensiones-ba-o/04-RESEARCH.md
@.planning/phases/04-pasos-1-3-uso-dimensiones-ba-o/04-PATTERNS.md
@.planning/phases/04-pasos-1-3-uso-dimensiones-ba-o/04-VALIDATION.md

<interfaces>
<!-- Contratos reales extraídos del codebase. El executor usa estos directamente, sin explorar. -->

Datos (src/data/models.js):
```js
export const MODELOS = [
  { id: 'N1', nombre: 'N1', largo: 4.5, camasBase: 2,   ocupantesIdeal: 2, personalizable: false, precioNeto: 29108976 },
  { id: 'N2', nombre: 'N2', largo: 5.2, camasBase: 2,   ocupantesIdeal: 3, personalizable: false, precioNeto: 34150926 },
  { id: 'N3', nombre: 'N3', largo: 6.1, camasBase: 3,   ocupantesIdeal: 4, personalizable: false, precioNeto: 37181710 },
  { id: 'N4', nombre: 'N4', largo: 6.6, camasBase: 4,   ocupantesIdeal: 4, personalizable: false, precioNeto: 38971845 },
  { id: 'N5', nombre: 'N5', largo: 7.6, camasBase: null, ocupantesIdeal: 6, personalizable: true,  precioNeto: 40937114 },
  { id: 'N6', nombre: 'N6', largo: 8.6, camasBase: null, ocupantesIdeal: 6, personalizable: true,  precioNeto: 42048149 },
  { id: 'N7', nombre: 'N7', largo: 9.6, camasBase: null, ocupantesIdeal: 8, personalizable: true,  precioNeto: 46180786 },
]
export const SUGERENCIA_OCUPANTES = { 2: 'N1', 3: 'N2', 4: 'N3', 5: 'N5', 6: 'N5', 8: 'N7' }
```

Geometría (src/data/geometry.js):
```js
export const GEOMETRIA = { anchoExterior: 2.6, anchoInterior: 2.52, anchoCama: 0.8, largoCama: 2.0,
  pasilloCentral: 0.92, zonaBaulera: 0.6, zonaCocina: 0.6 }
```

floorplanLayout.js (estado ACTUAL — lo que se modifica):
```js
// línea 21-23 (ratios FIJOS):
const RATIO_BANO = 0.22
const RATIO_DORMITORIO = 0.45
const RATIO_ESTAR = 0.33
// línea 26:
const MINIMO_CENTRAL = 0.4
// líneas 66-70 (reparto — el estar absorbe el residuo):
const restante = config.largo - GEOMETRIA.zonaBaulera - GEOMETRIA.zonaCocina
const largoBano = restante * RATIO_BANO
const largoDormitorio = restante * RATIO_DORMITORIO
const largoEstar = restante - largoBano - largoDormitorio
```
Nota: `config.bano.tamano` HOY se ignora en `calcularLayout`. La guarda `{ valido: false }` (líneas 57-63) no se toca.

mockConfig.js exporta SOLO: `CONFIG_MOCK_N4` (largo 6.6), `CONFIG_MOCK_N1` (largo 4.5), `CONFIGS_MOCK`.
NO existe `CONFIG_MOCK_N3` — el test de N3 construye el config inline: `{ largo: 6.1, bano: { tamano: 'ampliado' }, dormitorio: { camas: [] } }`.

Patrón de suite node:test (análogo formato.test.js / floorplanLayout.test.js):
```js
import { test } from 'node:test'
import assert from 'node:assert/strict'
const TOL = 1e-9   // tolerancia float (patrón existente)
```
package.json: `"type": "module"`, sin script `test`. Scripts actuales: dev, build, preview.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: Helper puro banoReglas.js (BANO-02) + su suite de test</name>
  <read_first>
    - src/data/models.js (fuente de verdad de MODELOS y sus largos — confirmar N3=6.1)
    - src/utils/floorplanLayout.js (análogo de helper puro: cabecera de comentario, import de /data, uso de find con ?. y ?? para tolerar estado adulterado)
    - src/utils/floorplanLayout.test.js líneas 1-10 (patrón de suite node:test: imports test/assert, comentario de cabecera con "Ejecutar: ...")
    - .planning/phases/04-pasos-1-3-uso-dimensiones-ba-o/04-PATTERNS.md sección "src/utils/banoReglas.js" (código a replicar) y "src/utils/banoReglas.test.js" (3 casos exactos)
  </read_first>
  <behavior>
    - Test 1: permiteBanoAmpliado('N1') === false y permiteBanoAmpliado('N2') === false (largo < 6.1)
    - Test 2: permiteBanoAmpliado('N3'..'N7') === true (largo >= 6.1)
    - Test 3: permiteBanoAmpliado('ZZ') === false (modeloId inexistente → tolera estado adulterado de localStorage, NO crashea)
    - Test 4: LARGO_MIN_BANO_AMPLIADO === 6.1
  </behavior>
  <action>
    Crear `src/utils/banoReglas.js` como helper PURO (sin React, sin DOM), replicando el estilo de
    módulo de `floorplanLayout.js` (comentario de cabecera que explica propósito + que el umbral se
    deriva del REQUIREMENT, NO de una lista de ids hardcodeada).

    Contenido exacto:
    ```js
    // Reglas de habilitación del baño (BANO-02). Helper PURO y testeable sin DOM.
    // El umbral se deriva del REQUIREMENT (N3 = 6,10 m); NO se hardcodea una lista de ids de modelo
    // (gate anti-hardcodeo de fases previas). Fuente de verdad del largo: src/data/models.js.
    import { MODELOS } from '../data/models.js'

    // Largo mínimo (m) que habilita "baño ampliado": N3=6,1. Constante nombrada, no lista de ids.
    export const LARGO_MIN_BANO_AMPLIADO = 6.1

    export function permiteBanoAmpliado(modeloId) {
      const modelo = MODELOS.find((m) => m.id === modeloId)
      return (modelo?.largo ?? 0) >= LARGO_MIN_BANO_AMPLIADO // N1/N2 → false; N3+ → true; id inválido → false
    }
    ```
    El `?.largo ?? 0` es OBLIGATORIO: tolera un `modeloId` adulterado leído de localStorage (control
    de seguridad Tampering del threat model) sin crashear.

    Crear `src/utils/banoReglas.test.js` con los 4 tests del bloque <behavior>, siguiendo el patrón
    de suite node:test (imports `test` de 'node:test' y `assert` de 'node:assert/strict', cabecera con
    "Ejecutar: node --test src/utils/banoReglas.test.js"). Para el test 2 iterar
    `for (const id of ['N3','N4','N5','N6','N7']) assert.equal(permiteBanoAmpliado(id), true)`.
  </action>
  <verify>
    <automated>node --test src/utils/banoReglas.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test src/utils/banoReglas.test.js` sale con 0 fallos (4/4 tests pass)
    - `grep -n "export function permiteBanoAmpliado" src/utils/banoReglas.js` devuelve 1 coincidencia
    - `grep -n "export const LARGO_MIN_BANO_AMPLIADO = 6.1" src/utils/banoReglas.js` devuelve 1 coincidencia
    - `grep -n "MODELOS.find" src/utils/banoReglas.js` devuelve 1 coincidencia (deriva de datos)
    - `grep -v '^//' src/utils/banoReglas.js | grep -cE "'N[1-7]'"` devuelve 0 (NO hay lista de ids hardcodeada en código)
    - `grep -n "?? 0" src/utils/banoReglas.js` devuelve 1 coincidencia (fallback anti-crash)
  </acceptance_criteria>
  <done>permiteBanoAmpliado devuelve false para N1/N2 y para ids inválidos, true para N3-N7; el umbral está como constante nombrada derivada del largo; la suite pasa.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: Test de datos models.test.js (USO-03 — sugerencia mapea a ids reales)</name>
  <read_first>
    - src/data/models.js (MODELOS + SUGERENCIA_OCUPANTES — confirmar el mapa 2→N1, 3→N2, 4→N3, 5→N5, 6→N5, 8→N7)
    - src/utils/floorplanLayout.test.js líneas 1-10 (patrón de cabecera + imports node:test)
    - .planning/phases/04-pasos-1-3-uso-dimensiones-ba-o/04-PATTERNS.md sección "src/data/models.test.js" (test exacto a replicar)
  </read_first>
  <behavior>
    - Test 1: cada valor de SUGERENCIA_OCUPANTES es un id existente en MODELOS (recorrer todas las entradas; ninguna apunta a un id inexistente)
    - Test 2: las claves de SUGERENCIA_OCUPANTES cubren las 6 opciones de ocupantes del REQUISITO (2,3,4,5,6,8) — USO-02
  </behavior>
  <action>
    Crear `src/data/models.test.js` siguiendo el patrón de suite node:test. Contenido:
    ```js
    // Tests de los datos de modelos (node:test). Ejecutar: node --test src/data/models.test.js
    // Codifica USO-03: SUGERENCIA_OCUPANTES mapea cada ocupante a un id existente de MODELOS.
    import { test } from 'node:test'
    import assert from 'node:assert/strict'
    import { MODELOS, SUGERENCIA_OCUPANTES } from './models.js'

    test('SUGERENCIA_OCUPANTES: cada valor es un id existente en MODELOS', () => {
      const ids = new Set(MODELOS.map((m) => m.id))
      for (const [ocupantes, id] of Object.entries(SUGERENCIA_OCUPANTES)) {
        assert.ok(ids.has(id), `ocupantes ${ocupantes} → ${id} debe existir en MODELOS`)
      }
    })

    test('SUGERENCIA_OCUPANTES: cubre las 6 opciones de ocupantes (2/3/4/5/6/8)', () => {
      const claves = Object.keys(SUGERENCIA_OCUPANTES).map(Number).sort((a, b) => a - b)
      assert.deepEqual(claves, [2, 3, 4, 5, 6, 8])
    })
    ```
    NO modificar `models.js` (los datos ya son correctos); este archivo es SOLO el test.
  </action>
  <verify>
    <automated>node --test src/data/models.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test src/data/models.test.js` sale con 0 fallos (2/2 tests pass)
    - `grep -n "SUGERENCIA_OCUPANTES" src/data/models.test.js` devuelve >= 2 coincidencias
    - `grep -n "\[2, 3, 4, 5, 6, 8\]" src/data/models.test.js` devuelve 1 coincidencia (cobertura de USO-02)
    - El archivo importa de './models.js' (no duplica los datos)
  </acceptance_criteria>
  <done>La suite verifica que toda sugerencia apunta a un modelo real y que las 6 opciones de ocupantes están cubiertas; pasa con node --test.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 3: BANO-03 — parametrizar RATIO_BANO por tamaño en floorplanLayout.js + 3 tests</name>
  <read_first>
    - src/utils/floorplanLayout.js COMPLETO (estado actual: RATIO_BANO=0.22 fijo líneas 21-23, MINIMO_CENTRAL líneas 26, reparto líneas 66-70, guarda valido:false líneas 57-63)
    - src/utils/floorplanLayout.test.js COMPLETO (patrón TOL=1e-9, reduce para suma, find por z.id; los tests existentes NO se deben romper)
    - src/data/mockConfig.js (confirmar que SOLO existen CONFIG_MOCK_N4 y CONFIG_MOCK_N1; NO existe CONFIG_MOCK_N3 → construir N3 inline)
    - src/data/geometry.js (zonaBaulera=0.6, zonaCocina=0.6)
    - .planning/phases/04-pasos-1-3-uso-dimensiones-ba-o/04-RESEARCH.md sección "Cambio mínimo para BANO-03" + Pitfall 1 (suma exacta) + Pitfall 2 (zonas negativas)
    - .planning/phases/04-pasos-1-3-uso-dimensiones-ba-o/04-PATTERNS.md sección "src/utils/floorplanLayout.js (MODIFICADO)" y "src/utils/floorplanLayout.test.js (EXTENDIDO)"
  </read_first>
  <behavior>
    - Test 1 (ampliado > estandar): para CONFIG_MOCK_N4, la zona 'bano' con bano.tamano:'ampliado' tiene largoM mayor que con 'estandar'
    - Test 2 (suma exacta): para CONFIG_MOCK_N4 con bano ampliado, la suma de largoM de las 5 zonas === config.largo (con TOL) — Pitfall 1
    - Test 3 (N3 sin zonas degeneradas): para { largo: 6.1, bano:{tamano:'ampliado'}, dormitorio:{camas:[]} }, las zonas bano/dormitorio/estar tienen largoM > 0 — Pitfall 2
    - Invariante: los 11 tests existentes de floorplanLayout.test.js siguen verdes (no romper la suma estandar ni el orden de zonas)
  </behavior>
  <action>
    MODIFICAR `src/utils/floorplanLayout.js` (cambio QUIRÚRGICO — NO tocar FloorPlan.jsx ni Bathroom.jsx):

    1. Reemplazar las constantes de ratio (líneas 21-23) por:
    ```js
    const RATIO_BANO_ESTANDAR = 0.22
    const RATIO_BANO_AMPLIADO = 0.30 // ≈ UI-SPEC; verificado por test: N3 ampliado deja zonas centrales > 0
    const RATIO_DORMITORIO = 0.45
    // RATIO_ESTAR se elimina como constante: el estar absorbe el residuo (suma exacta).
    ```
    2. En `calcularLayout`, DESPUÉS de la guarda `{ valido: false }` (tras línea 63) y al calcular el
       reparto (líneas 66-70), seleccionar el ratio por tamaño y mantener el patrón "estar absorbe el residuo":
    ```js
    const ratioBano =
      config?.bano?.tamano === 'ampliado' ? RATIO_BANO_AMPLIADO : RATIO_BANO_ESTANDAR
    const restante = config.largo - GEOMETRIA.zonaBaulera - GEOMETRIA.zonaCocina
    const largoBano = restante * ratioBano
    const largoDormitorio = restante * RATIO_DORMITORIO
    const largoEstar = restante - largoBano - largoDormitorio // el estar absorbe el residuo → suma exacta
    ```
    El `config?.bano?.tamano` con optional chaining es OBLIGATORIO: si bano falta o tamano no es
    'ampliado', cae a estándar (default seguro, tolera estado parcial/adulterado). NO tocar la guarda
    `{ valido: false }` de las líneas 57-63 (control DoS del threat model).

    3. EXTENDER `src/utils/floorplanLayout.test.js` agregando los 3 tests del bloque <behavior> AL FINAL
       del archivo, reutilizando `TOL`, `calcularLayout`, `CONFIG_MOCK_N4` (ya importados). Código:
    ```js
    // --- BANO-03: el tamaño de baño afecta el reparto del largo central. ---
    test('BANO-03: bano ampliado da largoBano mayor que estandar (mismo modelo)', () => {
      const est = calcularLayout({ ...CONFIG_MOCK_N4, bano: { tamano: 'estandar' } })
      const amp = calcularLayout({ ...CONFIG_MOCK_N4, bano: { tamano: 'ampliado' } })
      const bEst = est.zonas.find((z) => z.id === 'bano').largoM
      const bAmp = amp.zonas.find((z) => z.id === 'bano').largoM
      assert.ok(bAmp > bEst, `ampliado (${bAmp}) > estandar (${bEst})`)
    })

    test('BANO-03: con baño ampliado la suma de largoM === config.largo', () => {
      const { zonas } = calcularLayout({ ...CONFIG_MOCK_N4, bano: { tamano: 'ampliado' } })
      const suma = zonas.reduce((acc, z) => acc + z.largoM, 0)
      assert.ok(Math.abs(suma - CONFIG_MOCK_N4.largo) < TOL, `suma=${suma}`)
    })

    test('BANO-03: N3 ampliado deja las 3 zonas centrales con largoM positivo', () => {
      const { zonas } = calcularLayout({ largo: 6.1, bano: { tamano: 'ampliado' }, dormitorio: { camas: [] } })
      for (const id of ['bano', 'dormitorio', 'estar']) {
        assert.ok(zonas.find((z) => z.id === id).largoM > 0, `${id} debe ser > 0`)
      }
    })
    ```
    N3 se construye INLINE (largo 6.1) porque mockConfig.js NO exporta CONFIG_MOCK_N3.
  </action>
  <verify>
    <automated>node --test src/utils/floorplanLayout.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test src/utils/floorplanLayout.test.js` sale con 0 fallos (11 tests previos + 3 nuevos = 14/14 pass)
    - `grep -n "RATIO_BANO_AMPLIADO" src/utils/floorplanLayout.js` devuelve >= 2 coincidencias (declaración + uso)
    - `grep -n "config?.bano?.tamano === 'ampliado'" src/utils/floorplanLayout.js` devuelve 1 coincidencia
    - `grep -c "RATIO_ESTAR" src/utils/floorplanLayout.js` devuelve 0 (la constante fija RATIO_ESTAR fue eliminada; el estar absorbe el residuo)
    - `grep -n "valido: false" src/utils/floorplanLayout.js` sigue presente (>= 2 coincidencias — guarda DoS intacta)
    - `grep -c "BANO-03" src/utils/floorplanLayout.test.js` devuelve 3 (los 3 tests nuevos)
    - FloorPlan.jsx y Bathroom.jsx NO aparecen en el diff de esta tarea (cambio acotado a floorplanLayout.js + su test)
  </acceptance_criteria>
  <done>El reparto del largo usa RATIO_BANO_AMPLIADO=0.30 cuando bano.tamano==='ampliado'; la suma cierra exacta; N3 ampliado no degenera zonas; los 14 tests pasan y la guarda valido:false se conserva.</done>
</task>

<task type="auto">
  <name>Tarea 4: Script test en package.json (comando único de la suite)</name>
  <read_first>
    - package.json (scripts actuales: dev/build/preview; "type": "module")
    - .planning/phases/04-pasos-1-3-uso-dimensiones-ba-o/04-VALIDATION.md sección "Full suite command"
  </read_first>
  <action>
    Agregar al objeto `scripts` de package.json un script `test` que corra la suite completa listando
    las carpetas (evita depender de la expansión de glob `**` por shell):
    ```json
    "test": "node --test src/utils/banoReglas.test.js src/utils/floorplanLayout.test.js src/utils/formato.test.js src/data/models.test.js src/state/wizardReducer.test.js"
    ```
    NO usar glob `src/**/*.test.js` (no portable entre shells). Listar los archivos existentes + los
    nuevos de esta fase (banoReglas.test.js, models.test.js). NO agregar dependencias (sigue siendo
    node:test built-in, decisión 01-02).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "\"test\":" package.json` devuelve 1 coincidencia
    - `npm test` sale con código 0 y reporta todas las suites verdes (banoReglas + floorplanLayout + formato + models + wizardReducer)
    - `grep -c "vitest\|jest" package.json` devuelve 0 (sin dependencias de test nuevas)
  </acceptance_criteria>
  <done>`npm test` corre la suite completa con node:test y todo pasa; no se agregaron dependencias.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → app | El estado restaurado (`impacar_config_v1`) es entrada NO confiable: el usuario puede editarlo en DevTools. `modeloId` y `bano.tamano` pueden venir adulterados. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `banoReglas.permiteBanoAmpliado(modeloId)` con modeloId adulterado | mitigate | `MODELOS.find(...)?.largo ?? 0` → un id inexistente devuelve `false` (baño estándar), nunca crashea. Verificado por test 'modeloId inexistente → false'. |
| T-04-02 | DoS | `calcularLayout` con `bano` ausente o `tamano` inválido | mitigate | `config?.bano?.tamano === 'ampliado'` con optional chaining → cae a ratio estándar; la guarda `{ valido: false }` (líneas 57-63) se conserva intacta para largo imposible. Verificado por el test existente 'config inválida devuelve valido:false' que sigue verde. |
| T-04-03 | Tampering | Inyección de XSS vía valores del estado | accept | El layout solo produce números/ids controlados; no se renderiza HTML arbitrario. React escapa por defecto. Sin `dangerouslySetInnerHTML`. |
</threat_model>

<verification>
- `npm test` (o el listado de `node --test`) sale verde: banoReglas (4), models (2), floorplanLayout (14 = 11 previos + 3 BANO-03), más las suites preexistentes (formato, wizardReducer).
- `grep -c "RATIO_ESTAR" src/utils/floorplanLayout.js` === 0 y `grep -c "RATIO_BANO_AMPLIADO" src/utils/floorplanLayout.js` >= 2.
- Ningún cambio en FloorPlan.jsx, Bathroom.jsx, ni en archivos de datos (models.js/extras.js/geometry.js).
</verification>

<success_criteria>
- BANO-02: el umbral de baño ampliado vive en `banoReglas.js` como constante data-driven testeada.
- USO-03: `models.test.js` garantiza que la sugerencia apunta a modelos reales.
- BANO-03: `floorplanLayout.js` agranda la zona baño con 'ampliado' manteniendo la suma exacta y sin zonas negativas en N3.
- La suite completa pasa con `npm test`; no se agregaron dependencias.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/04-pasos-1-3-uso-dimensiones-ba-o/04-01-SUMMARY.md`.
</output>
