---
phase: 04-pasos-1-3-uso-dimensiones-ba-o
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - src/components/wizard/pasos/PasoBano.jsx
  - src/components/wizard/pasosRegistro.jsx
autonomous: false
requirements: [BANO-01, BANO-02, BANO-03]
must_haves:
  truths:
    - "El usuario elige el equipamiento del baño (inodoro c/depósito y cámara séptica, vanitory c/espejo) con checkboxes que se guardan en extras[]"
    - "El usuario elige el tamaño del baño (Estándar / Ampliado); 'Ampliado' está deshabilitado en N1/N2 con una nota explicativa"
    - "Al elegir 'Ampliado' el módulo de baño (celeste) crece en el plano con transición suave (BANO-03)"
    - "Los 3 pasos reales (Uso, Dimensiones, Baño) están enchufados en pasosRegistro.jsx reemplazando los stubs"
  artifacts:
    - path: "src/components/wizard/pasos/PasoBano.jsx"
      provides: "Paso 3 — checkboxes de equipamiento + selector de tamaño con disabled + subtítulo de posición fija"
      min_lines: 50
    - path: "src/components/wizard/pasosRegistro.jsx"
      provides: "Registro con los 3 componentes reales enchufados (uso/dimensiones/bano)"
      contains: "PasoBano"
  key_links:
    - from: "src/components/wizard/pasos/PasoBano.jsx"
      to: "data/extras.js EXTRAS (categoria 'bano')"
      via: "filtrado data-driven de equipamiento + toggle en extras[]"
      pattern: "categoria === 'bano'"
    - from: "src/components/wizard/pasos/PasoBano.jsx"
      to: "utils/banoReglas.js permiteBanoAmpliado"
      via: "disabled del chip Ampliado (BANO-02)"
      pattern: "permiteBanoAmpliado"
    - from: "src/components/wizard/pasosRegistro.jsx"
      to: "pasos/PasoUso.jsx, PasoDimensiones.jsx, PasoBano.jsx"
      via: "Componente real reemplaza StubPaso en las 3 primeras entradas"
      pattern: "Componente: Paso"
---

<objective>
Construir el Paso 3 (Baño) y enchufar los 3 pasos reales de la fase en el registro del wizard.
`PasoBano.jsx` cubre BANO-01 (equipamiento por checkboxes guardados en `extras[]`) y BANO-02
(selector de tamaño con "Ampliado" deshabilitado en N1/N2 vía `permiteBanoAmpliado` del Plan 01).
BANO-03 (el módulo de baño crece en el plano) ya está implementado en `floorplanLayout.js` (Plan 01);
acá solo se escribe `bano.tamano` y el plano reacciona con `.fp-anim` — el cierre del flujo end-to-end.

Finalmente, `pasosRegistro.jsx` reemplaza los 3 `StubPaso` de las entradas 'uso'/'dimensiones'/'bano'
por los componentes reales (PasoUso, PasoDimensiones, PasoBano), sin tocar la cáscara del wizard
(el registro es el contrato de enchufe SHELL-02). Este es el único plan que toca `pasosRegistro.jsx`
(file ownership exclusivo → evita conflicto con los Planes 02).

Output: `PasoBano.jsx` + `pasosRegistro.jsx` con los 3 pasos enchufados; flujo Pasos 1-3 navegable
con reflejo en vivo del plano.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-pasos-1-3-uso-dimensiones-ba-o/04-UI-SPEC.md
@.planning/phases/04-pasos-1-3-uso-dimensiones-ba-o/04-PATTERNS.md
@.planning/phases/04-pasos-1-3-uso-dimensiones-ba-o/04-RESEARCH.md

<interfaces>
<!-- Subcarpeta src/components/wizard/pasos/ → imports relativos ../../../ ; pasosRegistro.jsx está en wizard/ → import ./pasos/... -->

Datos de equipamiento (src/data/extras.js) — los 2 ítems de baño:
```js
{ id: 'inodoro-septica', nombre: 'Inodoro con depósito y cámara séptica', categoria: 'bano', precioNeto: 495000 }
{ id: 'vanitory-espejo', nombre: 'Vanitory completo con espejo',          categoria: 'bano', precioNeto: 283294 }
// filtrar: EXTRAS.filter((e) => e.categoria === 'bano')  → NO hardcodear ids
```

Estado (src/state/wizardReducer.js):
```js
estado.bano   = { tamano: 'estandar' | 'ampliado' }
estado.extras = string[]  // ids de accesorios — el equipamiento de baño va ACÁ (Pitfall 5), no en bano.equipamiento
// toggle inmutable:
const nuevos = estado.extras.includes(id) ? estado.extras.filter((x) => x !== id) : [...estado.extras, id]
dispatch({ type: ACCIONES.SET_CAMPO, campo: 'extras', valor: nuevos })
// cambio de tamaño (clonar bano):
dispatch({ type: ACCIONES.SET_CAMPO, campo: 'bano', valor: { ...estado.bano, tamano: 'ampliado' } })
```

Helper de Plan 04-01 (src/utils/banoReglas.js):
```js
export function permiteBanoAmpliado(modeloId): boolean   // N1/N2 → false; N3+ → true; id inválido → false
```

Registro actual (src/components/wizard/pasosRegistro.jsx) — las 3 primeras entradas a reemplazar:
```jsx
{ id: 'uso', titulo: 'Uso y ocupantes', Componente: (props) => <StubPaso numero={1} titulo="Uso y ocupantes" {...props} /> },
{ id: 'dimensiones', titulo: 'Dimensiones', Componente: (props) => <StubPaso numero={2} titulo="Dimensiones" {...props} /> },
{ id: 'bano', titulo: 'Baño', Componente: (props) => <StubPaso numero={3} titulo="Baño" {...props} /> },
// entradas 4-6 (dormitorio/cocina/extras) SIGUEN como StubPaso — son Phase 5.
```
El contrato de props inyectado por ConfiguratorWizard.jsx:54 es `estado` y `dispatch`.

BANO-03: ya implementado en floorplanLayout.js (Plan 04-01). Escribir bano.tamano y el plano
reacciona vía configDesdeEstado → PlanoPanel → FloorPlan + .fp-anim. NO tocar FloorPlan/Bathroom.

Clases heredadas (NO inventar tokens):
```
chip seleccionado: border-impacar-campo bg-impacar-campo/10 font-semibold text-impacar-campo
chip neutro:       border border-impacar-texto/10 hover:border-impacar-campo/40
chip disabled:     disabled:cursor-not-allowed disabled:opacity-40
checkbox marcado:  border-impacar-campo bg-impacar-campo/10 (input accent-[#2D5016])
touch target:      min-h-[44px] (chips: + min-w-[44px])
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: PasoBano.jsx — equipamiento (BANO-01) + tamaño con disabled (BANO-02)</name>
  <read_first>
    - src/components/wizard/pasos/PasoUso.jsx y PasoDimensiones.jsx (Plan 02 — replicar patrón de paso, profundidad de imports ../../../, clases de chip)
    - src/data/extras.js (los 2 ítems categoria 'bano' — filtrar, NO hardcodear ids)
    - src/state/wizardReducer.js (forma de estado.bano y estado.extras; SET_CAMPO reemplaza la clave entera)
    - src/utils/banoReglas.js (permiteBanoAmpliado — del Plan 01)
    - src/components/ConfiguratorWizard.jsx líneas 58-73 (patrón disabled heredado: disabled:cursor-not-allowed disabled:opacity-40)
    - .planning/phases/04-pasos-1-3-uso-dimensiones-ba-o/04-UI-SPEC.md Patrón B (tamaño) + Patrón C (checkboxes) + Copywriting Contract Paso 3 (copys exactos) + Estados (chip tamaño disabled)
    - .planning/phases/04-pasos-1-3-uso-dimensiones-ba-o/04-PATTERNS.md sección PasoBano.jsx (toggleExtra inmutable, chip Ampliado disabled, nota explicativa)
  </read_first>
  <action>
    Crear `src/components/wizard/pasos/PasoBano.jsx` (export default function PasoBano({ estado, dispatch })).
    Imports: `import { EXTRAS } from '../../../data/extras.js'`,
    `import { ACCIONES } from '../../../state/wizardReducer.js'`,
    `import { permiteBanoAmpliado } from '../../../utils/banoReglas.js'`.

    1. Título: `<h2 className="text-xl font-semibold text-impacar-texto">Baño</h2>` + subtítulo de
       posición fija (BANO-01): `El baño va en su posición fija, entre la baulera y el dormitorio.`
       (text-sm text-impacar-texto/70).

    2. Equipamiento (BANO-01, Patrón C), data-driven:
       ```js
       const extrasBano = EXTRAS.filter((e) => e.categoria === 'bano') // inodoro-septica, vanitory-espejo
       const toggleExtra = (id) => {
         const nuevos = estado.extras.includes(id)
           ? estado.extras.filter((x) => x !== id)
           : [...estado.extras, id]
         dispatch({ type: ACCIONES.SET_CAMPO, campo: 'extras', valor: nuevos }) // valor nuevo, NUNCA mutar
       }
       ```
       Subtítulo `Equipamiento del baño`. Lista vertical `space-y-2`. Cada ítem un `<label>` con
       `<input type="checkbox">` asociado (UX-02), área ≥44px:
       ```jsx
       {extrasBano.map((e) => {
         const marcado = estado.extras.includes(e.id)
         return (
           <label key={e.id} className={['flex min-h-[44px] items-center gap-3 rounded border p-3 cursor-pointer transition-colors',
             marcado ? 'border-impacar-campo bg-impacar-campo/10' : 'border-impacar-texto/10 bg-white/40'].join(' ')}>
             <input type="checkbox" checked={marcado} onChange={() => toggleExtra(e.id)} className="accent-[#2D5016]" />
             <span className="text-sm">{e.nombre}</span>
           </label>
         )
       })}
       ```
       El equipamiento se guarda en `extras[]` (Pitfall 5), NO en un campo `bano.equipamiento[]`.

    3. Tamaño del baño (BANO-02, Patrón B), separado por `mt-6`. Subtítulo `Tamaño del baño`.
       ```js
       const ampliadoHabilitado = permiteBanoAmpliado(estado.modeloId)
       const setTamano = (t) => dispatch({ type: ACCIONES.SET_CAMPO, campo: 'bano', valor: { ...estado.bano, tamano: t } })
       ```
       Dos chips en `flex flex-wrap gap-2`:
       - "Estándar": chip Patrón B, `aria-pressed={estado.bano.tamano === 'estandar'}`, onClick `setTamano('estandar')`.
       - "Ampliado": chip Patrón B, `aria-pressed={estado.bano.tamano === 'ampliado'}`,
         `disabled={!ampliadoHabilitado}` + `aria-disabled={!ampliadoHabilitado}`,
         onClick `setTamano('ampliado')`. Clases incluyen `disabled:cursor-not-allowed disabled:opacity-40`.
       Nota visible SOLO cuando deshabilitado, debajo de los chips (text-xs text-impacar-texto/70):
       ```jsx
       {!ampliadoHabilitado && (
         <p className="mt-2 text-xs text-impacar-texto/70">Disponible desde el modelo N3 (6,10 m) en adelante.</p>
       )}
       ```
       Copy EXACTO (trato de usted). El reflejo en el plano (BANO-03) ocurre solo al escribir
       bano.tamano (cableado existente + el cambio del Plan 01); NO importar FloorPlan/PlanoPanel acá.
  </action>
  <verify>
    <automated>node --check src/components/wizard/pasos/PasoBano.jsx && grep -q "permiteBanoAmpliado" src/components/wizard/pasos/PasoBano.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/components/wizard/pasos/PasoBano.jsx` sin error de sintaxis
    - `grep -n "EXTRAS.filter((e) => e.categoria === 'bano')" src/components/wizard/pasos/PasoBano.jsx` 1 coincidencia (data-driven)
    - `grep -cE "'inodoro-septica'|'vanitory-espejo'" src/components/wizard/pasos/PasoBano.jsx` devuelve 0 (no hardcodear ids de equipamiento)
    - `grep -n "type=\"checkbox\"" src/components/wizard/pasos/PasoBano.jsx` >= 1 (Patrón C; selección múltiple)
    - `grep -n "campo: 'extras'" src/components/wizard/pasos/PasoBano.jsx` 1 coincidencia (guarda en extras[], no en bano.equipamiento)
    - `grep -c "bano.equipamiento" src/components/wizard/pasos/PasoBano.jsx` devuelve 0 (Pitfall 5)
    - `grep -n "permiteBanoAmpliado(estado.modeloId)" src/components/wizard/pasos/PasoBano.jsx` 1 coincidencia (BANO-02 data-driven)
    - `grep -n "disabled={!ampliadoHabilitado}" src/components/wizard/pasos/PasoBano.jsx` 1 coincidencia
    - `grep -n "...estado.bano" src/components/wizard/pasos/PasoBano.jsx` >= 1 (inmutabilidad Pitfall 3)
    - `grep -n "Disponible desde el modelo N3 (6,10 m) en adelante." src/components/wizard/pasos/PasoBano.jsx` 1 coincidencia (copy exacto)
    - `grep -c "FloorPlan\|PlanoPanel" src/components/wizard/pasos/PasoBano.jsx` devuelve 0
    - `grep -ci "elegí\|marcá\|tocá" src/components/wizard/pasos/PasoBano.jsx` devuelve 0 (sin tuteo)
    - Inspección visual a ~375px (`npm run dev`, Paso 3): se ven 2 checkboxes de equipamiento que togglean; los chips Estándar/Ampliado; con N1/N2 "Ampliado" está atenuado y no clickeable, con la nota; con N3+ "Ampliado" se habilita y al elegirlo el módulo de baño celeste crece en el plano con transición. (manual)
  </acceptance_criteria>
  <done>El Paso 3 muestra los 2 ítems de equipamiento (toggle en extras[]) y el selector de tamaño con "Ampliado" deshabilitado + nota en N1/N2; al elegir Ampliado en N3+ escribe bano.tamano y el plano agranda la zona baño.</done>
</task>

<task type="auto">
  <name>Tarea 2: Enchufar los 3 pasos reales en pasosRegistro.jsx</name>
  <read_first>
    - src/components/wizard/pasosRegistro.jsx COMPLETO (las 6 entradas; solo se reemplazan las 3 primeras; las 4-6 quedan StubPaso)
    - src/components/wizard/pasos/PasoUso.jsx, PasoDimensiones.jsx, PasoBano.jsx (confirmar export default y nombre)
    - .planning/phases/04-pasos-1-3-uso-dimensiones-ba-o/04-PATTERNS.md sección "Enchufe en el registro de pasos" (contrato de enchufe sin tocar la cáscara)
  </read_first>
  <action>
    MODIFICAR `src/components/wizard/pasosRegistro.jsx`:
    1. Agregar imports al tope del archivo:
    ```jsx
    import PasoUso from './pasos/PasoUso.jsx'
    import PasoDimensiones from './pasos/PasoDimensiones.jsx'
    import PasoBano from './pasos/PasoBano.jsx'
    ```
    2. Reemplazar las 3 primeras entradas del array `PASOS` para que `Componente` apunte al componente
       real (los componentes ya aceptan { estado, dispatch }, que es lo que ConfiguratorWizard inyecta):
    ```jsx
    { id: 'uso', titulo: 'Uso y ocupantes', Componente: PasoUso },
    { id: 'dimensiones', titulo: 'Dimensiones', Componente: PasoDimensiones },
    { id: 'bano', titulo: 'Baño', Componente: PasoBano },
    ```
    3. DEJAR las entradas 4-6 (dormitorio, cocina, extras) como StubPaso — son Phase 5. NO tocarlas.
    4. La función `StubPaso` SIGUE en el archivo (la usan las entradas 4-6). NO eliminarla.
    NO modificar ConfiguratorWizard.jsx ni ningún otro archivo de la cáscara.
  </action>
  <verify>
    <automated>node --check src/components/wizard/pasosRegistro.jsx && grep -q "Componente: PasoUso" src/components/wizard/pasosRegistro.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/components/wizard/pasosRegistro.jsx` sin error de sintaxis
    - `grep -n "import PasoUso from './pasos/PasoUso.jsx'" src/components/wizard/pasosRegistro.jsx` 1 coincidencia
    - `grep -n "import PasoDimensiones from './pasos/PasoDimensiones.jsx'" src/components/wizard/pasosRegistro.jsx` 1 coincidencia
    - `grep -n "import PasoBano from './pasos/PasoBano.jsx'" src/components/wizard/pasosRegistro.jsx` 1 coincidencia
    - `grep -c "Componente: Paso" src/components/wizard/pasosRegistro.jsx` devuelve 3 (uso/dimensiones/bano)
    - `grep -c "StubPaso numero=" src/components/wizard/pasosRegistro.jsx` devuelve 3 (dormitorio/cocina/extras siguen stub — Phase 5)
    - `grep -c "function StubPaso" src/components/wizard/pasosRegistro.jsx` devuelve 1 (StubPaso conservado)
  </acceptance_criteria>
  <done>El registro enchufa PasoUso/PasoDimensiones/PasoBano en los 3 primeros pasos; los pasos 4-6 siguen como stub; la cáscara no se tocó.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Flujo completo de los Pasos 1-3 del wizard, ya enchufado: Uso → Dimensiones → Baño, con reflejo en vivo del plano (incluido el crecimiento del baño ampliado de BANO-03).</what-built>
  <how-to-verify>
    `npm run dev`, navegador a ~375px, "Comenzar" desde la landing:
    1. Recorrido end-to-end: ¿se navega Paso 1 → 2 → 3 con "Siguiente"/"Anterior" y la barra de progreso avanza? ¿Los 3 pasos muestran contenido real (ya no "estará disponible próximamente")?
    2. Paso 1: elegir un uso y ocupantes; ¿aparece la sugerencia de modelo (usted)?
    3. Paso 2: ¿el modelo sugerido viene pre-marcado? Cambiar de modelo; ¿el plano se redibuja con transición?
    4. Paso 3 — equipamiento: marcar/desmarcar inodoro y vanitory; ¿el toggle funciona y persiste al cambiar de paso y volver?
    5. Paso 3 — tamaño con N3+: con un modelo N3 o mayor, ¿"Ampliado" está habilitado? Al elegir "Ampliado", ¿la zona de baño (celeste) crece en el plano con transición suave y el resto de las zonas se recomprime?
    6. Paso 3 — tamaño con N1/N2: volver al Paso 2, elegir N1 o N2; volver al Paso 3, ¿"Ampliado" está deshabilitado con la nota "Disponible desde el modelo N3 (6,10 m) en adelante."? Si tenía Ampliado, ¿se forzó a Estándar (corrección Q4)?
    7. Persistencia: recargar la página (F5); ¿se retoma en el mismo paso con las selecciones intactas?
    8. Sin precios: ¿NO aparece ningún "$" ni total en estos 3 pasos?
    9. Mobile ~375px: ¿sin desbordes, touch targets cómodos, plano colapsable con "Ver plano actual"?
  </how-to-verify>
  <resume-signal>Escriba "aprobado" o describa qué ajustar.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → app | `estado.modeloId`, `estado.bano.tamano`, `estado.extras` se restauran desde `impacar_config_v1`; pueden venir adulterados (id de extra inexistente, tamano inválido, modeloId desconocido). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | `PasoBano` habilitando "Ampliado" con un `modeloId` adulterado | mitigate | `permiteBanoAmpliado(estado.modeloId)` (Plan 01) devuelve `false` para ids inexistentes → "Ampliado" queda deshabilitado por defecto ante estado adulterado, nunca crashea. |
| T-04-08 | Tampering | `estado.extras` con ids de extra inexistentes | mitigate | El render de checkboxes itera SOLO sobre `EXTRAS.filter(categoria==='bano')` (lista controlada); un id basura en `estado.extras` no genera UI, solo afecta `.includes()` (booleano inocuo). Phase 5 sumará precios solo de ids reales de EXTRAS. |
| T-04-09 | DoS | `bano.tamano` con valor inválido (ni 'estandar' ni 'ampliado') propagado al plano | mitigate | `calcularLayout` (Plan 01) usa `config?.bano?.tamano === 'ampliado'`; cualquier otro valor cae a ratio estándar (default seguro), sin romper la suma ni la guarda valido:false. |
| T-04-10 | Info Disclosure / XSS | Render de nombres de equipamiento y copys | accept | Nombres vienen de `/data` controlado; React escapa por defecto; sin `dangerouslySetInnerHTML`. |
</threat_model>

<verification>
- `node --check` pasa para PasoBano.jsx y pasosRegistro.jsx.
- Grep gates (data-driven, anti-hardcodeo de ids, extras[] no bano.equipamiento, copy exacto, anti-tuteo, 3 enchufados / 3 stubs) verdes.
- Suite del Plan 01 sigue verde: `npm test`.
- Checkpoint humano aprobado: recorrido 1-3 end-to-end, baño ampliado crece en el plano, disabled en N1/N2 con nota, persistencia, sin precios.
</verification>

<success_criteria>
- BANO-01: equipamiento por checkboxes (inodoro/vanitory) guardado en extras[].
- BANO-02: tamaño Estándar/Ampliado; "Ampliado" deshabilitado en N1/N2 con nota explicativa; corrección a estándar al bajar de modelo (Q4, vía Plan 02).
- BANO-03: al elegir "Ampliado" el módulo de baño crece en el plano con transición (motor del Plan 01).
- Los 3 pasos reales enchufados; pasos 4-6 siguen stub; flujo navegable y persistente; sin precios.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/04-pasos-1-3-uso-dimensiones-ba-o/04-03-SUMMARY.md`.
</output>
