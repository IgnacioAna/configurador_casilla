---
phase: 04-pasos-1-3-uso-dimensiones-ba-o
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/wizard/pasos/PasoUso.jsx
  - src/components/wizard/pasos/PasoDimensiones.jsx
autonomous: false
requirements: [USO-01, USO-02, USO-03, DIM-01, DIM-02]
must_haves:
  truths:
    - "El usuario ve 5 cards de uso (Contratista rural, Ganadero, Agrícola, Vivienda, Otro) con ícono SVG inline y selecciona una sola"
    - "El usuario elige la cantidad de ocupantes entre 2/3/4/5/6/8 en chips segmentados"
    - "Al elegir ocupantes el sistema pre-selecciona el modelo de SUGERENCIA_OCUPANTES y muestra el feedback de sugerencia (trato de usted)"
    - "El usuario ve 7 cards de modelo (N1-N7) con largo, camas base e 'ideal para N personas', con el sugerido marcado por un badge"
    - "Al elegir un modelo el plano se redibuja (cableado configDesdeEstado existente)"
  artifacts:
    - path: "src/components/wizard/pasos/PasoUso.jsx"
      provides: "Paso 1 — cards de uso + chips de ocupantes + feedback de sugerencia"
      min_lines: 60
    - path: "src/components/wizard/pasos/PasoDimensiones.jsx"
      provides: "Paso 2 — 7 cards de modelo con metadatos + badge Sugerido + corrección silenciosa de baño ampliado"
      min_lines: 50
  key_links:
    - from: "src/components/wizard/pasos/PasoUso.jsx"
      to: "wizardReducer ACCIONES.SET_CAMPO"
      via: "dispatch al seleccionar uso/ocupantes/modelo sugerido"
      pattern: "SET_CAMPO"
    - from: "src/components/wizard/pasos/PasoUso.jsx"
      to: "data/models.js SUGERENCIA_OCUPANTES"
      via: "pre-selección data-driven de modeloId"
      pattern: "SUGERENCIA_OCUPANTES"
    - from: "src/components/wizard/pasos/PasoDimensiones.jsx"
      to: "data/models.js MODELOS + utils/banoReglas.js"
      via: "metadatos de card + corrección de baño ampliado"
      pattern: "permiteBanoAmpliado"
---

<objective>
Construir los componentes de los Pasos 1 (Uso y ocupantes) y 2 (Dimensiones / Modelo) del wizard.
Ambos son componentes presentacionales que reciben `{ estado, dispatch }`, leen de `/data` y
disparan `SET_CAMPO`. NO se inventan estilos: se copian las clases Tailwind del UI-SPEC (Patrones A/B/D/E).
NO se pasa nada al plano por props: el cableado `configDesdeEstado → PlanoPanel → FloorPlan` ya
existe; al escribir `modeloId` el plano reacciona solo con la transición de 300ms (DIM-02).

Propósito: cubrir USO-01/02/03 (selección de uso, ocupantes y sugerencia automática) y DIM-01/02
(7 modelos reales con badge "Sugerido" + reflejo en vivo). Es el corazón visual de la fase.

Output: `PasoUso.jsx` y `PasoDimensiones.jsx` en la subcarpeta nueva `src/components/wizard/pasos/`.
El enchufe en `pasosRegistro.jsx` lo hace el Plan 03 (Wave 3) para no compartir ese archivo entre planes.
Hasta entonces estos componentes quedan creados pero no enchufados (no rompen nada: los stubs siguen activos).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-pasos-1-3-uso-dimensiones-ba-o/04-UI-SPEC.md
@.planning/phases/04-pasos-1-3-uso-dimensiones-ba-o/04-PATTERNS.md
@.planning/phases/04-pasos-1-3-uso-dimensiones-ba-o/04-RESEARCH.md

<interfaces>
<!-- Contratos reales. La subcarpeta es src/components/wizard/pasos/ → imports relativos con ../../../ -->

Contrato de props del paso (ConfiguratorWizard.jsx:54):
```jsx
<Paso.Componente estado={estado} dispatch={dispatch} />
```

Reducer (src/state/wizardReducer.js):
```js
export const ACCIONES = { SET_CAMPO: 'SET_CAMPO', SIGUIENTE, ANTERIOR, IR_A_PASO, RESET }
// SET_CAMPO reemplaza la clave entera: { ...estado, [accion.campo]: accion.valor }
export const estadoInicial = {
  pasoActual: 0, uso: null, ocupantes: null, modeloId: 'N4',
  bano: { tamano: 'estandar' }, dormitorio: { camas: [] },
  cocina: { horno: false, heladera: null }, estar: { mesa: false }, extras: [],
}
// dispatch para escribir config:
dispatch({ type: ACCIONES.SET_CAMPO, campo: 'uso',      valor: <string> })
dispatch({ type: ACCIONES.SET_CAMPO, campo: 'ocupantes', valor: <number> })
dispatch({ type: ACCIONES.SET_CAMPO, campo: 'modeloId',  valor: 'Nx' })
dispatch({ type: ACCIONES.SET_CAMPO, campo: 'bano',      valor: { ...estado.bano, tamano: 'estandar' } }) // corrección Q4
```

Datos (src/data/models.js):
```js
MODELOS: id, nombre, largo, camasBase (null en N5/N6/N7), ocupantesIdeal, personalizable
  N1=4.5/2 · N2=5.2/2 · N3=6.1/3 · N4=6.6/4 · N5=7.6/null · N6=8.6/null · N7=9.6/null
SUGERENCIA_OCUPANTES = { 2:'N1', 3:'N2', 4:'N3', 5:'N5', 6:'N5', 8:'N7' }
```

Helper de Plan 04-01 (src/utils/banoReglas.js):
```js
export function permiteBanoAmpliado(modeloId): boolean   // N1/N2 → false, N3+ → true, id inválido → false
export const LARGO_MIN_BANO_AMPLIADO = 6.1
```

Reflejo en el plano: NO pasar props a FloorPlan. ConfiguratorWizard.jsx:18 ya hace
`configDesdeEstado(estado)` y lo pasa a PlanoPanel. Escribir modeloId → plano reacciona con .fp-anim.

Clases Tailwind heredadas (NO inventar tokens nuevos):
```
seleccionado: border-impacar-campo bg-impacar-campo/10   (chips: + font-semibold text-impacar-campo)
neutro:       border border-impacar-texto/10 bg-white/40 hover:border-impacar-campo/40 hover:bg-impacar-campo/5
foco:         focus:outline-none focus:ring-2 focus:ring-impacar-campo/40
touch target: min-h-[44px]  (chips: + min-w-[44px])
título paso:  text-xl font-semibold text-impacar-texto
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: PasoUso.jsx — cards de uso (USO-01) + chips de ocupantes (USO-02) + sugerencia (USO-03)</name>
  <read_first>
    - src/components/wizard/pasosRegistro.jsx (contrato de props del stub + título tipográfico text-xl font-semibold text-impacar-texto)
    - src/components/ConfiguratorWizard.jsx líneas 53-54 (la section que envuelve al paso ya tiene rounded border bg-white/40 p-4 — el paso NO duplica ese contenedor) y líneas 58-73 (clases de botón heredadas)
    - src/data/models.js (SUGERENCIA_OCUPANTES — NO hardcodear el mapeo en el componente)
    - src/state/wizardReducer.js (ACCIONES.SET_CAMPO, forma del estado: uso/ocupantes/modeloId)
    - .planning/phases/04-pasos-1-3-uso-dimensiones-ba-o/04-UI-SPEC.md Patrón A (card), Patrón B (chip), Patrón D (feedback) + Copywriting Contract Paso 1 (copys exactos)
    - .planning/phases/04-pasos-1-3-uso-dimensiones-ba-o/04-PATTERNS.md sección PasoUso.jsx (clases exactas, handlers elegirUso/elegirOcupantes)
  </read_first>
  <action>
    Crear `src/components/wizard/pasos/PasoUso.jsx` (export default function PasoUso({ estado, dispatch })).
    Imports relativos desde la subcarpeta: `import { ACCIONES } from '../../../state/wizardReducer.js'`
    y `import { SUGERENCIA_OCUPANTES } from '../../../data/models.js'`.

    Estructura:
    1. Título del paso: `<h2 className="text-xl font-semibold text-impacar-texto">Uso y ocupantes</h2>`
       (NO envolver en otra card: la <section> de ConfiguratorWizard ya aporta el contenedor blanco).

    2. Bloque USO (USO-01): subtítulo `¿Para qué va a usar la casilla?` (text-sm text-impacar-texto/70).
       Definir la lista de usos como constante local (es un conjunto de UI, no de datos de negocio):
       ```js
       const USOS = [
         { id: 'contratista', label: 'Contratista rural' },
         { id: 'ganadero',    label: 'Ganadero' },
         { id: 'agricola',    label: 'Agrícola' },
         { id: 'vivienda',    label: 'Vivienda' },
         { id: 'otro',        label: 'Otro' },
       ]
       ```
       Grid `grid grid-cols-1 sm:grid-cols-2 gap-3`. Cada card es un `<button type="button">` (Patrón A),
       NO un `<div>`, con `aria-pressed={estado.uso === u.id}` y onClick `elegirUso(u.id)`. Clases exactas:
       ```
       min-h-[44px] rounded p-4 text-left transition-colors focus:outline-none focus:ring-2 focus:ring-impacar-campo/40
       seleccionado → border border-impacar-campo bg-impacar-campo/10
       neutro       → border border-impacar-texto/10 bg-white/40 hover:border-impacar-campo/40 hover:bg-impacar-campo/5
       ```
       Cada card incluye un SVG inline 24×24 arriba del label (stroke="currentColor" fill="none"
       strokeWidth="1.5", clase text-impacar-campo si seleccionada / text-impacar-texto/70 si no).
       Íconos sugeridos (line icons, dibujo simple): contratista→casco/herramienta, ganadero→vaca/cerca,
       agrícola→espiga/tractor, vivienda→casa, otro→tres puntos. NO usar librería de íconos (SVG a mano).
       handler: `const elegirUso = (id) => dispatch({ type: ACCIONES.SET_CAMPO, campo: 'uso', valor: id })`

    3. Bloque OCUPANTES (USO-02), separado del bloque uso por `mt-6`: subtítulo
       `¿Cuántas personas la van a habitar?`. Constante `const OCUPANTES = [2, 3, 4, 5, 6, 8]`.
       Fila de chips `flex flex-wrap gap-2`, cada chip `<button type="button">` (Patrón B) con
       `aria-pressed={estado.ocupantes === n}`. Clases exactas:
       ```
       min-h-[44px] min-w-[44px] rounded px-4 py-2 text-sm transition-colors focus:outline-none focus:ring-2 focus:ring-impacar-campo/40
       seleccionado → border border-impacar-campo bg-impacar-campo/10 font-semibold text-impacar-campo
       neutro       → border border-impacar-texto/10 hover:border-impacar-campo/40
       ```
       handler USO-03 (pre-selección data-driven, NO hardcodear el mapeo):
       ```js
       const elegirOcupantes = (n) => {
         dispatch({ type: ACCIONES.SET_CAMPO, campo: 'ocupantes', valor: n })
         const sugerido = SUGERENCIA_OCUPANTES[n]
         if (sugerido) dispatch({ type: ACCIONES.SET_CAMPO, campo: 'modeloId', valor: sugerido })
       }
       ```

    4. Feedback de sugerencia (USO-03, Patrón D), visible solo si `estado.ocupantes` está elegido,
       bajo los chips (`mt-3`). NO toast ni modal — panel inline:
       ```jsx
       {estado.ocupantes && SUGERENCIA_OCUPANTES[estado.ocupantes] && (
         <div className="mt-3 rounded border border-impacar-campo/30 bg-impacar-campo/5 p-3 text-sm text-impacar-texto">
           Le sugerimos el modelo {SUGERENCIA_OCUPANTES[estado.ocupantes]} para {estado.ocupantes} personas. Lo puede cambiar en el paso siguiente.
         </div>
       )}
       ```
       Copy EXACTO (trato de usted). NO tutear.
  </action>
  <verify>
    <automated>node --check src/components/wizard/pasos/PasoUso.jsx && grep -q "SUGERENCIA_OCUPANTES" src/components/wizard/pasos/PasoUso.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/components/wizard/pasos/PasoUso.jsx` no reporta error de sintaxis
    - `grep -c "type=\"button\"" src/components/wizard/pasos/PasoUso.jsx` devuelve >= 11 (5 cards uso + 6 chips ocupantes)
    - `grep -c "aria-pressed" src/components/wizard/pasos/PasoUso.jsx` devuelve >= 11
    - `grep -n "SET_CAMPO, campo: 'uso'" src/components/wizard/pasos/PasoUso.jsx` 1 coincidencia
    - `grep -n "SET_CAMPO, campo: 'ocupantes'" src/components/wizard/pasos/PasoUso.jsx` 1 coincidencia
    - `grep -n "SUGERENCIA_OCUPANTES\[" src/components/wizard/pasos/PasoUso.jsx` >= 1 (mapeo data-driven, no if/switch)
    - `grep -c "\[2, 3, 4, 5, 6, 8\]" src/components/wizard/pasos/PasoUso.jsx` devuelve 1 (USO-02)
    - `grep -n "Le sugerimos el modelo" src/components/wizard/pasos/PasoUso.jsx` 1 coincidencia (copy de usted exacto)
    - `grep -c "<svg" src/components/wizard/pasos/PasoUso.jsx` devuelve >= 5 (un ícono SVG inline por uso)
    - `grep -ci "elegí\|seleccioná\|tené" src/components/wizard/pasos/PasoUso.jsx` devuelve 0 (sin tuteo)
    - Inspección visual a ~375px (`npm run dev`, Paso 1): se ven 5 cards con ícono en 1 columna, click marca UNA sola con borde verde; los 6 chips de ocupantes en fila; al elegir ocupantes aparece el panel de sugerencia con "Le sugerimos el modelo Nx ...". (manual — sin React Testing Library en el stack, decisión 01-02)
  </acceptance_criteria>
  <done>El Paso 1 renderiza 5 cards de uso con ícono (selección única vía aria-pressed), 6 chips de ocupantes, y al elegir ocupantes pre-selecciona modeloId desde SUGERENCIA_OCUPANTES y muestra el feedback en trato de usted.</done>
</task>

<task type="auto">
  <name>Tarea 2: PasoDimensiones.jsx — 7 cards de modelo (DIM-01) + badge Sugerido + reflejo en vivo (DIM-02)</name>
  <read_first>
    - src/components/wizard/pasos/PasoUso.jsx (recién creado — replicar el patrón de card/handler y la profundidad de imports ../../../)
    - src/data/models.js (MODELOS completo: largo, camasBase null en N5/N6/N7, ocupantesIdeal; SUGERENCIA_OCUPANTES)
    - src/utils/banoReglas.js (permiteBanoAmpliado — del Plan 04-01; usado para la corrección silenciosa Q4)
    - src/state/wizardReducer.js (SET_CAMPO; forma de estado.bano = { tamano })
    - .planning/phases/04-pasos-1-3-uso-dimensiones-ba-o/04-UI-SPEC.md Patrón A + Patrón E (badge) + Copywriting Contract Paso 2 + Estados (matriz)
    - .planning/phases/04-pasos-1-3-uso-dimensiones-ba-o/04-PATTERNS.md sección PasoDimensiones.jsx (metadatosCard, elegirModelo con corrección Q4, esSugerido, Pitfall 4)
  </read_first>
  <action>
    Crear `src/components/wizard/pasos/PasoDimensiones.jsx` (export default function PasoDimensiones({ estado, dispatch })).
    Imports: `import { MODELOS, SUGERENCIA_OCUPANTES } from '../../../data/models.js'`,
    `import { ACCIONES } from '../../../state/wizardReducer.js'`,
    `import { permiteBanoAmpliado } from '../../../utils/banoReglas.js'`.

    1. Título: `<h2 className="text-xl font-semibold text-impacar-texto">Dimensiones</h2>` + subtítulo
       `Elija el modelo de su casilla` (text-sm text-impacar-texto/70). (trato de usted)

    2. Metadatos de card (DIM-01) — formato argentino con coma decimal; camasBase null → "camas a medida":
       ```js
       function metadatosCard(m) {
         const largo = `${m.largo.toFixed(2).replace('.', ',')} m`          // "6,60 m"
         const camas = m.camasBase === null ? 'camas a medida' : `${m.camasBase} camas`
         return `${largo} · ${camas} · ideal para ${m.ocupantesIdeal} personas`
       }
       ```
       NO hardcodear largos ni la lista de modelos: mapear sobre MODELOS (anti-hardcodeo).

    3. Grid `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3`. Por cada `m` de MODELOS un
       `<button type="button">` (Patrón A, SIN ícono — los íconos son solo de uso) con
       `aria-pressed={estado.modeloId === m.id}`, onClick `elegirModelo(m.id)`. Clases idénticas a
       las cards de uso (seleccionado border-impacar-campo bg-impacar-campo/10 / neutro
       border-impacar-texto/10 bg-white/40 hover... / foco ring). Contenido de la card:
       - nombre del modelo `m.nombre` (text-sm font-semibold; text-impacar-campo si seleccionada)
       - badge "Sugerido" (Patrón E) si `m.id === SUGERENCIA_OCUPANTES[estado.ocupantes]` — pill
         `text-xs font-semibold text-impacar-campo bg-impacar-campo/10 rounded-full px-2 py-0.5`.
         INDEPENDIENTE del estado seleccionado (Pitfall 4): el badge queda en su card aunque el
         usuario elija otro modelo; el borde verde es lo que se mueve.
       - línea de metadatos `metadatosCard(m)` (text-xs text-impacar-texto/70).

    4. Handler con corrección silenciosa Q4 (si baja a un modelo que no permite ampliado, forzar estandar):
       ```js
       const elegirModelo = (id) => {
         dispatch({ type: ACCIONES.SET_CAMPO, campo: 'modeloId', valor: id })
         if (!permiteBanoAmpliado(id) && estado.bano.tamano === 'ampliado') {
           dispatch({ type: ACCIONES.SET_CAMPO, campo: 'bano', valor: { ...estado.bano, tamano: 'estandar' } })
         }
       }
       ```
       Clonar bano con spread (Pitfall 3 — inmutabilidad: nunca mutar estado.bano).

    5. Reflejo en vivo (DIM-02): NO pasar props a FloorPlan. Al escribir modeloId el plano se
       redibuja vía el cableado existente (configDesdeEstado en ConfiguratorWizard). NO importar ni
       renderizar FloorPlan/PlanoPanel desde este componente.
  </action>
  <verify>
    <automated>node --check src/components/wizard/pasos/PasoDimensiones.jsx && grep -q "permiteBanoAmpliado" src/components/wizard/pasos/PasoDimensiones.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/components/wizard/pasos/PasoDimensiones.jsx` sin error de sintaxis
    - `grep -n "MODELOS.map" src/components/wizard/pasos/PasoDimensiones.jsx` 1 coincidencia (cards data-driven; sin lista hardcodeada)
    - `grep -cE "id: 'N[1-7]'|largo: [0-9]" src/components/wizard/pasos/PasoDimensiones.jsx` devuelve 0 (no se redefinen datos de modelo en el componente)
    - `grep -n "toFixed(2).replace('.', ',')" src/components/wizard/pasos/PasoDimensiones.jsx` 1 coincidencia (formato argentino)
    - `grep -n "camas a medida" src/components/wizard/pasos/PasoDimensiones.jsx` 1 coincidencia (camasBase null)
    - `grep -n "Sugerido" src/components/wizard/pasos/PasoDimensiones.jsx` >= 1 (badge Patrón E)
    - `grep -n "permiteBanoAmpliado(id)" src/components/wizard/pasos/PasoDimensiones.jsx` 1 coincidencia (corrección Q4)
    - `grep -n "...estado.bano" src/components/wizard/pasos/PasoDimensiones.jsx` 1 coincidencia (inmutabilidad)
    - `grep -c "FloorPlan\|PlanoPanel" src/components/wizard/pasos/PasoDimensiones.jsx` devuelve 0 (no se acopla al plano por props)
    - `grep -ci "elegí\|cambiá\|mirá" src/components/wizard/pasos/PasoDimensiones.jsx` devuelve 0 (sin tuteo; "Elija" con mayúscula y usted es válido)
    - Inspección visual a ~375px (`npm run dev`, Paso 2): 7 cards (N1-N7) con "X,XX m · N camas/camas a medida · ideal para N personas"; la card sugerida según ocupantes muestra el pill "Sugerido"; click en otra mueve el borde verde sin mover el badge; el plano se redibuja al cambiar modelo. (manual)
  </acceptance_criteria>
  <done>El Paso 2 renderiza las 7 cards de modelo con metadatos derivados de MODELOS, marca la sugerida con un badge independiente del seleccionado, y al elegir modelo escribe modeloId (redibuja el plano) corrigiendo bano.tamano a 'estandar' si el nuevo modelo no admite ampliado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Pasos 1 y 2 del wizard (componentes creados, aún no enchufados al registro — se enchufan en el Plan 03). Para verificar visualmente AHORA sin enchufe, el executor puede importar temporalmente PasoUso/PasoDimensiones en una vista de prueba, o esperar al Plan 03; el contrato visual a confirmar es el de estos dos componentes.</what-built>
  <how-to-verify>
    Con `npm run dev` y el navegador a ~375px (DevTools, dispositivo Samsung/iPhone SE ancho ~375):
    1. Paso 1 — Uso: ¿se ven las 5 cards (Contratista rural, Ganadero, Agrícola, Vivienda, Otro) cada una con un ícono? ¿Al clickear una queda seleccionada SOLA con borde/fondo verde? ¿Los íconos se ven nítidos (SVG line)?
    2. Paso 1 — Ocupantes: ¿están los 6 chips (2,3,4,5,6,8)? ¿Al elegir uno aparece el panel "Le sugerimos el modelo Nx para N personas. Lo puede cambiar en el paso siguiente."? ¿El trato es de usted (sin tuteo)?
    3. Paso 2 — Modelos: ¿se ven las 7 cards N1-N7 con "6,60 m · 4 camas · ideal para 4 personas" (coma decimal) y N5/N6/N7 con "camas a medida"? ¿La card sugerida tiene el pill "Sugerido"? ¿Al elegir otra card el borde verde se mueve pero el badge "Sugerido" se queda donde estaba?
    4. Reflejo del plano: al cambiar de modelo en el Paso 2, ¿el plano (abrir "Ver plano actual" en mobile o mirarlo en desktop) cambia el largo con transición suave?
    5. ¿Touch targets cómodos (≥44px) sin desbordes horizontales a 375px?
  </how-to-verify>
  <resume-signal>Escriba "aprobado" o describa qué ajustar (copys, íconos, layout, selección).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → app | `estado.modeloId`, `estado.ocupantes`, `estado.uso` se restauran desde `impacar_config_v1` y pueden estar adulterados. Los componentes los leen para resaltar selección. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | `PasoDimensiones` resaltando un `modeloId` adulterado | mitigate | El resaltado usa comparación `estado.modeloId === m.id` sobre la lista real MODELOS; un id inexistente simplemente no resalta ninguna card (no crashea). La derivación de largo/baño usa `permiteBanoAmpliado` (Plan 01) que ya tolera ids inválidos. |
| T-04-05 | Tampering | `SUGERENCIA_OCUPANTES[estado.ocupantes]` con ocupantes adulterado | mitigate | El acceso por clave devuelve `undefined` para una clave inexistente; el feedback y el badge se protegen con `&& SUGERENCIA_OCUPANTES[...]` antes de renderizar. No se dispara SET_CAMPO de modeloId si `sugerido` es falsy. |
| T-04-06 | Info Disclosure / XSS | Render de copys y metadatos | accept | Todos los textos son literales o vienen de `/data` controlado; React escapa por defecto; sin `dangerouslySetInnerHTML`. |
</threat_model>

<verification>
- `node --check` pasa para ambos componentes.
- Grep gates de anti-hardcodeo, anti-tuteo, data-driven (ver acceptance_criteria) verdes.
- Checkpoint humano aprobado a ~375px (selección única, sugerencia, badge independiente, reflejo del plano).
</verification>

<success_criteria>
- USO-01: 5 cards de uso con ícono, selección única.
- USO-02: chips de ocupantes 2/3/4/5/6/8.
- USO-03: al elegir ocupantes se pre-selecciona el modelo de SUGERENCIA_OCUPANTES y se muestra el feedback (usted).
- DIM-01: 7 cards de modelo con largo/camas/"ideal para N personas" y badge "Sugerido".
- DIM-02: al cambiar modelo el plano se redibuja (cableado existente, sin props nuevas).
</success_criteria>

<output>
Al terminar, crear `.planning/phases/04-pasos-1-3-uso-dimensiones-ba-o/04-02-SUMMARY.md`.
</output>
