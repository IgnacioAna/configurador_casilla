---
phase: 03-cascara-del-wizard-y-persistencia
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src/components/wizard/PlanoPanel.jsx
  - src/components/ConfiguratorWizard.jsx
autonomous: false
requirements: [PLANO-03]
must_haves:
  truths:
    - "En desktop el plano va sticky a la derecha del wizard (no se va de pantalla al desplazar los pasos)"
    - "En mobile el plano está colapsado tras un disparador Ver plano actual y se puede expandir"
    - "El plano se dibuja desde el estado del wizard vía configDesdeEstado (no desde un mock fijo)"
    - "El plano refleja el modelo inicial razonable y mantiene su forma config alineada con FloorPlan"
  artifacts:
    - path: "src/components/wizard/PlanoPanel.jsx"
      provides: "Contenedor responsive del plano: sticky en desktop, colapsable/modal en mobile, embebe FloorPlan"
      exports: ["default"]
      contains: "Ver plano actual"
      min_lines: 40
  key_links:
    - from: "src/components/wizard/PlanoPanel.jsx"
      to: "src/components/FloorPlan.jsx"
      via: "<FloorPlan config={config} /> con config derivada del estado"
      pattern: "<FloorPlan"
    - from: "src/components/ConfiguratorWizard.jsx"
      to: "src/components/wizard/PlanoPanel.jsx"
      via: "configDesdeEstado(estado) → <PlanoPanel config={config} />"
      pattern: "configDesdeEstado\\(estado\\)"
    - from: "src/components/wizard/PlanoPanel.jsx"
      to: "estado de colapso mobile"
      via: "useState para abrir/cerrar el plano en mobile"
      pattern: "useState\\("
---

<objective>
Integrar el plano (FloorPlan, Fase 2) dentro del wizard con el layout responsive de PLANO-03:
en desktop el plano va sticky a la derecha; en mobile va colapsado tras un disparador
"Ver plano actual" (card colapsable y/o modal a pantalla completa). El plano se dibuja derivando
la forma `config` del estado del wizard vía configDesdeEstado (Plan 01) — no desde un mock fijo.

En esta fase los pasos son stubs y todavía no llenan la config, así que el plano muestra el modelo
inicial razonable (N4 por defecto, definido en estadoInicial del Plan 01). Cuando las Fases 4-5
llenen los pasos, el plano ya estará reaccionando al estado.

Propósito: cerrar el último success criterion de la fase (plano colapsable mobile / sticky desktop)
reutilizando el comportamiento del UI-SPEC de Fase 2, sin reimplementar el dibujo.
Output: PlanoPanel.jsx (contenedor responsive) + integración en ConfiguratorWizard.jsx.

Cubre PLANO-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-cascara-del-wizard-y-persistencia/03-01-SUMMARY.md
@.planning/phases/03-cascara-del-wizard-y-persistencia/03-02-SUMMARY.md
@.planning/phases/02-motor-de-plano-svg/02-UI-SPEC.md

<interfaces>
<!-- Contrato de FloorPlan (Fase 2) y de configDesdeEstado (Plan 01). Usar tal cual. -->

src/components/FloorPlan.jsx:
  export default function FloorPlan({ config })
  // config: { modeloId, largo, bano:{tamano}, dormitorio:{camas}, cocina:{horno,heladera}, estar:{mesa,rebatible} }
  // Renderiza estado Empty si config es nula; Error si la geometría es inválida; escala con width="100%".

src/state/wizardReducer.js:
  configDesdeEstado(estado) => objeto config para FloorPlan  // deriva el largo desde MODELOS

Leyenda de zonas (ya estaba en App.jsx en Fase 2, src/App.jsx ZONAS_LEYENDA) — reutilizar el
mapeo color→zona si se quiere mostrar la leyenda junto al plano:
  Baño #BFE3EE · Dormitorio #C9A66B · Cocina #A7C796 · Estar/comedor #ECECE4 · Baulera #ECECE4
  + "C cucheta · S simple · M matrimonial"

UI-SPEC Fase 2 (02-UI-SPEC.md) — PLANO-03 está reservado allí:
  - "Touch target mínimo (preparación PLANO-03): cualquier control HTML sobre el plano (botón
     colapsar/zoom) debe ser ≥44px."
  - "El plano se diseña para embeberse en ambos layouts (sticky desktop / colapsable mobile)."
  - El <svg> ya escala con width="100%" y preserveAspectRatio — el contenedor define el ancho.

Breakpoints Tailwind: usar `lg:` (≥1024px) como umbral desktop. Mobile-first = base sin prefijo.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PlanoPanel responsive (sticky desktop / colapsable mobile) embebiendo FloorPlan</name>
  <files>src/components/wizard/PlanoPanel.jsx</files>
  <read_first>
    - src/components/FloorPlan.jsx líneas 95-135 (firma { config }, estados, width="100%")
    - .planning/phases/02-motor-de-plano-svg/02-UI-SPEC.md sección "Spacing Scale" (padding md=16px del wrapper) y la nota de touch target ≥44px para PLANO-03
    - src/App.jsx (Fase 2) — patrón del contenedor del plano (rounded border, p-4) y la leyenda ZONAS_LEYENDA, para reutilizar el estilo
  </read_first>
  <action>
    Crear `src/components/wizard/PlanoPanel.jsx`:
    - Recibe prop `config` (objeto config para FloorPlan; lo pasa el wizard vía configDesdeEstado).
    - Estado local `const [abiertoMobile, setAbiertoMobile] = useState(false)`.
    - DESKTOP (clases `lg:`): renderiza un contenedor con `lg:sticky lg:top-4` (o top adecuado) que
      mantiene el plano visible al desplazar los pasos. Dentro: el título "Plano actual" (sobrio,
      trato de usted), el wrapper del plano (rounded border border-impacar-texto/10 bg-impacar-fondo
      p-4, como en Fase 2) con `<FloorPlan config={config} />`, y opcionalmente la leyenda de zonas.
      En desktop el plano está SIEMPRE visible (no colapsable).
    - MOBILE (base, oculto en `lg:` con `lg:hidden` para el disparador): un botón "Ver plano actual"
      (min-h-[44px], touch target ≥44px del UI-SPEC; ancho cómodo, focus:ring) que alterna
      `abiertoMobile`. Cuando `abiertoMobile` es true, mostrar el plano. Elegir UNA de estas dos
      presentaciones (ambas válidas por PLANO-03) y documentarla en el SUMMARY:
        (a) Card colapsable: el FloorPlan se despliega inline debajo del botón (texto del botón
            alterna "Ver plano actual" / "Ocultar plano"); o
        (b) Modal/overlay a pantalla completa: un overlay fixed inset-0 con el plano y un botón
            "Cerrar" (≥44px), con aria-modal y cierre por Escape.
      Recomendación: card colapsable inline (más simple, sin foco atrapado); si se elige modal,
      manejar foco y Escape. Usar `aria-expanded` en el botón disparador.
    - Estructura para que el MISMO componente cubra ambos modos: el bloque sticky desktop con
      clases `hidden lg:block` y el bloque colapsable mobile con `lg:hidden`. Así no se duplica el
      FloorPlan visualmente en ambos breakpoints a la vez.
    - Todo el copy en trato de usted: "Ver plano actual", "Ocultar plano" / "Cerrar", "Plano actual".
    - Si config es nula, FloorPlan ya muestra su estado Empty (no hace falta lógica extra). Export default.
  </action>
  <verify>
    <automated>cd "C:/Users/Usuario/OneDrive/Desktop/Proyectos/Impacar" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/components/wizard/PlanoPanel.jsx`
    - `grep "<FloorPlan" src/components/wizard/PlanoPanel.jsx` presente (embebe el plano de Fase 2)
    - `grep "Ver plano actual" src/components/wizard/PlanoPanel.jsx` presente (disparador mobile en usted)
    - `grep -E "lg:sticky|lg:block" src/components/wizard/PlanoPanel.jsx` presente (sticky/visible en desktop)
    - `grep "lg:hidden" src/components/wizard/PlanoPanel.jsx` presente (disparador solo en mobile)
    - `grep -E "useState\(" src/components/wizard/PlanoPanel.jsx` presente (estado de colapso mobile)
    - `grep -E "aria-expanded|aria-modal" src/components/wizard/PlanoPanel.jsx` presente (accesibilidad del colapso)
    - `grep "min-h-\[44px\]" src/components/wizard/PlanoPanel.jsx` presente (touch target del UI-SPEC)
    - `npm run build` termina sin errores
  </acceptance_criteria>
  <done>PlanoPanel muestra el plano sticky en desktop y colapsable tras "Ver plano actual" en mobile,
  embebiendo FloorPlan, con touch target ≥44px y aria del colapso. `npm run build` pasa.</done>
</task>

<task type="auto">
  <name>Task 2: Integrar PlanoPanel en el wizard derivando config del estado</name>
  <files>src/components/ConfiguratorWizard.jsx</files>
  <read_first>
    - src/components/ConfiguratorWizard.jsx (Plan 02 — el comentario reservado `{/* PLANO: ... */}` marca dónde integrar)
    - src/components/wizard/PlanoPanel.jsx (Task 1 — prop config)
    - .planning/phases/03-cascara-del-wizard-y-persistencia/03-01-SUMMARY.md (configDesdeEstado)
  </read_first>
  <action>
    Editar `src/components/ConfiguratorWizard.jsx`:
    - Importar PlanoPanel de './wizard/PlanoPanel.jsx' y configDesdeEstado de '../state/wizardReducer.js'.
    - Calcular `const configPlano = configDesdeEstado(estado)` dentro del componente.
    - Reemplazar el comentario reservado `{/* PLANO: integrado en Plan 03 (PLANO-03) */}` (o la zona
      equivalente) por `<PlanoPanel config={configPlano} />`.
    - Ajustar el layout del wizard a dos columnas en desktop: contenedor con
      `lg:grid lg:grid-cols-[1fr_minmax(320px,420px)] lg:gap-8` (o flex equivalente) donde la columna
      izquierda son los pasos + navegación y la derecha el PlanoPanel (que internamente se hace
      sticky). En mobile (base) una sola columna: los pasos arriba y el disparador "Ver plano actual"
      del PlanoPanel accesible dentro del flujo (típicamente arriba de los pasos o tras la barra de
      progreso, según se vea mejor a 375px). Mantener mobile-first.
    - No romper la navegación, la barra de progreso ni "Volver a empezar" del Plan 02.
  </action>
  <verify>
    <automated>cd "C:/Users/Usuario/OneDrive/Desktop/Proyectos/Impacar" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "configDesdeEstado(estado)" src/components/ConfiguratorWizard.jsx` presente (config derivada del estado)
    - `grep "<PlanoPanel" src/components/ConfiguratorWizard.jsx` presente (integrado, ya no es comentario)
    - `grep -c "PLANO: integrado en Plan 03" src/components/ConfiguratorWizard.jsx` devuelve 0 (el placeholder fue reemplazado)
    - `grep -E "lg:grid|lg:flex" src/components/ConfiguratorWizard.jsx` presente (layout de dos columnas en desktop)
    - `npm run build` termina sin errores
  </acceptance_criteria>
  <done>El wizard pasa configDesdeEstado(estado) a PlanoPanel; en desktop hay dos columnas con el
  plano sticky a la derecha, en mobile el plano queda colapsable. La navegación del Plan 02 sigue
  funcionando. `npm run build` pasa.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    El plano (FloorPlan de Fase 2) integrado en el wizard: sticky a la derecha en desktop y
    colapsable ("Ver plano actual") en mobile, dibujándose desde el estado del wizard. Claude ya
    verificó que `npm run build` pasa.
  </what-built>
  <how-to-verify>
    1. `npm run dev`, abrir http://localhost:5173, tocar "Comenzar" para entrar al wizard.
    2. DESKTOP (ventana ancha, ≥1024px): el plano aparece a la DERECHA de los pasos. Desplazar la
       página / cambiar de paso: el plano permanece visible (sticky), no se va de pantalla.
    3. MOBILE (DevTools responsive ~375px): el plano NO se muestra expandido por defecto; hay un
       botón "Ver plano actual". Tocarlo → se muestra el plano (card inline o modal a pantalla
       completa, según lo implementado). Volver a tocar "Ocultar plano" / "Cerrar" → se colapsa.
    4. Confirmar que el plano dibuja una casilla real (modelo inicial N4: croquis con zonas de
       color, cotas), no un estado vacío.
    5. Verificar que no haya desbordes ni el plano tapando los controles en mobile.
  </how-to-verify>
  <resume-signal>Escriba "aprobado" si el plano es sticky en desktop y colapsable en mobile, o describa los problemas.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| estado del wizard → FloorPlan (render) | configDesdeEstado proyecta el estado (potencialmente restaurado de localStorage) a la forma config que el plano dibuja. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | config derivada de un estado restaurado/adulterado en localStorage | mitigate | FloorPlan ya valida internamente: si la geometría es inválida muestra el estado Error y si config es nula muestra Empty (UI-SPEC Fase 2). Además el lazy initializer del Plan 01 descarta estado corrupto a estadoInicial, por lo que configDesdeEstado recibe un modeloId válido. |
| T-03-08 | Denial of Service | render del plano en mobile (overlay/modal) atrapando el foco o bloqueando scroll | mitigate | Si se elige modal: cierre por Escape + botón Cerrar ≥44px + restaurar scroll al cerrar. Si se elige card colapsable inline (recomendado): no atrapa foco ni bloquea scroll, evitando el riesgo de raíz. |
</threat_model>

<verification>
- `npm run build` pasa.
- PlanoPanel embebe <FloorPlan> y expone el disparador "Ver plano actual" en mobile.
- El wizard pasa configDesdeEstado(estado) a PlanoPanel (plano desde el estado, no mock fijo).
- El placeholder de comentario PLANO fue reemplazado por el componente real.
- Checkpoint humano confirma sticky en desktop y colapsable en mobile, plano dibujando el modelo real.
</verification>

<success_criteria>
- PLANO-03: en mobile el plano es colapsable ("Ver plano actual"); en desktop va sticky a la derecha.
- El plano se dibuja desde el estado del wizard (configDesdeEstado), manteniendo la forma config de FloorPlan.
- `npm run build` pasa.
</success_criteria>

<output>
Al completar, crear `.planning/phases/03-cascara-del-wizard-y-persistencia/03-03-SUMMARY.md`
documentando: qué presentación mobile se eligió (card colapsable vs modal), cómo se hizo sticky el
plano en desktop (clases), y que el plano ya reacciona al estado vía configDesdeEstado (listo para
que Fases 4-5 lo llenen). Cerrar la fase resumiendo los 5 success criteria cumplidos.
</output>
