---
phase: 05-pasos-4-6-y-motores-dormitorio-cocina-extras-precios
plan: 04
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/components/wizard/BarraPrecio.jsx
autonomous: true
requirements: [PRECIO-01]

must_haves:
  truths:
    - "El usuario ve el presupuesto en vivo: neto + IVA 21% + total c/IVA, en formato argentino, derivado del estado."
    - "La barra es presentacional pura: recibe { estado }, calcula con calcularPresupuesto + formatPrecio, no conoce navegación ni plano."
  artifacts:
    - path: "src/components/wizard/BarraPrecio.jsx"
      provides: "Barra/chip de presupuesto (3 líneas neto/IVA/total) — PRECIO-01"
      min_lines: 25
  key_links:
    - from: "src/components/wizard/BarraPrecio.jsx"
      to: "src/utils/motorPrecios.js"
      via: "calcularPresupuesto(estado)"
      pattern: "calcularPresupuesto"
    - from: "src/components/wizard/BarraPrecio.jsx"
      to: "src/utils/formato.js"
      via: "formatPrecio de neto/iva/total"
      pattern: "formatPrecio"
---

<objective>
Crear `BarraPrecio.jsx`, el componente presentacional puro del presupuesto en vivo (PRECIO-01).
Recibe `{ estado }`, llama a `calcularPresupuesto(estado)` (Plan 05-01) y muestra 3 líneas
(Neto / IVA 21% / Total c/IVA) con `formatPrecio` en formato argentino. NO decide su propia
visibilidad ni posición sticky — eso lo controla el contenedor (Plan 05-05, montaje condicional
`pasoActual >= 3`). La barra solo presenta.

Purpose: La superficie de precio de la fase. Aislarla como componente puro (recibe estado, no
navegación) la hace reusable en el Resumen (Fase 6) y testeable por composición.
Output: 1 archivo .jsx nuevo en src/components/wizard/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pasos-4-6-y-motores-dormitorio-cocina-extras-precios/05-UI-SPEC.md

<interfaces>
<!-- Contratos exactos. La barra recibe SOLO { estado }. -->

De src/utils/motorPrecios.js (Plan 05-01):
```js
export function calcularPresupuesto(estado) // → { neto: number, iva: number, total: number }
```

De src/utils/formato.js:
```js
export function formatPrecio(n) // 38971845 -> "$38.971.845"
```

Copywriting EXACTO (UI-SPEC "Barra de precio — labels", D-11):
```
Caption (encabezado): "Presupuesto estimado"
Línea 1: "Neto"        + valor    (text-sm regular)
Línea 2: "IVA 21%"     + valor    (text-sm text-impacar-texto/70)
Línea 3: "Total c/IVA" + valor    (text-sm font-semibold text-impacar-campo)  ← mayor jerarquía
```

Tokens de la card (UI-SPEC "Barra de precio sticky", variante desktop/bloque):
```
contenedor: 'rounded border border-impacar-texto/10 bg-impacar-fondo p-4'
valores alineados a la derecha (flex justify-between por línea)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: BarraPrecio — 3 líneas neto/IVA/total desde calcularPresupuesto (PRECIO-01)</name>
  <read_first>
    - src/utils/motorPrecios.js (calcularPresupuesto — Plan 05-01)
    - src/utils/formato.js (formatPrecio)
    - src/components/wizard/PlanoPanel.jsx (lenguaje de card: rounded border border-impacar-texto/10 bg-impacar-fondo p-4)
    - .planning/phases/05-.../05-UI-SPEC.md § "Component Inventory 5. Barra de precio sticky" + "Copywriting Contract" (labels EXACTOS, D-11)
  </read_first>
  <action>
Crear `src/components/wizard/BarraPrecio.jsx` (presentacional puro, recibe `{ estado }`, imports
`../../`). Estructura:

```jsx
import { calcularPresupuesto } from '../../utils/motorPrecios.js'
import { formatPrecio } from '../../utils/formato.js'

// Barra/chip de presupuesto en vivo (PRECIO-01, D-11). Presentacional puro: recibe { estado },
// calcula y formatea. NO decide visibilidad (eso lo hace ConfiguratorWizard, pasoActual>=3) ni
// conoce el plano/navegación. Las 3 líneas: Neto / IVA 21% / Total c/IVA en formato argentino.
export default function BarraPrecio({ estado }) {
  const { neto, iva, total } = calcularPresupuesto(estado)
  return (
    <div className="rounded border border-impacar-texto/10 bg-impacar-fondo p-4">
      <p className="text-xs text-impacar-texto/70">Presupuesto estimado</p>
      <dl className="mt-2 space-y-1">
        <div className="flex items-center justify-between">
          <dt className="text-sm">Neto</dt>
          <dd className="text-sm">{formatPrecio(neto)}</dd>
        </div>
        <div className="flex items-center justify-between">
          <dt className="text-sm text-impacar-texto/70">IVA 21%</dt>
          <dd className="text-sm text-impacar-texto/70">{formatPrecio(iva)}</dd>
        </div>
        <div className="flex items-center justify-between">
          <dt className="text-sm font-semibold text-impacar-campo">Total c/IVA</dt>
          <dd className="text-sm font-semibold text-impacar-campo">{formatPrecio(total)}</dd>
        </div>
      </dl>
    </div>
  )
}
```

NOTAS:
- NO incluir desglose ítem por ítem (D-11: eso es Fase 6, Resumen). Solo las 3 líneas.
- NO importar el plano, ni leer `estado.pasoActual` (no decide visibilidad).
- Reusar `calcularPresupuesto` (Plan 05-01) — NO sumar precios a mano aquí.
- La barra siempre muestra al menos el precio del modelo (con extras vacío, neto = precioNeto del
  modelo; nunca $0 si hay modelo válido) — garantizado por el motor.
- El styling sticky/posición lo aplica el contenedor (Plan 05-05) envolviendo este componente; la
  barra es agnóstica a su posición. NO agregar `sticky`/`fixed` aquí.
  </action>
  <verify>
    <automated>npx esbuild src/components/wizard/BarraPrecio.jsx --bundle --format=esm --external:react --external:../* --outfile=/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - grep: BarraPrecio.jsx contiene `function BarraPrecio({ estado })`
    - grep: BarraPrecio.jsx contiene `calcularPresupuesto` y `formatPrecio` (reusa el motor + formato; no recalcula)
    - grep: BarraPrecio.jsx contiene las cadenas exactas `Presupuesto estimado`, `Neto`, `IVA 21%`, `Total c/IVA`
    - grep: BarraPrecio.jsx contiene `text-impacar-campo` aplicado a la línea Total (mayor jerarquía)
    - grep: BarraPrecio.jsx NO contiene `pasoActual` (no decide visibilidad)
    - grep: BarraPrecio.jsx NO contiene `1.21` ni `0.21` ni `precioNeto` (no recalcula precio/IVA a mano)
    - grep: BarraPrecio.jsx NO contiene `FloorPlan` ni `sticky` ni `fixed` (agnóstico a posición)
    - El componente compila como JSX válido (esbuild sin errores)
  </acceptance_criteria>
  <done>BarraPrecio muestra Neto / IVA 21% / Total c/IVA con formatPrecio desde calcularPresupuesto; presentacional puro, sin lógica de visibilidad ni acoplamiento al plano.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → BarraPrecio | El `estado` (con `modeloId`/`extras` posiblemente adulterados) entra a `calcularPresupuesto`. Única frontera (client-side puro). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Tampering | `BarraPrecio({ estado })` | mitigate | La robustez vive en `calcularPresupuesto` (Plan 05-01): modeloId inválido → neto 0, extras no-array → ignorado. La barra solo formatea el resultado; `formatPrecio` tolera `NaN`/null (`Number(n) || 0`). Nunca crashea ni muestra `$NaN`. |
| T-05-12 | DoS / Info / Repudiation / Elevation | — | accept | No aplica: componente presentacional sin red/servidor/sesión; precios de Lista 108 son catálogo público. |
</threat_model>

<verification>
- `npm run build` OK (BarraPrecio compila e importa el motor limpio).
- Reuso verificado: usa calcularPresupuesto + formatPrecio (no recalcula IVA ni suma a mano).
- Gate anti-voseo: copy en trato de usted ("Presupuesto estimado") — sin verbos en voseo.
</verification>

<success_criteria>
- BarraPrecio muestra 3 líneas (Neto / IVA 21% / Total c/IVA) en formato argentino vía formatPrecio.
- Presentacional puro: recibe { estado }, reusa calcularPresupuesto; no conoce navegación/plano/posición.
- Tolera estado adulterado (delegado al motor); nunca $NaN.
</success_criteria>

<output>
After completion, create `.planning/phases/05-pasos-4-6-y-motores-dormitorio-cocina-extras-precios/05-04-SUMMARY.md`
</output>
</content>
</invoke>
