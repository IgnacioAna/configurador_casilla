---
phase: 05-pasos-4-6-y-motores-dormitorio-cocina-extras-precios
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/validacionCamas.js
  - src/utils/validacionCamas.test.js
  - src/utils/motorPrecios.js
  - src/utils/motorPrecios.test.js
  - src/data/extras.js
  - src/data/extras.test.js
  - package.json
autonomous: true
requirements: [DORM-02, PRECIO-01, EXTRAS-01]
user_setup: []

must_haves:
  truths:
    - "El sistema sabe si una combinación de camas entra en la capacidad real del modelo (N4 admite 4 footprints; matrimonial cuenta 2; N5+ siempre entran)."
    - "El sistema calcula el presupuesto (modelo + extras seleccionados) como neto + IVA 21% + total, desde una sola fuente (extras[])."
    - "Los 9 extras de confort/energía están etiquetados con un subgrupo data-driven (7 confort + 2 energía)."
    - "Los tests nuevos corren con `npm test` (no quedan huérfanos)."
  artifacts:
    - path: "src/utils/validacionCamas.js"
      provides: "capacidadFootprints, footprintsDeCamas, camasEntran (DORM-02)"
      exports: ["capacidadFootprints", "footprintsDeCamas", "camasEntran"]
    - path: "src/utils/motorPrecios.js"
      provides: "calcularPresupuesto(estado) → {neto, iva, total} (PRECIO-01)"
      exports: ["calcularPresupuesto"]
    - path: "src/data/extras.js"
      provides: "EXTRAS con metadato subgrupo en los 9 'extras'"
      contains: "subgrupo"
    - path: "package.json"
      provides: "script test que incluye los 3 archivos nuevos"
      contains: "validacionCamas.test.js"
  key_links:
    - from: "src/utils/motorPrecios.js"
      to: "src/utils/formato.js"
      via: "import calcularIVA/calcularTotal"
      pattern: "from './formato.js'"
    - from: "src/utils/validacionCamas.js"
      to: "src/data/models.js"
      via: "import MODELOS (camasBase/personalizable)"
      pattern: "from '../data/models.js'"
    - from: "package.json"
      to: "src/utils/motorPrecios.test.js"
      via: "script test enumera el archivo"
      pattern: "motorPrecios.test.js"
---

<objective>
Construir los DOS motores de lógica pura de la fase (validación de capacidad de camas y motor de
precios) y el metadato `subgrupo` del catálogo de extras, todo testeado con `node:test` y
enchufado al script `test` de `package.json`. Interface-first: estos motores son consumidos por
los componentes de la Wave 2 (PasoDormitorio, PasoCocina, PasoExtras, BarraPrecio), así que se
construyen y testean ANTES de cualquier UI.

Purpose: Aislar la lógica de dominio (capacidad real desde `camasBase`, suma de precios desde una
sola fuente `extras[]`, split confort/energía data-driven) en funciones puras verificables, sin
DOM, como en Fase 4. Resuelve los Pitfalls 1 (capacidad incoherente), 3 (doble fuente de precio)
y 5 (tests huérfanos).
Output: 3 helpers/data nuevos + 3 archivos de test + script `test` actualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pasos-4-6-y-motores-dormitorio-cocina-extras-precios/05-RESEARCH.md
@.planning/phases/05-pasos-4-6-y-motores-dormitorio-cocina-extras-precios/05-VALIDATION.md

<interfaces>
<!-- Contratos exactos extraídos del codebase. El executor usa estos directamente — sin explorar. -->

De src/data/models.js (MODELOS — fuente de verdad de la capacidad, Pitfall 1):
```js
export const MODELOS = [
  { id: 'N1', nombre: 'N1', largo: 4.5, camasBase: 2, precioNeto: 29108976, ocupantesIdeal: 2, personalizable: false },
  { id: 'N2', nombre: 'N2', largo: 5.2, camasBase: 2, precioNeto: 34150926, ocupantesIdeal: 3, personalizable: false },
  { id: 'N3', nombre: 'N3', largo: 6.1, camasBase: 3, precioNeto: 37181710, ocupantesIdeal: 4, personalizable: false },
  { id: 'N4', nombre: 'N4', largo: 6.6, camasBase: 4, precioNeto: 38971845, ocupantesIdeal: 4, personalizable: false },
  { id: 'N5', nombre: 'N5', largo: 7.6, camasBase: null, precioNeto: 40937114, ocupantesIdeal: 6, personalizable: true },
  { id: 'N6', nombre: 'N6', largo: 8.6, camasBase: null, precioNeto: 42048149, ocupantesIdeal: 6, personalizable: true },
  { id: 'N7', nombre: 'N7', largo: 9.6, camasBase: null, precioNeto: 46180786, ocupantesIdeal: 8, personalizable: true },
]
```

De src/utils/formato.js (REUSAR — NO recrear formato/IVA):
```js
export function formatPrecio(n)   // 29108976 -> "$29.108.976"
export function calcularIVA(neto) // neto * 0.21
export function calcularTotal(neto) // neto * 1.21
```

De src/data/extras.js (las 17 entradas; los 9 de categoria 'extras' reciben subgrupo):
```js
// categoria 'extras' (9): split canónico (PROJECT.md "Paso sugerido")
//   ENERGÍA (2): panel-solar-160, sistema-solar-220
//   CONFORT (7): calefactor-4000, caldera-12v, split-3200, tv-32-directv,
//                estereo-pioneer, cortinas-blackout, toldo
```

Patrón de test existente (src/data/models.test.js — clonar estructura node:test):
```js
import { test } from 'node:test'
import assert from 'node:assert/strict'
```

Script test actual de package.json (línea 10 — lista FIJA, sin glob; Pitfall 5):
```
"test": "node --test src/utils/banoReglas.test.js src/utils/floorplanLayout.test.js src/utils/formato.test.js src/data/models.test.js src/state/wizardReducer.test.js src/hooks/usePersistedConfig.test.js"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Motor de validación de capacidad de camas (DORM-02)</name>
  <read_first>
    - src/data/models.js (MODELOS: camasBase, personalizable — fuente de verdad de la capacidad)
    - src/utils/banoReglas.js (patrón de helper puro de Fase 4: import de MODELOS + MODELOS.find + tolerancia a id adulterado)
    - src/data/models.test.js (estructura de test node:test a clonar)
    - .planning/phases/05-.../05-RESEARCH.md § "Pitfall 1" y "Validación de capacidad de camas (puro, DORM-02)" (la fórmula recomendada)
  </read_first>
  <behavior>
    - capacidadFootprints('N4') === 4 (camasBase real del catálogo, NO la zona fraccionada del plano)
    - capacidadFootprints('N1') === 2 ; capacidadFootprints('N3') === 3
    - capacidadFootprints('N5') === Infinity ; capacidadFootprints('N6') === Infinity ; capacidadFootprints('N7') === Infinity (personalizable=true)
    - capacidadFootprints('XX') === 0 ; capacidadFootprints(undefined) === 0 (id adulterado → no entra nada, sin crashear)
    - footprintsDeCamas([{tipo:'S'},{tipo:'S'}]) === 2 ; footprintsDeCamas([{tipo:'M'}]) === 2 (matrimonial = 2 footprints)
    - footprintsDeCamas([{tipo:'C'},{tipo:'M'}]) === 3 (C=1 + M=2)
    - footprintsDeCamas(null) === 0 ; footprintsDeCamas('texto') === 0 ; footprintsDeCamas([{}]) tolera elementos sin tipo (cuenta 1)
    - camasEntran('N4', [4 simples]) === true ; camasEntran('N4', [5 simples]) === false (límite exacto)
    - camasEntran('N4', [{tipo:'M'},{tipo:'M'}]) === false (2 matrimoniales = 4 footprints entra, pero ojo: 2 M = 4 footprints === 4 → true; verificar) — testear el límite con M=2 footprints
    - camasEntran('N7', [20 simples]) === true (personalizable → Infinity, siempre entra)
  </behavior>
  <action>
Crear `src/utils/validacionCamas.js` con TRES exports puros (sin React, sin DOM), importando
`MODELOS` de `'../data/models.js'`:

```js
import { MODELOS } from '../data/models.js'

// Capacidad de footprints del modelo. FUENTE DE VERDAD = camasBase del catálogo
// (NO la zona fraccionada del plano: ver Pitfall 1). N5+ personalizable → sin tope.
export function capacidadFootprints(modeloId) {
  const m = MODELOS.find((x) => x.id === modeloId)
  if (!m) return 0                       // id adulterado/inexistente → no entra nada (seguro)
  if (m.personalizable) return Infinity  // N5/N6/N7 "a medida": sin tope
  return m.camasBase ?? 0
}

// Footprints que ocupa una combinación. Matrimonial (M) = 2; cucheta (C) / simple (S) = 1.
export function footprintsDeCamas(camas) {
  const lista = Array.isArray(camas) ? camas : []
  return lista.reduce((n, c) => n + (c?.tipo === 'M' ? 2 : 1), 0)
}

// ¿Entra la combinación en el modelo? (DORM-02). Usado por la advertencia sin bloqueo (D-03).
export function camasEntran(modeloId, camas) {
  return footprintsDeCamas(camas) <= capacidadFootprints(modeloId)
}
```

Crear `src/utils/validacionCamas.test.js` (node:test + node:assert/strict, clonando la estructura
de `src/data/models.test.js`) cubriendo TODO el `<behavior>`. Importar de `'./validacionCamas.js'`.
Incluir explícitamente: N4→4, N1→2, N3→3, N5/N6/N7→Infinity, id adulterado→0, M=2 footprints,
no-array→0, límite 4-entra/5-no-entra en N4, y N7 con 20 simples → true.

NO importar `GEOMETRIA` ni recomputar la zona del plano: la capacidad sale de `camasBase`
(documentar en un comentario que `camasBase` YA codifica la geometría real 2 footprints/fila).
  </action>
  <verify>
    <automated>node --test src/utils/validacionCamas.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test src/utils/validacionCamas.test.js` exits 0 (todos los tests pasan)
    - grep: src/utils/validacionCamas.js contiene `export function capacidadFootprints`, `export function footprintsDeCamas`, `export function camasEntran`
    - grep: src/utils/validacionCamas.js contiene `personalizable` y `camasBase` (capacidad data-driven)
    - grep: src/utils/validacionCamas.js NO contiene `0.45` ni `largoDormitorio` ni `GEOMETRIA` (no recomputa la zona del plano — Pitfall 1)
    - grep: src/utils/validacionCamas.test.js contiene `Infinity` y `38971845`-no-aplica; sí contiene aserciones para 'N4', 'N5', 'N7'
  </acceptance_criteria>
  <done>validacionCamas.js exporta los 3 helpers; el test cubre capacidad data-driven, M=2 footprints, N5+ Infinity, id adulterado y límite N4; `node --test` verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Motor de precios — presupuesto en vivo (PRECIO-01)</name>
  <read_first>
    - src/utils/formato.js (calcularIVA, calcularTotal — REUSAR, NO recrear el 0.21)
    - src/data/models.js (MODELOS.precioNeto; N4 = 38971845)
    - src/data/extras.js (EXTRAS con id/precioNeto — la suma se filtra por ids del estado)
    - .planning/phases/05-.../05-RESEARCH.md § "Motor de precios (puro, PRECIO-01)" (código de referencia)
    - .planning/phases/05-.../05-VALIDATION.md § "Per-Task Verification Map" (valores de aserción VERIFIED)
  </read_first>
  <behavior>
    - calcularPresupuesto({modeloId:'N4', extras:[]}) → { neto: 38971845, iva: 38971845*0.21, total: 38971845*1.21 }
    - calcularPresupuesto({modeloId:'N4', extras:[<los 17 ids reales>]}) → neto 55977868 ; Math.round(total) === 67733220
    - una sola fuente: calcularPresupuesto({modeloId:'N4', extras:[], cocina:{horno:true}}) → neto 38971845 (cocina.horno NO suma; solo extras[] suma)
    - agregar 'cocina-horno' al array extras sube el neto exactamente 585800 (precioNeto del horno)
    - estado adulterado: calcularPresupuesto({modeloId:'XX', extras:[]}) → neto 0 (modelo inexistente, no crashea)
    - calcularPresupuesto({modeloId:'N1', extras:'no-array'}) → neto 29108976 (extras no-array → se ignora, suma 0; nunca crashea)
    - calcularPresupuesto(null) → { neto: 0, ... } (no crashea con estado nulo)
    - ids inexistentes en extras[] se ignoran (no rompen la suma)
  </behavior>
  <action>
Crear `src/utils/motorPrecios.js` con un export puro, REUSANDO `formato.js` (NO multiplicar 1.21 a
mano — el 0.21 vive solo en data/geometry.js vía formato.js):

```js
import { MODELOS } from '../data/models.js'
import { EXTRAS } from '../data/extras.js'
import { calcularIVA, calcularTotal } from './formato.js'

// Neto = precioNeto del modelo + suma de precioNeto de los extras seleccionados (UNA sola fuente:
// estado.extras[]). NUNCA mirar estado.cocina / estado.estar (D-13/D-14, Pitfall 3).
export function calcularPresupuesto(estado) {
  const modelo = MODELOS.find((m) => m.id === estado?.modeloId)
  const base = modelo?.precioNeto ?? 0
  const ids = Array.isArray(estado?.extras) ? estado.extras : []
  const sumaExtras = EXTRAS
    .filter((e) => ids.includes(e.id))
    .reduce((acc, e) => acc + e.precioNeto, 0)
  const neto = base + sumaExtras
  return { neto, iva: calcularIVA(neto), total: calcularTotal(neto) }
}
```

Crear `src/utils/motorPrecios.test.js` (node:test) cubriendo TODO el `<behavior>`. Para el test
de "los 17 extras", construir el array de ids a partir de `EXTRAS.map(e => e.id)` (NO hardcodear
17 strings) y afirmar `neto === 55977868` y `Math.round(total) === 67733220`. Afirmar también:
N4 solo → neto 38971845; cocina.horno NO suma; estado adulterado (modeloId 'XX' → 0, extras
no-array → solo modelo, estado null → 0). Importar de `'./motorPrecios.js'` y `'./extras.js'`/
`'../data/extras.js'` según corresponda.
  </action>
  <verify>
    <automated>node --test src/utils/motorPrecios.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test src/utils/motorPrecios.test.js` exits 0
    - grep: src/utils/motorPrecios.js contiene `export function calcularPresupuesto`
    - grep: src/utils/motorPrecios.js contiene `from './formato.js'` y `calcularIVA` y `calcularTotal` (reusa formato, no recrea IVA)
    - grep: src/utils/motorPrecios.js NO contiene `1.21` ni `0.21` ni `* IVA` literal (el IVA solo vive en formato.js/geometry.js)
    - grep: src/utils/motorPrecios.js NO contiene `estado.cocina` ni `estado.estar` (una sola fuente: extras[])
    - grep: src/utils/motorPrecios.test.js contiene `38971845`, `55977868`, `67733220`
  </acceptance_criteria>
  <done>motorPrecios.js exporta calcularPresupuesto reusando formato.js; el test verifica N4 solo (38.971.845), N4+17 extras (55.977.868 / total 67.733.220), una sola fuente y estado adulterado; `node --test` verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Metadato subgrupo en extras + test + script test de package.json (D-08, Pitfall 5)</name>
  <read_first>
    - src/data/extras.js (las 17 entradas; los 9 de categoria 'extras' reciben subgrupo)
    - src/data/models.test.js (estructura de test de datos node:test a clonar)
    - package.json (línea 10: script `test` con lista FIJA de archivos — Pitfall 5)
    - .planning/phases/05-.../05-RESEARCH.md § "Pattern 3: Split confort/energía data-driven" (split canónico)
  </read_first>
  <behavior>
    - cada extra con categoria:'extras' tiene un campo subgrupo cuyo valor ∈ {'confort','energia'}
    - exactamente 7 extras con subgrupo:'confort' y 2 con subgrupo:'energia'
    - energía = panel-solar-160, sistema-solar-220 ; confort = el resto de los 'extras'
    - los extras de categoria 'bano'/'dormitorio'/'cocina' NO tienen subgrupo (o lo tienen undefined) — el split es solo para 'extras'
    - `npm test` ejecuta validacionCamas.test.js, motorPrecios.test.js y extras.test.js (no quedan huérfanos)
  </behavior>
  <action>
PARTE A — `src/data/extras.js`: agregar `subgrupo` a los 9 extras de `categoria: 'extras'` SIN
tocar id/nombre/precioNeto/categoria. Split canónico (PROJECT.md "Paso sugerido", verificado):
  - ENERGÍA (`subgrupo: 'energia'`): `panel-solar-160`, `sistema-solar-220`
  - CONFORT (`subgrupo: 'confort'`): `calefactor-4000`, `caldera-12v`, `split-3200`,
    `tv-32-directv`, `estereo-pioneer`, `cortinas-blackout`, `toldo`
NO agregar `subgrupo` a las entradas de categoria 'bano'/'dormitorio'/'cocina'.

PARTE B — `src/data/extras.test.js` (node:test, clonar estructura de models.test.js):
```js
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { EXTRAS } from './extras.js'

test("cada extra categoria:'extras' tiene subgrupo válido (confort|energia)", () => {
  const extras = EXTRAS.filter((e) => e.categoria === 'extras')
  for (const e of extras) {
    assert.ok(['confort', 'energia'].includes(e.subgrupo), `${e.id} debe tener subgrupo confort|energia`)
  }
})
test('split 7 confort + 2 energia', () => {
  const extras = EXTRAS.filter((e) => e.categoria === 'extras')
  assert.equal(extras.filter((e) => e.subgrupo === 'confort').length, 7)
  assert.equal(extras.filter((e) => e.subgrupo === 'energia').length, 2)
})
test('energia = panel-solar-160 + sistema-solar-220', () => {
  const energia = EXTRAS.filter((e) => e.subgrupo === 'energia').map((e) => e.id).sort()
  assert.deepEqual(energia, ['panel-solar-160', 'sistema-solar-220'])
})
```

PARTE C — `package.json`: el script `test` enumera archivos a mano (Pitfall 5). AGREGAR los 3
archivos nuevos al final de la lista del script `test` (línea ~10), separados por espacio:
`src/utils/validacionCamas.test.js src/utils/motorPrecios.test.js src/data/extras.test.js`.
NO cambiar a glob (mantener el estilo de lista explícita ya en uso). Verificar que el JSON quede
válido (sin comas colgantes).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test src/data/extras.test.js` exits 0
    - `npm test` exits 0 y su salida incluye los tests de validacionCamas, motorPrecios y extras (conteo de tests SUBE respecto a antes de la fase)
    - grep: src/data/extras.js contiene exactamente 9 ocurrencias de `subgrupo` (filtrado de comentarios) — verificar con `grep -v '^//' src/data/extras.js | grep -c "subgrupo:"` === 9
    - grep: src/data/extras.js contiene `subgrupo: 'energia'` en las líneas de panel-solar-160 y sistema-solar-220
    - grep: package.json contiene `validacionCamas.test.js`, `motorPrecios.test.js`, `extras.test.js` en el script test
  </acceptance_criteria>
  <done>Los 9 extras 'extras' tienen subgrupo (7 confort + 2 energía); extras.test.js verde; los 3 tests nuevos corren bajo `npm test`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → motores puros | El `estado` restaurado de `impacar_config_v1` (key del usuario) entra a `calcularPresupuesto`/`camasEntran`. Es la única frontera: app 100% client-side, sin red, sin backend, sin auth, sin secretos. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `calcularPresupuesto(estado)` | mitigate | `estado.extras` no-array → `Array.isArray` cae a `[]`; `modeloId` inválido → `MODELOS.find` undefined → base 0; `estado` null → optional chaining. Neto ≥ 0, nunca crashea (test cubre los 3 casos). |
| T-05-02 | Tampering | `capacidadFootprints` / `footprintsDeCamas` | mitigate | `modeloId` inexistente → capacidad 0 (no entra nada); `camas` no-array → 0 footprints (`Array.isArray`); elemento sin `tipo` → cuenta 1 (optional chaining `c?.tipo`). No crashea. |
| T-05-03 | DoS | motores puros | accept | Sin loops sobre input no acotado de red; `extras`/`camas` provienen de UI local. Riesgo nulo (no hay atacante remoto; el usuario solo se afecta a sí mismo). |
| T-05-04 | Info Disclosure / Repudiation / Elevation | — | accept | No aplica: sin servidor, sin sesión, sin datos de terceros, sin privilegios. Precios de Lista 108 ya son públicos (catálogo). |
</threat_model>

<verification>
- `npm test` verde con los 3 archivos nuevos incluidos (conteo de tests sube).
- `npm run build` OK (los nuevos módulos ESM importan limpio).
- Gate anti-hardcodeo: la capacidad sale de `camasBase` (no de `0.45`/zona); el IVA solo de formato.js.
- Gate una-sola-fuente: motorPrecios no lee `estado.cocina`/`estado.estar`.
</verification>

<success_criteria>
- camasEntran/capacidadFootprints/footprintsDeCamas exportados y testeados (DORM-02).
- calcularPresupuesto exportado, reusa formato.js, suma única desde extras[] (PRECIO-01).
- Los 9 extras 'extras' con subgrupo (7 confort + 2 energía), data-driven (D-08).
- Los 3 tests nuevos corren bajo `npm test` (Pitfall 5 resuelto).
- Valores de aserción exactos: N4 neto 38.971.845; N4+17 extras neto 55.977.868, total 67.733.220.
</success_criteria>

<output>
After completion, create `.planning/phases/05-pasos-4-6-y-motores-dormitorio-cocina-extras-precios/05-01-SUMMARY.md`
</output>
