---
phase: 05-pasos-4-6-y-motores-dormitorio-cocina-extras-precios
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/state/wizardReducer.js
  - src/state/wizardReducer.test.js
autonomous: true
requirements: [COCINA-01, COCINA-02, COCINA-03, COCINA-04]

must_haves:
  truths:
    - "El plano sabe si hay horno, qué heladera y si hay mesa derivándolo SOLO de extras[] (una sola fuente de verdad)."
    - "configDesdeEstado sigue devolviendo las 6 keys exactas que consume FloorPlan, sin crashear con estado adulterado."
  artifacts:
    - path: "src/state/wizardReducer.js"
      provides: "configDesdeEstado deriva cocina/estar de extras[] (D-14)"
      contains: "extras.includes"
    - path: "src/state/wizardReducer.test.js"
      provides: "tests de derivación D-14 + aserciones de estadoInicial ajustadas"
      contains: "configDesdeEstado"
  key_links:
    - from: "src/state/wizardReducer.js"
      to: "estado.extras[]"
      via: "configDesdeEstado deriva flags del plano"
      pattern: "extras\\.includes\\('cocina-horno'\\)"
---

<objective>
Reescribir `configDesdeEstado(estado)` para que DERIVE de `extras[]` lo que el plano necesita
dibujar (¿hay horno? ¿qué heladera? ¿hay mesa?) en vez de leer `estado.cocina`/`estado.estar`
(D-14). Eliminar los campos `cocina`/`estar` de `estadoInicial` y actualizar las aserciones del
test en el MISMO plan (Pitfall 4: `wizardReducer.js` + su test son una unidad editable). Resultado:
UNA sola fuente de verdad (`extras[]`) para el plano y el motor de precios.

Purpose: Sin esto, el plano de la Wave 2 leería de dos lugares (placeholders `cocina.horno` vs
`extras[]`), duplicando o desincronizando el horno/heladera/mesa (Pitfall 3). configDesdeEstado es
el punto único de proyección estado→plano.
Output: configDesdeEstado data-driven desde extras[] + estadoInicial sin placeholders + test verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pasos-4-6-y-motores-dormitorio-cocina-extras-precios/05-RESEARCH.md

<interfaces>
<!-- Estado actual EXACTO (a editar). El executor parte de aquí. -->

src/state/wizardReducer.js — estadoInicial actual (a editar: quitar cocina/estar):
```js
export const estadoInicial = {
  pasoActual: 0,
  uso: null,
  ocupantes: null,
  modeloId: 'N4',
  bano: { tamano: 'estandar' },
  dormitorio: { camas: [] },
  cocina: { horno: false, heladera: null }, // ← ELIMINAR (D-14)
  estar: { mesa: false },                    // ← ELIMINAR (D-14)
  extras: [],
}
```

configDesdeEstado actual (a reescribir — debe seguir devolviendo las MISMAS 6 keys):
```js
export function configDesdeEstado(estado) {
  const largo = MODELOS.find((m) => m.id === estado.modeloId)?.largo
  return { modeloId: estado.modeloId, largo, bano: estado.bano,
           dormitorio: estado.dormitorio, cocina: estado.cocina, estar: estado.estar }
}
```

copiaEstadoInicial() (helper interno — también clona cocina/estar; ajustar al quitarlos):
```js
function copiaEstadoInicial() {
  return { ...estadoInicial, bano: {...}, dormitorio: {...},
           cocina: { ...estadoInicial.cocina }, estar: { ...estadoInicial.estar }, // ← quitar estas 2
           extras: [...estadoInicial.extras] }
}
```

Cómo consume FloorPlan el config (NO cambiar el shape — el plano lee config.cocina.horno/heladera y config.estar.mesa):
```jsx
// FloorPlan.jsx:124  const tieneMesa = Boolean(config.estar?.mesa)
// FloorPlan.jsx:191  <Kitchen ... opciones={config.cocina} />   // Kitchen lee opciones.horno/heladera
```

Ids reales en extras[] que se derivan (de src/data/extras.js):
```
'cocina-horno' | 'heladera-220' | 'heladera-12v' | 'mesa-cano'
```

Tests existentes que afirman la forma de estadoInicial (src/state/wizardReducer.test.js — AJUSTAR):
```js
// línea ~22: assert.deepEqual(estadoInicial.cocina, { horno: false, heladera: null })  // ← quitar
// línea ~23: assert.deepEqual(estadoInicial.estar, { mesa: false })                     // ← quitar
// línea ~84: assert.notEqual(reseteado.cocina, estadoInicial.cocina)                    // ← quitar
// línea ~89-98: configDesdeEstado debe seguir devolviendo las 6 keys ordenadas: bano,cocina,dormitorio,estar,largo,modeloId
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: configDesdeEstado deriva de extras[] (D-14) + estadoInicial sin placeholders</name>
  <read_first>
    - src/state/wizardReducer.js (estadoInicial, copiaEstadoInicial, configDesdeEstado — los 3 a editar)
    - src/state/wizardReducer.test.js (aserciones de estadoInicial y de las keys de configDesdeEstado — a ajustar)
    - src/components/FloorPlan.jsx líneas 124, 191 (cómo el plano consume config.estar.mesa y config.cocina) — el shape de salida NO cambia
    - src/components/FloorPlanElements/Kitchen.jsx (recibe opciones={horno, heladera})
    - .planning/phases/05-.../05-RESEARCH.md § "Pattern 5" y "Pitfall 4"
  </read_first>
  <behavior>
    - estadoInicial YA NO tiene las keys `cocina` ni `estar` (sí mantiene bano, dormitorio, extras)
    - configDesdeEstado(estadoInicial) devuelve EXACTAMENTE las 6 keys ordenadas: ['bano','cocina','dormitorio','estar','largo','modeloId']
    - configDesdeEstado({modeloId:'N4', extras:['cocina-horno'], ...}) → config.cocina.horno === true
    - configDesdeEstado({..., extras:['heladera-220']}) → config.cocina.heladera === 'heladera-220'
    - configDesdeEstado({..., extras:['heladera-12v']}) → config.cocina.heladera === 'heladera-12v'
    - configDesdeEstado({..., extras:[]}) → config.cocina === { horno:false, heladera:null } y config.estar === { mesa:false }
    - configDesdeEstado({..., extras:['mesa-cano']}) → config.estar.mesa === true
    - configDesdeEstado({modeloId:'N4'}) (sin extras) → no crashea; cocina/estar caen a defaults neutros (optional chaining sobre extras no-array)
    - el largo se sigue derivando de MODELOS por modeloId (N4 → 6.6); las aserciones existentes de largo siguen pasando
    - RESET sigue devolviendo un estado fresco sin compartir referencias (dormitorio nuevo)
  </behavior>
  <action>
PARTE A — `src/state/wizardReducer.js`:

1. En `estadoInicial`: ELIMINAR las líneas `cocina: { horno: false, heladera: null }` y
   `estar: { mesa: false }`. Mantener el resto intacto (incluido `extras: []`). Actualizar el
   comentario de bloque si menciona cocina/estar.

2. En `copiaEstadoInicial()`: ELIMINAR las líneas `cocina: { ...estadoInicial.cocina }` y
   `estar: { ...estadoInicial.estar }` (ya no existen esos campos). Mantener el clon de `bano`,
   `dormitorio.camas` y `extras`.

3. Reescribir `configDesdeEstado` para DERIVAR cocina/estar de `extras[]` (una sola fuente, D-14).
   El shape de salida NO cambia (sigue siendo las mismas 6 keys que consume FloorPlan):

```js
export function configDesdeEstado(estado) {
  const largo = MODELOS.find((m) => m.id === estado.modeloId)?.largo
  const extras = Array.isArray(estado.extras) ? estado.extras : []
  const heladera = extras.includes('heladera-220')
    ? 'heladera-220'
    : extras.includes('heladera-12v')
      ? 'heladera-12v'
      : null
  return {
    modeloId: estado.modeloId,
    largo,
    bano: estado.bano,
    dormitorio: estado.dormitorio,
    cocina: { horno: extras.includes('cocina-horno'), heladera },
    estar: { mesa: extras.includes('mesa-cano') },
  }
}
```

PARTE B — `src/state/wizardReducer.test.js` (Pitfall 4 — actualizar en el MISMO plan):

1. En el test 'estadoInicial: forma esperada': ELIMINAR las dos aserciones
   `assert.deepEqual(estadoInicial.cocina, ...)` y `assert.deepEqual(estadoInicial.estar, ...)`.
   Opcional: agregar `assert.equal(estadoInicial.cocina, undefined)` para documentar la eliminación.
2. En el test 'RESET': ELIMINAR la línea `assert.notEqual(reseteado.cocina, estadoInicial.cocina)`
   (ese campo ya no existe). Mantener la aserción de `dormitorio`.
3. El test 'configDesdeEstado: produce el shape exacto' — las 6 keys ['bano','cocina','dormitorio',
   'estar','largo','modeloId'] SIGUEN siendo correctas (no tocar esa aserción de keys).
4. AGREGAR tests nuevos de derivación D-14: con `extras:['cocina-horno']` → cocina.horno true;
   con `extras:['heladera-220']` → heladera 'heladera-220'; con `extras:['heladera-12v']` →
   'heladera-12v'; con `extras:['mesa-cano']` → estar.mesa true; con `extras:[]` → cocina
   {horno:false,heladera:null} y estar {mesa:false}; estado sin extras → no crashea.
   Construir los estados base con `wizardReducer(estadoInicial, SET_CAMPO campo:'extras' valor:[...])`.

NO tocar `esEstadoValido` en `usePersistedConfig.js` (ya NO exige cocina/estar; solo exige `extras`
array, lo cual sigue válido). Confirmar por lectura que no requiere cambio.
  </action>
  <verify>
    <automated>node --test src/state/wizardReducer.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test src/state/wizardReducer.test.js` exits 0
    - grep: src/state/wizardReducer.js contiene `extras.includes('cocina-horno')`, `extras.includes('mesa-cano')`, `heladera-220`, `heladera-12v`
    - grep: src/state/wizardReducer.js — `estadoInicial` NO contiene `cocina:` ni `estar:` (verificar el bloque estadoInicial; `grep -n "cocina\|estar" src/state/wizardReducer.js` solo debe aparecer dentro de configDesdeEstado, no en estadoInicial)
    - grep: src/state/wizardReducer.test.js contiene aserciones para `cocina.horno`, `cocina.heladera` y `estar.mesa` derivadas de extras
    - grep: src/state/wizardReducer.test.js — el array de keys `['bano','cocina','dormitorio','estar','largo','modeloId']` sigue presente (shape de salida intacto)
    - `npm test` exits 0 (no rompe los tests de hooks/usePersistedConfig que dependen de estadoInicial)
  </acceptance_criteria>
  <done>configDesdeEstado deriva cocina/estar de extras[] con defaults seguros; estadoInicial sin placeholders; el test del reducer (ajustado + nuevos casos D-14) y `npm test` verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → configDesdeEstado | El `estado` restaurado de `impacar_config_v1` (posiblemente viejo, con `cocina`/`estar` legacy o sin `extras`) entra a la proyección estado→plano. Única frontera (client-side puro). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | `configDesdeEstado(estado)` | mitigate | `estado.extras` no-array o ausente → `Array.isArray` cae a `[]`; cocina/estar se derivan a defaults neutros (`{horno:false,heladera:null}`, `{mesa:false}`). Estado viejo con `cocina`/`estar` legacy se IGNORA (no se lee). No crashea. |
| T-05-06 | Tampering | estado persistido legacy | mitigate | Migración no requerida: campos viejos ignorados, nuevos arrancan vacíos. `esEstadoValido` sigue exigiendo `extras` array (frontera ya endurecida en Fase 4); no se relaja. |
| T-05-07 | DoS / Info / Repudiation / Elevation | — | accept | No aplica: proyección pura sin red/servidor/sesión. |
</threat_model>

<verification>
- `npm test` verde (reducer + hooks no rompen tras quitar cocina/estar de estadoInicial).
- `npm run build` OK.
- Gate una-sola-fuente: configDesdeEstado deriva de extras[]; el plano sigue leyendo config.cocina/config.estar sin cambios.
- esEstadoValido sin cambios (sigue aceptando estados sin cocina/estar; exige extras array).
</verification>

<success_criteria>
- configDesdeEstado deriva horno/heladera/mesa de extras[] (D-14); 6 keys de salida intactas.
- estadoInicial sin cocina/estar; copiaEstadoInicial y los tests ajustados (Pitfall 4).
- Tolera estado adulterado/viejo (optional chaining, defaults neutros) sin crashear.
- Tests del reducer (ajustados + casos D-14) y `npm test` verdes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-pasos-4-6-y-motores-dormitorio-cocina-extras-precios/05-02-SUMMARY.md`
</output>
