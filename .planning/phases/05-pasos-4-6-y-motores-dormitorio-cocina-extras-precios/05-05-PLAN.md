---
phase: 05-pasos-4-6-y-motores-dormitorio-cocina-extras-precios
plan: 05
type: execute
wave: 3
depends_on: [05-03, 05-04]
files_modified:
  - src/components/wizard/pasosRegistro.jsx
  - src/components/ConfiguratorWizard.jsx
autonomous: false
requirements: [DORM-03, COCINA-04, PRECIO-01]

must_haves:
  truths:
    - "Los Pasos 4-6 muestran los componentes reales (no stubs) y avanzan con Anterior/Siguiente."
    - "La barra de precio aparece desde el Paso 4 (no en Pasos 1-3) y se actualiza en vivo."
    - "El plano refleja camas C/S/M, horno, mesa y heladera al configurar (DORM-03, COCINA-04)."
  artifacts:
    - path: "src/components/wizard/pasosRegistro.jsx"
      provides: "Pasos 4-6 enchufados a los componentes reales"
      contains: "PasoDormitorio"
    - path: "src/components/ConfiguratorWizard.jsx"
      provides: "Montaje condicional de BarraPrecio (pasoActual >= 3)"
      contains: "BarraPrecio"
  key_links:
    - from: "src/components/wizard/pasosRegistro.jsx"
      to: "src/components/wizard/pasos/PasoDormitorio.jsx"
      via: "import + Componente del paso 'dormitorio'"
      pattern: "PasoDormitorio"
    - from: "src/components/ConfiguratorWizard.jsx"
      to: "src/components/wizard/BarraPrecio.jsx"
      via: "render condicional estado.pasoActual >= 3"
      pattern: "estado.pasoActual >= 3"
---

<objective>
Enchufar los 3 componentes reales (PasoDormitorio/PasoCocina/PasoExtras) en `pasosRegistro.jsx`
reemplazando los `StubPaso` 4-6, y montar `BarraPrecio` en `ConfiguratorWizard.jsx` condicionado a
`pasoActual >= 3` (D-10/Pitfall 6: NO aparece en Pasos 1-3). Luego un checkpoint human-verify a
~375px que valida el flujo completo de la fase en vivo (steppers, exclusividad de heladera,
advertencia de capacidad, barra de precio desde Paso 4, reflejo del plano).

Purpose: Es el enchufe final que pone la fase "en vivo". `pasosRegistro.jsx` tiene file ownership
exclusivo de este plan (precedente Fase 4: ningún otro plan lo toca). El checkpoint cierra la
verificación visual diferida de DORM-03/COCINA-04/PRECIO-01 que los tests puros no cubren.
Output: 2 archivos editados + verificación humana del flujo de Pasos 4-6.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pasos-4-6-y-motores-dormitorio-cocina-extras-precios/05-UI-SPEC.md
@src/components/wizard/pasosRegistro.jsx
@src/components/ConfiguratorWizard.jsx

<interfaces>
<!-- Estado actual EXACTO de los 2 archivos a editar. -->

src/components/wizard/pasosRegistro.jsx — entradas 4-6 actuales (a reemplazar):
```jsx
{ id: 'dormitorio', titulo: 'Dormitorio', Componente: (props) => <StubPaso numero={4} titulo="Dormitorio" {...props} /> },
{ id: 'cocina', titulo: 'Cocina y estar', Componente: (props) => <StubPaso numero={5} titulo="Cocina y estar" {...props} /> },
{ id: 'extras', titulo: 'Extras', Componente: (props) => <StubPaso numero={6} titulo="Extras" {...props} /> },
```
(Los Pasos 1-3 ya están enchufados a PasoUso/PasoDimensiones/PasoBano — NO tocar.)

src/components/ConfiguratorWizard.jsx — estructura del layout actual (líneas 43-55):
```jsx
<div className="lg:grid lg:grid-cols-[1fr_minmax(320px,400px)] lg:items-start lg:gap-8">
  <div className="mb-4 lg:order-2 lg:mb-0">
    <PlanoPanel config={configPlano} />     {/* columna del plano (desktop, sticky) */}
  </div>
  <div className="lg:order-1">
    <section className="rounded border ... p-4">
      <Paso.Componente estado={estado} dispatch={dispatch} />
    </section>
    {/* botones Anterior/Siguiente + Volver a empezar */}
  </div>
</div>
```

Contrato de BarraPrecio (Plan 05-04 — recibe SOLO { estado }):
```jsx
<BarraPrecio estado={estado} />
```

D-10 / UI-SPEC "Barra de precio sticky" (posición):
```
Mobile (<lg): sticky al fondo del viewport, ancho completo, border-t + sombra sutil.
Desktop (lg+): dentro de la columna del plano (lg:order-2), debajo del PlanoPanel, como bloque.
Visibilidad: SOLO pasoActual >= 3 (Paso 4 es índice 3, 0-based). Pitfall 6.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Enchufar Pasos 4-6 reales en pasosRegistro.jsx</name>
  <read_first>
    - src/components/wizard/pasosRegistro.jsx (registro completo; reemplazar SOLO las entradas 4-6)
    - src/components/wizard/pasos/PasoDormitorio.jsx (export default — Plan 05-03)
    - src/components/wizard/pasos/PasoCocina.jsx (export default — Plan 05-03)
    - src/components/wizard/pasos/PasoExtras.jsx (export default — Plan 05-03)
  </read_first>
  <action>
Editar `src/components/wizard/pasosRegistro.jsx`:

1. Agregar los imports de los 3 componentes reales junto a los existentes (PasoUso/PasoDimensiones/
   PasoBano):
```jsx
import PasoDormitorio from './pasos/PasoDormitorio.jsx'
import PasoCocina from './pasos/PasoCocina.jsx'
import PasoExtras from './pasos/PasoExtras.jsx'
```

2. En el array `PASOS`, reemplazar las 3 entradas stub (dormitorio/cocina/extras) por el componente
   real (mismo patrón que los Pasos 1-3 ya enchufados):
```jsx
{ id: 'dormitorio', titulo: 'Dormitorio', Componente: PasoDormitorio },
{ id: 'cocina', titulo: 'Cocina y estar', Componente: PasoCocina },
{ id: 'extras', titulo: 'Extras', Componente: PasoExtras },
```

3. La función `StubPaso` ya no se usa: ELIMINARLA (todos los 6 pasos están enchufados). Si esbuild/
   build advierte por import sin usar, no aplica (StubPaso era local). Verificar que no quede
   referencia a `StubPaso` en el archivo.

NO tocar las entradas de los Pasos 1-3. NO modificar la cáscara del wizard en este task.
  </action>
  <verify>
    <automated>npx esbuild src/components/wizard/pasosRegistro.jsx --bundle --format=esm --external:react --external:./* --outfile=/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - grep: pasosRegistro.jsx contiene `import PasoDormitorio`, `import PasoCocina`, `import PasoExtras`
    - grep: pasosRegistro.jsx contiene `Componente: PasoDormitorio`, `Componente: PasoCocina`, `Componente: PasoExtras`
    - grep: pasosRegistro.jsx NO contiene `StubPaso` (eliminado; los 6 pasos enchufados)
    - grep: pasosRegistro.jsx NO contiene `próximamente` (ya no hay pasos stub)
    - El archivo compila como JSX válido (esbuild sin errores)
  </acceptance_criteria>
  <done>Los Pasos 4-6 del registro apuntan a PasoDormitorio/PasoCocina/PasoExtras; StubPaso eliminado; Pasos 1-3 intactos.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Montar BarraPrecio condicional (pasoActual >= 3) en ConfiguratorWizard.jsx</name>
  <read_first>
    - src/components/ConfiguratorWizard.jsx (layout del grid plano/pasos; dónde montar la barra)
    - src/components/wizard/BarraPrecio.jsx (export default, recibe { estado } — Plan 05-04)
    - .planning/phases/05-.../05-UI-SPEC.md § "Barra de precio sticky" (posición mobile/desktop, D-10) + "Pitfall 6"
  </read_first>
  <action>
Editar `src/components/ConfiguratorWizard.jsx`:

1. Agregar el import: `import BarraPrecio from './wizard/BarraPrecio.jsx'` junto a los demás imports
   de wizard (PlanoPanel, BarraProgreso).

2. DESKTOP — dentro de la columna del plano (`lg:order-2`), debajo del `<PlanoPanel>`, montar la
   barra como bloque SOLO desde el Paso 4. La barra desktop va junto al plano (D-10):
```jsx
<div className="mb-4 lg:order-2 lg:mb-0">
  <PlanoPanel config={configPlano} />
  {estado.pasoActual >= 3 && (
    <div className="mt-4 hidden lg:block">
      <BarraPrecio estado={estado} />
    </div>
  )}
</div>
```

3. MOBILE — barra sticky al fondo del viewport, ancho completo, SOLO desde el Paso 4. Montarla
   fuera del grid (al final del `<main>` o como hermano sticky), visible solo en mobile
   (`lg:hidden`):
```jsx
{estado.pasoActual >= 3 && (
  <div className="sticky bottom-0 z-10 mt-4 border-t border-impacar-texto/10 bg-impacar-fondo p-3 shadow-[0_-2px_8px_rgba(0,0,0,0.06)] lg:hidden">
    <BarraPrecio estado={estado} />
  </div>
)}
```
   Ubicar este bloque mobile dentro del `<main>` pero fuera del grid de 2 columnas (al final), para
   que el `sticky bottom-0` se ancle al scroll del contenido. Verificar que el `<main>` tenga
   suficiente altura para que el sticky funcione (no requiere cambios de layout si el contenido
   scrollea).

CRÍTICO (Pitfall 6 / D-10): la condición `estado.pasoActual >= 3` envuelve AMBAS variantes
(desktop y mobile). En Pasos 1-3 (índices 0/1/2) la barra NO renderiza — la regla "sin precios" de
Fase 4 se preserva. Paso 4 = índice 3.

NO cambiar la lógica de navegación (siguiente/anterior), ni el reflejo del plano (ya cableado).
NO pasar `pasoActual` a BarraPrecio (la barra no decide visibilidad; el contenedor sí).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep: ConfiguratorWizard.jsx contiene `import BarraPrecio from './wizard/BarraPrecio.jsx'`
    - grep: ConfiguratorWizard.jsx contiene `estado.pasoActual >= 3` (montaje condicional — Pitfall 6)
    - grep: ConfiguratorWizard.jsx contiene `<BarraPrecio estado={estado} />` (al menos 1 vez; idealmente 2: desktop + mobile)
    - grep: ConfiguratorWizard.jsx contiene `sticky bottom-0` y `lg:hidden` (variante mobile) y `hidden lg:block` (variante desktop)
    - `npm run build` exits 0 (build de Vite OK con la barra montada)
    - `npm test` exits 0 (no rompe ningún test)
  </acceptance_criteria>
  <done>BarraPrecio montada en desktop (bajo el plano) y mobile (sticky bottom), ambas envueltas por pasoActual >= 3; build verde; no aparece en Pasos 1-3.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint — verificación visual del flujo Pasos 4-6 a ~375px</name>
  <what-built>
    Pasos 4-6 enchufados (PasoDormitorio/PasoCocina/PasoExtras), motores de validación de camas y
    de precios en línea, BarraPrecio sticky desde Paso 4, y el plano reflejando camas/horno/mesa/
    heladera. `npm test` verde (validacionCamas, motorPrecios, extras, wizardReducer D-14) y
    `npm run build` OK.
  </what-built>
  <how-to-verify>
    Levantar el dev server: `npm run dev`. Abrir en un viewport de ~375px (DevTools → device
    Samsung/iPhone, o ventana angosta). Verificar de punta a punta:

    1. PASOS 1-3 SIN PRECIO: en Uso, Dimensiones y Baño la barra de precio NO aparece (regla de
       Fase 4 preservada). Elegir un modelo (p.ej. N4) en el Paso 2.
    2. PASO 4 — DORMITORIO:
       - La barra de precio APARECE (sticky al fondo en mobile): "Presupuesto estimado" + Neto /
         IVA 21% / Total c/IVA en formato `$38.971.845` (3 líneas).
       - Steppers C/S/M con −/+: agregar camas; el número sube y el plano dibuja las camas
         marrón con etiqueta C/S/M (2 filas + pasillo). El total NO cambia con camas (las camas no
         tienen precio propio; la barra muestra modelo + extras).
       - Matrimonial: el botón `+` se deshabilita al llegar a 1 y aparece "Máximo 1 matrimonial por
         casilla."
       - Capacidad: en N4 agregar 5 camas simples → aparece la advertencia cobre "Esta combinación
         de camas no entra en el modelo N4. Le sugerimos elegir un modelo más largo." y el botón
         "Siguiente" SIGUE habilitado (aviso sin bloqueo, D-03). En N5-N7 aparece "se arma a medida"
         en lugar de la advertencia.
    3. PASO 5 — COCINA Y ESTAR:
       - Toggle "Cocina con horno industrial": al marcarlo el total sube $585.800 y el plano refleja
         la cocina/horno (zona verde).
       - Heladera (Sin / 220V A++ / 12V c/pantalla): elegir "220V A++" suma $1.699.000; luego elegir
         "12V c/pantalla" REEMPLAZA (no se acumulan; el total no suma ambas); "Sin" deja sin heladera.
       - Toggles "Mesa de caño" / "Banco despensero": al marcar la mesa el plano dibuja la mesa en el
         estar; ambos suben el total.
    4. PASO 6 — EXTRAS: dos subgrupos con encabezado "Confort" (7) y "Energía" (2); cada extra con su
       precio; marcar varios sube el total en vivo. Estos NO se dibujan en el plano (D-09).
    5. PERSISTENCIA: recargar (F5) en el Paso 5 → retoma el mismo paso con las selecciones intactas y
       la barra de precio con el mismo total.
    6. TRATO DE USTED: todo el copy en usted ("Arme", "Sume", "Le sugerimos"), sin voseo.
  </how-to-verify>
  <resume-signal>Escriba "aprobado" si todo pasa, o describa los problemas encontrados (paso, comportamiento esperado vs observado).</resume-signal>
  <action>Verificacion manual humana: el ejecutor levanta el dev server (npm run dev), pausa, y el usuario recorre los Pasos 4-6 a ~375px siguiendo how-to-verify. No hay edicion de codigo en este task.</action>
  <verify>
    <automated>npm test &amp;&amp; npm run build</automated>
  </verify>
  <done>El usuario respondio "aprobado" tras verificar el flujo completo: sin precio en Pasos 1-3, barra desde Paso 4, steppers + tope matrimonial, advertencia de capacidad sin bloqueo, exclusividad de heladera, reflejo del plano (camas/horno/mesa), persistencia F5 y trato de usted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → wizard render | El `estado` restaurado (con `pasoActual`/`extras` posiblemente adulterados) decide qué paso y si la barra se monta. Única frontera (client-side puro). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | Tampering | montaje condicional `estado.pasoActual >= 3` | mitigate | `pasoActual` ya viene acotado a [0, TOTAL_PASOS-1] por el reducer (`acotarPaso`) y `esEstadoValido` exige `typeof pasoActual === 'number'`; un valor no-numérico cae a estadoInicial. La comparación `>= 3` con un número fuera de rango no rompe el render (a lo sumo no muestra/muestra la barra). |
| T-05-14 | Tampering | Pasos 4-6 render | mitigate | Los componentes (Plan 05-03) ya toleran extras/camas adulterados (Array.isArray + optional chaining). El registro solo enchufa; no introduce nueva frontera. |
| T-05-15 | DoS / Info / Repudiation / Elevation | — | accept | No aplica: render client-side sin red/servidor/sesión; precios de Lista 108 son catálogo público. |
</threat_model>

<verification>
- `npm test` verde (suite completa con los archivos nuevos de la fase).
- `npm run build` OK con los 6 pasos enchufados y la barra montada.
- Pitfall 6: la barra NO aparece en Pasos 1-3 (verificado visualmente en el checkpoint).
- DORM-03 / COCINA-04 / PRECIO-01 (UI): reflejo del plano y barra en vivo verificados a ~375px.
</verification>

<success_criteria>
- Pasos 4-6 muestran los componentes reales; StubPaso eliminado (SHELL-02 intacto).
- BarraPrecio aparece desde Paso 4 (no en 1-3), sticky en mobile / bloque en desktop (D-10).
- El plano refleja camas C/S/M, horno, mesa y heladera (DORM-03, COCINA-04).
- Checkpoint human-verify aprobado a ~375px (flujo completo, exclusividad de heladera, advertencia
  sin bloqueo, persistencia F5, trato de usted).
</success_criteria>

<output>
After completion, create `.planning/phases/05-pasos-4-6-y-motores-dormitorio-cocina-extras-precios/05-05-SUMMARY.md`
</output>
