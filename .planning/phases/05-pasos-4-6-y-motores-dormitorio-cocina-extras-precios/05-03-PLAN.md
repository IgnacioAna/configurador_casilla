---
phase: 05-pasos-4-6-y-motores-dormitorio-cocina-extras-precios
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - src/components/wizard/pasos/PasoDormitorio.jsx
  - src/components/wizard/pasos/PasoCocina.jsx
  - src/components/wizard/pasos/PasoExtras.jsx
autonomous: true
requirements: [DORM-01, DORM-02, DORM-03, COCINA-01, COCINA-02, COCINA-03, EXTRAS-01]

must_haves:
  truths:
    - "El usuario combina camas C/S/M con steppers +/- (matrimonial topea en 1) y ve una advertencia clara si no entran (sin bloquear Siguiente)."
    - "El usuario configura cocina (horno opcional), elige UNA heladera (Sin/220V/12V, exclusiva) y agrega elementos del estar (mesa, banco)."
    - "El usuario selecciona accesorios de Confort y Energía agrupados por subgrupo data-driven."
    - "Cada selección escribe en extras[]/dormitorio.camas y el plano reacciona solo (sin props del plano)."
  artifacts:
    - path: "src/components/wizard/pasos/PasoDormitorio.jsx"
      provides: "Stepper C/S/M + advertencia de capacidad (DORM-01/02/03)"
      min_lines: 60
    - path: "src/components/wizard/pasos/PasoCocina.jsx"
      provides: "Toggle horno + selector exclusivo heladera + toggles estar (COCINA-01/02/03)"
      min_lines: 60
    - path: "src/components/wizard/pasos/PasoExtras.jsx"
      provides: "Extras agrupados Confort/Energía por subgrupo (EXTRAS-01)"
      min_lines: 40
  key_links:
    - from: "src/components/wizard/pasos/PasoDormitorio.jsx"
      to: "src/utils/validacionCamas.js"
      via: "import camasEntran para la advertencia"
      pattern: "camasEntran"
    - from: "src/components/wizard/pasos/PasoCocina.jsx"
      to: "estado.extras[]"
      via: "selector exclusivo de heladera (filtra ambos ids)"
      pattern: "filter"
    - from: "src/components/wizard/pasos/PasoExtras.jsx"
      to: "src/data/extras.js"
      via: "agrupado por subgrupo (data-driven)"
      pattern: "subgrupo"
---

<objective>
Crear los TRES componentes presentacionales de los Pasos 4-6, clonando el patrón de `PasoBano.jsx`
(`{ estado, dispatch }`, lee de `/data`, despacha `SET_CAMPO` con array inmutable; NO recibe props
del plano). PasoDormitorio (steppers C/S/M + advertencia de capacidad), PasoCocina (horno + heladera
exclusiva + estar) y PasoExtras (extras agrupados Confort/Energía). Los componentes quedan creados
pero NO enchufados — el Plan 05-05 toca `pasosRegistro.jsx` (file ownership exclusivo, como en Fase 4).

Purpose: La UI de los 3 pasos finales. El plano reacciona solo vía `configDesdeEstado` (Plan 05-02);
el precio lo muestra la BarraPrecio (Plan 05-04). Estos componentes son desacoplados de ambos.
Output: 3 archivos .jsx nuevos en src/components/wizard/pasos/, alineados al UI-SPEC aprobado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pasos-4-6-y-motores-dormitorio-cocina-extras-precios/05-UI-SPEC.md
@.planning/phases/05-pasos-4-6-y-motores-dormitorio-cocina-extras-precios/05-RESEARCH.md
@src/components/wizard/pasos/PasoBano.jsx
@src/components/wizard/pasos/PasoUso.jsx

<interfaces>
<!-- Contratos exactos que el executor consume. -->

Contrato del paso (ConfiguratorWizard.jsx:54 — recibe SOLO estado/dispatch):
```jsx
<Paso.Componente estado={estado} dispatch={dispatch} />
```

De src/state/wizardReducer.js:
```js
export const ACCIONES = { SET_CAMPO: 'SET_CAMPO', ... }
// dispatch({ type: ACCIONES.SET_CAMPO, campo: 'extras', valor: nuevoArray })
// dispatch({ type: ACCIONES.SET_CAMPO, campo: 'dormitorio', valor: { ...estado.dormitorio, camas } })
```

De src/utils/validacionCamas.js (Plan 05-01 — consumir para la advertencia):
```js
export function camasEntran(modeloId, camas) // boolean (DORM-02)
```

De src/data/models.js (para la advertencia: nombre + personalizable):
```js
// MODELOS.find(m => m.id === estado.modeloId) → { nombre, personalizable, ... }
```

De src/data/extras.js (ids reales — filtrar por categoria/subgrupo, NO hardcodear listas):
```
categoria 'cocina': cocina-horno(585800), heladera-220(1699000), heladera-12v(1690000), mesa-cano(218000), banco-despensero(172098)
categoria 'extras' (con subgrupo tras Plan 05-01):
  confort: calefactor-4000, caldera-12v, split-3200, tv-32-directv, estereo-pioneer, cortinas-blackout, toldo
  energia: panel-solar-160, sistema-solar-220
```

De src/utils/formato.js (para el precio caption de cada extra):
```js
export function formatPrecio(n) // 410000 -> "$410.000"
```

Patrón toggle inmutable en extras[] (de PasoBano.jsx — REUSAR):
```jsx
const toggleExtra = (id) => {
  const nuevos = estado.extras.includes(id)
    ? estado.extras.filter((x) => x !== id)
    : [...estado.extras, id]
  dispatch({ type: ACCIONES.SET_CAMPO, campo: 'extras', valor: nuevos })
}
```

Clases de control activo (UI-SPEC — idénticas a PasoBano/PasoUso):
```
activo:  'border border-impacar-campo bg-impacar-campo/10 font-semibold text-impacar-campo'
reposo:  'border border-impacar-texto/10 hover:border-impacar-campo/40'
touch:   'min-h-[44px] min-w-[44px]'  (obligatorio en todo control)
foco:    'focus:outline-none focus:ring-2 focus:ring-impacar-campo/40'
warn:    'border-impacar-cobre text-impacar-cobre'  (advertencia de capacidad)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: PasoDormitorio — steppers C/S/M + advertencia de capacidad (DORM-01/02/03)</name>
  <read_first>
    - src/components/wizard/pasos/PasoBano.jsx (contrato { estado, dispatch }, h2/subtítulo, clases activo/reposo)
    - src/components/wizard/pasos/PasoUso.jsx (patrón chips segmentados + aria-pressed)
    - src/utils/validacionCamas.js (camasEntran — consumir para la advertencia)
    - src/data/models.js (MODELOS.find → nombre, personalizable)
    - .planning/phases/05-.../05-UI-SPEC.md § "Component Inventory 1. Stepper de camas" y "Copywriting Contract" (copys EXACTOS)
    - .planning/phases/05-.../05-RESEARCH.md § "Pattern 4: Stepper de camas con tope por tipo"
  </read_first>
  <action>
Crear `src/components/wizard/pasos/PasoDormitorio.jsx` (presentacional, `{ estado, dispatch }`,
imports `../../../`). Estructura:

1. `<h2 className="text-xl font-semibold text-impacar-texto">Dormitorio</h2>` + subtítulo
   `<p className="mt-2 text-sm text-impacar-texto/70">Arme el dormitorio combinando los tipos de cama.</p>`

2. TIPOS de cama como constante UI (etiquetas, no datos de negocio):
   `const TIPOS = [{ tipo:'C', label:'Cucheta marinera (C)' }, { tipo:'S', label:'Cama simple (S)' }, { tipo:'M', label:'Matrimonial (M)' }]`

3. Helpers (Pattern 4, array inmutable — NUNCA push/splice):
```jsx
const camas = Array.isArray(estado.dormitorio?.camas) ? estado.dormitorio.camas : []
const contar = (tipo) => camas.filter((c) => c.tipo === tipo).length
const setCamas = (nuevas) =>
  dispatch({ type: ACCIONES.SET_CAMPO, campo: 'dormitorio', valor: { ...estado.dormitorio, camas: nuevas } })
const agregar = (tipo) => {
  if (tipo === 'M' && contar('M') >= 1) return   // D-02: matrimonial máx 1
  setCamas([...camas, { tipo }])
}
const quitar = (tipo) => {
  const idx = camas.findIndex((c) => c.tipo === tipo)
  if (idx === -1) return
  setCamas(camas.filter((_, i) => i !== idx))
}
```

4. Una FILA por tipo (`space-y-2`): label a la izquierda (`text-sm`) + grupo `[ − ] {n} [ + ]` a
   la derecha. Botones `−`/`+` con `min-h-[44px] min-w-[44px] rounded border`, foco
   `focus:ring-impacar-campo/40`. Número `{n}` en `w-8 text-center text-sm font-semibold`, en
   `text-impacar-campo` si `n >= 1`, con `aria-live="polite"`. Botón `−` `disabled` cuando `n===0`;
   botón `+` de Matrimonial `disabled + aria-disabled` cuando `contar('M') >= 1`. Cada botón con
   `aria-label` ("Agregar cucheta", "Quitar cucheta", etc.).

5. Nota tope matrimonial (visible solo cuando `contar('M') >= 1`):
   `<p className="text-xs text-impacar-texto/70">Máximo 1 matrimonial por casilla.</p>`

6. Bloque advertencia de capacidad (`mt-6`), data-driven con camasEntran:
```jsx
const modelo = MODELOS.find((m) => m.id === estado.modeloId)
const entran = camasEntran(estado.modeloId, camas)
const personalizable = Boolean(modelo?.personalizable)
// Si personalizable → nota "a medida" en lugar de advertencia.
// Si !personalizable && !entran && camas.length > 0 → advertencia cobre.
```
   - Cuando `personalizable === true`:
     `<p className="mt-6 text-xs text-impacar-texto/70">El modelo {modelo?.nombre} se arma a medida: admite más camas según el largo.</p>`
   - Cuando `!personalizable && !entran && camas.length > 0`, caja cobre:
     `<div className="mt-6 rounded border border-impacar-cobre p-3 text-sm text-impacar-cobre">Esta combinación de camas no entra en el modelo {modelo?.nombre}. Le sugerimos elegir un modelo más largo.</div>`
   - NUNCA deshabilitar nada de navegación (D-03: aviso sin bloqueo; el botón Siguiente vive en la cáscara).

DORM-03: el componente NO importa ni renderiza el plano; al escribir `dormitorio.camas` el plano
reacciona solo (configDesdeEstado → PlanoPanel). Optional chaining en `estado.dormitorio?.camas`
(tolera estado adulterado).
  </action>
  <verify>
    <automated>npx esbuild src/components/wizard/pasos/PasoDormitorio.jsx --bundle --format=esm --external:react --external:../* --outfile=/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - grep: PasoDormitorio.jsx contiene `function PasoDormitorio({ estado, dispatch })` (contrato del paso)
    - grep: PasoDormitorio.jsx contiene `camasEntran` (importado de validacionCamas, consumido para la advertencia)
    - grep: PasoDormitorio.jsx contiene la cadena exacta `Máximo 1 matrimonial por casilla.`
    - grep: PasoDormitorio.jsx contiene `no entra en el modelo` y `border-impacar-cobre` (advertencia D-03)
    - grep: PasoDormitorio.jsx contiene `se arma a medida` (nota personalizables N5-N7)
    - grep: PasoDormitorio.jsx NO contiene `FloorPlan` ni `PlanoPanel` (no acopla el paso al plano)
    - grep: PasoDormitorio.jsx NO contiene `.push(` ni `.splice(` (array inmutable)
    - gate anti-voseo: PasoDormitorio.jsx NO contiene ` arma ` en voseo (es trato de usted "arme"); grep `Arme el dormitorio`
    - El componente compila como JSX válido (esbuild sin errores de sintaxis)
  </acceptance_criteria>
  <done>PasoDormitorio renderiza 3 steppers C/S/M con tope M=1, advertencia cobre cuando no entran (sin bloquear), nota "a medida" para N5-N7; escribe dormitorio.camas inmutable; no acopla el plano.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: PasoCocina — horno + selector exclusivo de heladera + estar (COCINA-01/02/03)</name>
  <read_first>
    - src/components/wizard/pasos/PasoBano.jsx (toggle inmutable en extras[]; checkboxes data-driven; chips segmentados)
    - src/data/extras.js (ids reales categoria 'cocina')
    - .planning/phases/05-.../05-UI-SPEC.md § "Component Inventory 2 (heladera) y 3 (toggles)" + "Copywriting Contract" (labels EXACTOS)
    - .planning/phases/05-.../05-RESEARCH.md § "Pattern 2: Selector exclusivo de heladera" y "Pitfall 2"
  </read_first>
  <action>
Crear `src/components/wizard/pasos/PasoCocina.jsx` (presentacional, `{ estado, dispatch }`). Importar
EXTRAS al tope del archivo (misma profundidad relativa que PasoBano.jsx, que estos clonan):
```jsx
import { EXTRAS } from '../../../data/extras.js'
```
Estructura:

1. `<h2>Cocina y estar</h2>` (text-xl font-semibold) + subtítulo:
   `La cocina incluye mesada, bacha y cocina a gas. Sume las opciones que necesite.`

2. toggleExtra (idéntico a PasoBano — array inmutable):
```jsx
const extras = Array.isArray(estado.extras) ? estado.extras : []
const toggleExtra = (id) => {
  const nuevos = extras.includes(id) ? extras.filter((x) => x !== id) : [...extras, id]
  dispatch({ type: ACCIONES.SET_CAMPO, campo: 'extras', valor: nuevos })
}
```

3. SELECTOR EXCLUSIVO DE HELADERA (COCINA-02, D-05, Pattern 2 — NO es toggle). Derivar los ids de
   heladera de EXTRAS (anti-hardcodeo) por prefijo:
```jsx
const IDS_HELADERA = EXTRAS.filter((e) => e.categoria === 'cocina' && e.id.startsWith('heladera-')).map((e) => e.id)
const elegirHeladera = (id /* string | null */) => {
  const sinHeladera = extras.filter((x) => !IDS_HELADERA.includes(x))
  const nuevos = id ? [...sinHeladera, id] : sinHeladera
  dispatch({ type: ACCIONES.SET_CAMPO, campo: 'extras', valor: nuevos })
}
```
   3 segmentos en `role="radiogroup"` con label "Heladera": "Sin" (id=null) · "220V A++"
   (id='heladera-220') · "12V c/pantalla" (id='heladera-12v'). Cada segmento botón con
   `aria-pressed`, `min-h-[44px] min-w-[44px] px-4 py-2`, clases activo/reposo del UI-SPEC.
   "marcado": para 220V/12V → `extras.includes(id)`; para "Sin" → ningún id de IDS_HELADERA presente.
   Mapear los 2 segmentos de heladera desde EXTRAS (no hardcodear nombres más allá del label corto;
   usar el segmento { id, label } como pequeño array UI: `[{id:null,label:'Sin'},{id:'heladera-220',label:'220V A++'},{id:'heladera-12v',label:'12V c/pantalla'}]`).

4. TOGGLES horno/mesa/banco (COCINA-01/03, D-06) — checkboxes `<label>` como PasoBano
   (`min-h-[44px]`, `accent-[#2D5016]`, marcado → `border-impacar-campo bg-impacar-campo/10`).
   Labels EXACTOS: "Cocina con horno industrial" (id 'cocina-horno') · "Mesa de caño"
   (id 'mesa-cano') · "Banco despensero" (id 'banco-despensero'). Usar un array UI
   `[{id:'cocina-horno',label:'Cocina con horno industrial'}, {id:'mesa-cano',label:'Mesa de caño'}, {id:'banco-despensero',label:'Banco despensero'}]` y `.map()` con `toggleExtra`.

El componente NO importa el plano; al escribir extras[] el plano reacciona (configDesdeEstado deriva
horno/heladera/mesa, Plan 05-02). Separar bloques con `mt-6` (Heladera / Estar). Optional chaining.
  </action>
  <verify>
    <automated>npx esbuild src/components/wizard/pasos/PasoCocina.jsx --bundle --format=esm --external:react --external:../* --outfile=/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - grep: PasoCocina.jsx contiene `import { EXTRAS } from '../../../data/extras.js'` (import explícito de EXTRAS)
    - grep: PasoCocina.jsx contiene `function PasoCocina({ estado, dispatch })`
    - grep: PasoCocina.jsx contiene `startsWith('heladera-')` (ids de heladera derivados, NO hardcodeados como array literal)
    - grep: PasoCocina.jsx contiene `role="radiogroup"` (selector exclusivo de heladera)
    - grep: PasoCocina.jsx contiene `filter((x) => !IDS_HELADERA.includes(x))` (exclusividad mutua — Pitfall 2)
    - grep: PasoCocina.jsx contiene las cadenas exactas `Cocina con horno industrial`, `Mesa de caño`, `Banco despensero`, `220V A++`, `12V c/pantalla`
    - grep: PasoCocina.jsx contiene los ids `cocina-horno`, `mesa-cano`, `banco-despensero`
    - grep: PasoCocina.jsx NO contiene `FloorPlan` ni `PlanoPanel`
    - grep: PasoCocina.jsx NO contiene `.push(` ni `.splice(`
    - El componente compila como JSX válido (esbuild sin errores)
  </acceptance_criteria>
  <done>PasoCocina ofrece toggle horno, selector exclusivo de heladera (elegir una quita la otra), toggles mesa/banco; todo en extras[] inmutable; ids de heladera derivados; no acopla el plano.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: PasoExtras — extras agrupados Confort/Energía por subgrupo (EXTRAS-01)</name>
  <read_first>
    - src/components/wizard/pasos/PasoBano.jsx (lista de checkboxes data-driven + toggle inmutable)
    - src/data/extras.js (EXTRAS con metadato subgrupo tras Plan 05-01)
    - src/utils/formato.js (formatPrecio para el precio caption de cada extra)
    - .planning/phases/05-.../05-UI-SPEC.md § "Component Inventory 4. Extras agrupados" + "Copywriting Contract"
    - .planning/phases/05-.../05-RESEARCH.md § "Pattern 3: Split confort/energía data-driven"
  </read_first>
  <action>
Crear `src/components/wizard/pasos/PasoExtras.jsx` (presentacional, `{ estado, dispatch }`). Importar
EXTRAS al tope del archivo (misma profundidad relativa que PasoBano.jsx, que este clona):
```jsx
import { EXTRAS } from '../../../data/extras.js'
```
Estructura:

1. `<h2>Extras</h2>` + subtítulo: `Sume los accesorios de confort y energía que prefiera.`

2. toggleExtra idéntico a PasoBano (array inmutable en extras[]).

3. SUBGRUPOS data-driven (NO lista de ids hardcodeada — gate anti-hardcodeo, D-08):
```jsx
const SUBGRUPOS = [{ id: 'confort', titulo: 'Confort' }, { id: 'energia', titulo: 'Energía' }]
// Para cada subgrupo: EXTRAS.filter((e) => e.categoria === 'extras' && e.subgrupo === g.id)
```
   Renderizar 2 secciones (cada una separada por `mt-6`):
   - Encabezado `text-sm font-semibold text-impacar-campo` con el `titulo`.
   - Lista de `<label>` checkbox (como PasoBano): `min-h-[44px]`, `accent-[#2D5016]`, marcado →
     `border-impacar-campo bg-impacar-campo/10`. Cada label: nombre del extra (`text-sm`) +
     precio caption a la derecha (`text-xs text-impacar-texto/70`) con
     `formatPrecio(e.precioNeto)`. Usar `flex items-center justify-between` o un span a la derecha
     (`ml-auto`) para alinear el precio.

El componente NO toca el plano (D-09: estos extras no tienen footprint). Solo escriben extras[]
(la BarraPrecio del Plan 05-04 los suma). Optional chaining sobre estado.extras.
  </action>
  <verify>
    <automated>npx esbuild src/components/wizard/pasos/PasoExtras.jsx --bundle --format=esm --external:react --external:../* --outfile=/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - grep: PasoExtras.jsx contiene `import { EXTRAS } from '../../../data/extras.js'` (import explícito de EXTRAS)
    - grep: PasoExtras.jsx contiene `function PasoExtras({ estado, dispatch })`
    - grep: PasoExtras.jsx contiene `e.subgrupo === g.id` o equivalente `subgrupo` (agrupado data-driven)
    - grep: PasoExtras.jsx contiene las cadenas exactas `Confort` y `Energía`
    - grep: PasoExtras.jsx contiene `formatPrecio` (precio caption por extra)
    - grep: PasoExtras.jsx NO contiene los ids literales de extras (p.ej. NO `'calefactor-4000'`, NO `'panel-solar-160'`) — todo derivado de EXTRAS por filtro
    - grep: PasoExtras.jsx contiene `categoria === 'extras'` (filtro de catálogo)
    - grep: PasoExtras.jsx NO contiene `.push(` ni `.splice(`
    - El componente compila como JSX válido (esbuild sin errores)
  </acceptance_criteria>
  <done>PasoExtras lista los 9 extras agrupados en Confort (7) y Energía (2) por metadato subgrupo, con checkbox + precio; toggle inmutable en extras[]; sin ids hardcodeados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → pasos | `estado.extras` / `estado.dormitorio.camas` restaurados de `impacar_config_v1` se leen en los handlers de los 3 pasos. Única frontera (client-side puro; la UI no recibe input de red). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Tampering | handlers de los 3 pasos | mitigate | `Array.isArray(estado.extras)` y `estado.dormitorio?.camas` con fallback `[]` antes de `.includes`/`.filter`/`.findIndex`; nunca crashea con extras/camas adulterados de localStorage (precedente Fase 4 CR-01). |
| T-05-09 | Tampering | selector exclusivo de heladera | mitigate | `IDS_HELADERA` derivado de `EXTRAS` (no de input); el filtro tolera ids desconocidos en extras[] (los preserva, solo quita los de heladera). Imposible quedar con 2 heladeras (Pitfall 2). |
| T-05-10 | DoS / Info / Repudiation / Elevation | — | accept | No aplica: componentes presentacionales sin red/servidor/sesión; los precios (Lista 108) son catálogo público. |
</threat_model>

<verification>
- `npm run build` OK (los 3 componentes compilan e importan limpio).
- Gate anti-hardcodeo: heladera por prefijo, extras por subgrupo, sin listas de ids literales en los componentes.
- Gate anti-voseo (UX-03): copy en trato de usted ("Arme", "Sume", "Le sugerimos") — grep voseo = 0.
- Desacople: ningún paso importa el plano ni el precio.
</verification>

<success_criteria>
- PasoDormitorio: steppers C/S/M, M topea en 1, advertencia cobre sin bloqueo, nota "a medida" N5-N7 (DORM-01/02/03).
- PasoCocina: horno toggle, heladera exclusiva (Sin/220V/12V), mesa/banco toggles (COCINA-01/02/03).
- PasoExtras: 9 extras agrupados Confort/Energía por subgrupo (EXTRAS-01).
- Los 3 escriben en extras[]/dormitorio.camas inmutable; el plano reacciona solo.
</success_criteria>

<output>
After completion, create `.planning/phases/05-pasos-4-6-y-motores-dormitorio-cocina-extras-precios/05-03-SUMMARY.md`
</output>
</content>
</invoke>
