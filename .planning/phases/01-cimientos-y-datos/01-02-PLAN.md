---
phase: 01-cimientos-y-datos
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/data/models.js
  - src/data/extras.js
  - src/data/geometry.js
  - src/data/financiacion.js
  - src/utils/formato.js
  - src/App.jsx
autonomous: true
requirements: []   # Phase 1 es infraestructura — sin REQ-ID propio; estos datos alimentan PLANO/DIM/DORM/PRECIO en fases siguientes.

user_setup: []

must_haves:
  truths:
    - "`data/models.js` expone los 7 modelos reales (N1-N7) con id, largo, camas base y precio neto exactos de la Lista 108."
    - "`data/extras.js` expone los 17 accesorios reales con su precio neto exacto y su paso/categoría sugerido."
    - "Las constantes de geometría real (exterior 2.60m, interior 2.52m, cama 0.80m, pasillo 0.92m, baulera/cocina 0.60m, IVA 0.21) están centralizadas en `data/geometry.js`."
    - "`formatPrecio()` formatea un número en formato argentino ($ con punto de miles) y existe un helper de IVA."
    - "La suma de los 7 precios netos importados coincide con la suma esperada (smoke check de que los datos importan y los valores son correctos)."
  artifacts:
    - path: "src/data/models.js"
      provides: "Los 7 modelos N1-N7 con largo, camas base, precio neto y sugerencia de ocupantes"
      exports: ["MODELOS"]
      contains: "29108976"
    - path: "src/data/extras.js"
      provides: "Los 17 accesorios reales con precio neto y categoría/paso"
      exports: ["EXTRAS"]
      contains: "495000"
    - path: "src/data/geometry.js"
      provides: "Constantes de geometría real y la tasa de IVA centralizadas"
      exports: ["GEOMETRIA", "IVA"]
      contains: "2.52"
    - path: "src/utils/formato.js"
      provides: "formatPrecio (formato argentino) y helpers de IVA (neto→IVA→total)"
      exports: ["formatPrecio", "calcularIVA", "calcularTotal"]
      contains: "es-AR"
    - path: "src/data/financiacion.js"
      provides: "Opciones de financiación de la spec (consumidas en el resumen, Phase 6)"
      exports: ["FINANCIACION"]
  key_links:
    - from: "src/App.jsx"
      to: "src/data/models.js"
      via: "import { MODELOS }"
      pattern: "import.*MODELOS.*from.*data/models"
    - from: "src/App.jsx"
      to: "src/utils/formato.js"
      via: "import { formatPrecio }"
      pattern: "import.*formatPrecio.*from.*utils/formato"
    - from: "src/data/models.js"
      to: "src/data/geometry.js"
      via: "ancho exterior compartido (referencia conceptual)"
      pattern: "GEOMETRIA|2\\.60"
---

<objective>
Crear los archivos de datos reales de la Lista 108 (Feb-2026) aislados en `src/data`,
las constantes de geometría real centralizadas, y las utilidades de formato de precio
(formato argentino) y cálculo de IVA. Wirearlos en `App.jsx` como smoke render para
probar que importan y que `formatPrecio()` funciona.

Purpose: Estos datos son la fuente autoritativa que consumen TODAS las fases siguientes:
el plano SVG (geometría), el wizard (modelos, sugerencia de ocupantes), el motor de
validación de camas (geometría) y el motor de precios (precios netos + IVA + formato).
Los precios reales son 2-3x los placeholders del prompt original — deben ser EXACTOS.

Output: `data/models.js` (7 modelos), `data/extras.js` (17 accesorios),
`data/geometry.js` (geometría + IVA), `data/financiacion.js`, `utils/formato.js`
(formatPrecio + IVA), y `App.jsx` mostrando un smoke render con un precio formateado.

Cubre los Success Criteria #2, #3 y #4 de la fase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-cimientos-y-datos/01-01-SUMMARY.md

<interfaces>
<!-- Contratos que crean estos archivos. Las fases siguientes importan de acá. -->
<!-- El executor NO necesita explorar el codebase: estos son los shapes a producir. -->

// src/data/models.js
export const MODELOS = [
  { id, nombre, largo, camasBase, precioNeto, ocupantesIdeal, personalizable }
  // ...7 entradas N1..N7
]

// src/data/extras.js
export const EXTRAS = [
  { id, nombre, precioNeto, categoria }  // categoria: 'bano' | 'dormitorio' | 'cocina' | 'extras'
  // ...17 entradas
]

// src/data/geometry.js
export const GEOMETRIA = { anchoExterior, anchoInterior, anchoCama, largoCama, pasilloCentral, zonaBaulera, zonaCocina }
export const IVA = 0.21

// src/utils/formato.js
export function formatPrecio(n) -> string  // "$29.108.976"
export function calcularIVA(neto) -> number
export function calcularTotal(neto) -> number  // neto * 1.21

// src/data/financiacion.js
export const FINANCIACION = [ { id, nombre, detalle } ]
</interfaces>

<notes>
- DEPENDE de Plan 01 (Wave 1): el scaffold, `src/data/`, `src/utils/` y `App.jsx` ya existen.
- Precios y medidas: copiar VERBATIM de PROJECT.md > "Datos reales (Lista 108)". NO redondear,
  NO inventar, NO usar placeholders. Los valores exactos están abajo en cada <action>.
- ESM (`export const`) — el proyecto usa `"type": "module"`.
- Idioma de los `nombre`: español argentino (son los textos que verá el cliente en fases siguientes).
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear data/models.js, data/geometry.js y data/financiacion.js</name>
  <read_first>
    - PROJECT.md (sección "Datos reales (Lista 108)": tabla de Modelos y sección Geometría — valores exactos)
    - El bloque <interfaces> de este plan (shapes a producir)
  </read_first>
  <files>src/data/models.js, src/data/geometry.js, src/data/financiacion.js</files>
  <action>
Crear los tres archivos de datos base. Usar los valores EXACTOS de la Lista 108.

**1. `src/data/geometry.js`** — geometría real centralizada + IVA. Valores exactos (metros):
   ```js
   // Geometría real de las casillas Impacar (Lista 108). Todas las medidas en metros.
   export const GEOMETRIA = {
     anchoExterior: 2.60,   // ancho exterior fijo (transporte vial)
     anchoInterior: 2.52,   // ancho interior útil
     anchoCama: 0.80,       // ancho estándar de cama
     largoCama: 2.00,       // largo de cama (corre sobre el eje del largo)
     pasilloCentral: 0.92,  // 0.80 + 0.92 + 0.80 = 2.52 (2 camas + pasillo)
     zonaBaulera: 0.60,     // baulera fija en un extremo
     zonaCocina: 0.60,      // cocina fija en el otro extremo
   }

   export const IVA = 0.21  // 21%
   ```
   IMPORTANTE: verificar que `anchoCama * 2 + pasilloCentral === anchoInterior` (0.80*2 + 0.92 = 2.52).

**2. `src/data/models.js`** — los 7 modelos N1-N7. Precios NETOS exactos (sin separadores, como Number):
   | id | nombre | largo | camasBase | precioNeto | ocupantesIdeal | personalizable |
   | N1 | "N1"   | 4.50  | 2 | 29108976 | 2 | false |
   | N2 | "N2"   | 5.20  | 2 | 34150926 | 3 | false |
   | N3 | "N3"   | 6.10  | 3 | 37181710 | 4 | false |
   | N4 | "N4"   | 6.60  | 4 | 38971845 | 4 | false |
   | N5 | "N5"   | 7.60  | null (opcional/personalizable) | 40937114 | 6 | true |
   | N6 | "N6"   | 8.60  | null (opcional/personalizable) | 42048149 | 6 | true |
   | N7 | "N7"   | 9.60  | null (opcional/personalizable) | 46180786 | 8 | true |

   Estructura por entrada:
   ```js
   export const MODELOS = [
     { id: 'N1', nombre: 'N1', largo: 4.50, camasBase: 2,    precioNeto: 29108976, ocupantesIdeal: 2, personalizable: false },
     { id: 'N2', nombre: 'N2', largo: 5.20, camasBase: 2,    precioNeto: 34150926, ocupantesIdeal: 3, personalizable: false },
     { id: 'N3', nombre: 'N3', largo: 6.10, camasBase: 3,    precioNeto: 37181710, ocupantesIdeal: 4, personalizable: false },
     { id: 'N4', nombre: 'N4', largo: 6.60, camasBase: 4,    precioNeto: 38971845, ocupantesIdeal: 4, personalizable: false },
     { id: 'N5', nombre: 'N5', largo: 7.60, camasBase: null, precioNeto: 40937114, ocupantesIdeal: 6, personalizable: true },
     { id: 'N6', nombre: 'N6', largo: 8.60, camasBase: null, precioNeto: 42048149, ocupantesIdeal: 6, personalizable: true },
     { id: 'N7', nombre: 'N7', largo: 9.60, camasBase: null, precioNeto: 46180786, ocupantesIdeal: 8, personalizable: true },
   ]
   ```

   Agregar también el mapeo ocupantes→modelo sugerido (consumido en Paso 1, Phase 4). Documentar
   el criterio en un comentario. Mapeo (de planning_context, ajustado con criterio sobre camasBase):
   ```js
   // Sugerencia automática de modelo según cantidad de ocupantes (editable en el Paso 2).
   // Criterio: el modelo cuyo ocupantesIdeal cubre la cantidad pedida con el menor largo.
   // 2→N1, 3→N2, 4→N3, 5→N5, 6→N5, 8→N7 (N4 disponible como alternativa de 4 camas).
   export const SUGERENCIA_OCUPANTES = {
     2: 'N1',
     3: 'N2',
     4: 'N3',
     5: 'N5',
     6: 'N5',
     8: 'N7',
   }
   ```

**3. `src/data/financiacion.js`** — opciones de financiación (consumidas en el resumen, Phase 6).
   La spec no fija montos; dejar opciones genéricas orientativas en español argentino:
   ```js
   // Opciones de financiación orientativas (se muestran en el resumen — Phase 6).
   // Sin montos definidos por la fábrica todavía; el asesor comercial confirma condiciones.
   export const FINANCIACION = [
     { id: 'contado',   nombre: 'Pago contado',           detalle: 'Precio de lista, sujeto a confirmación del asesor.' },
     { id: 'financiado', nombre: 'Financiación en cuotas', detalle: 'Planes de pago a convenir con el asesor comercial.' },
     { id: 'permuta',   nombre: 'Permuta / entrega',      detalle: 'Se aceptan permutas a evaluar por la fábrica.' },
   ]
   ```
  </action>
  <verify>
    <automated>node --input-type=module -e "import('./src/data/models.js').then(m=>{const s=m.MODELOS.reduce((a,x)=>a+x.precioNeto,0); if(s!==268579506){console.error('suma modelos incorrecta:',s);process.exit(1)} if(m.MODELOS.length!==7)process.exit(1); console.log('models ok')})" && node --input-type=module -e "import('./src/data/geometry.js').then(g=>{if(g.GEOMETRIA.anchoCama*2+g.GEOMETRIA.pasilloCentral!==g.GEOMETRIA.anchoInterior){console.error('geometria no cierra');process.exit(1)} if(g.IVA!==0.21)process.exit(1); console.log('geometry ok')})"</automated>
  </verify>
  <acceptance_criteria>
    - `src/data/models.js` exporta `MODELOS` con exactamente 7 entradas (N1..N7).
    - Los precios netos son exactos: `grep -q 29108976 src/data/models.js` y también 34150926, 37181710, 38971845, 40937114, 42048149, 46180786.
    - La suma de los 7 `precioNeto` es 268.579.506 (validado por el smoke check automatizado).
    - N5/N6/N7 tienen `personalizable: true` y `camasBase: null`.
    - `src/data/geometry.js` exporta `GEOMETRIA` con `anchoExterior: 2.60`, `anchoInterior: 2.52`, `anchoCama: 0.80`, `pasilloCentral: 0.92`, `zonaBaulera: 0.60`, `zonaCocina: 0.60`.
    - `IVA === 0.21` y `anchoCama*2 + pasilloCentral === anchoInterior` (2.52).
    - `src/data/models.js` exporta `SUGERENCIA_OCUPANTES` con las 6 claves (2,3,4,5,6,8).
    - `src/data/financiacion.js` exporta `FINANCIACION` (array no vacío).
  </acceptance_criteria>
  <done>Los tres archivos de datos existen con los valores exactos de la Lista 108; la suma de precios y la consistencia geométrica pasan el smoke check.</done>
</task>

<task type="auto">
  <name>Task 2: Crear data/extras.js con los 17 accesorios reales</name>
  <read_first>
    - PROJECT.md (sección "Datos reales (Lista 108)" > tabla de Accesorios — los 17 con precio y paso)
    - El bloque <interfaces> (shape de EXTRAS)
  </read_first>
  <files>src/data/extras.js</files>
  <action>
Crear `src/data/extras.js` exportando `EXTRAS`: array de los 17 accesorios reales con precio
neto EXACTO y `categoria` (paso sugerido). Mapear "Paso sugerido" → `categoria`:
Baño→'bano', Dormitorio→'dormitorio', Cocina/estar→'cocina', Extras/*→'extras'.

Valores exactos (id slug en español, nombre como en el catálogo, precioNeto como Number):
   ```js
   export const EXTRAS = [
     { id: 'inodoro-septica',   nombre: 'Inodoro con depósito y cámara séptica',                     precioNeto: 495000,  categoria: 'bano' },
     { id: 'vanitory-espejo',   nombre: 'Vanitory completo con espejo',                              precioNeto: 283294,  categoria: 'bano' },
     { id: 'cajonera-bajo-cama', nombre: 'Cajonera bajo cama (x4)',                                  precioNeto: 322000,  categoria: 'dormitorio' },
     { id: 'cocina-horno',      nombre: 'Cocina con horno industrial',                               precioNeto: 585800,  categoria: 'cocina' },
     { id: 'heladera-220',      nombre: 'Heladera con freezer 220V cat A++',                         precioNeto: 1699000, categoria: 'cocina' },
     { id: 'heladera-12v',      nombre: 'Heladera con freezer 12V + pantalla + regulador',           precioNeto: 1690000, categoria: 'cocina' },
     { id: 'mesa-cano',         nombre: 'Mesa de caño + melamina + aluminio',                        precioNeto: 218000,  categoria: 'cocina' },
     { id: 'banco-despensero',  nombre: 'Banco despensero',                                          precioNeto: 172098,  categoria: 'cocina' },
     { id: 'calefactor-4000',   nombre: 'Calefactor 4000 CAL',                                       precioNeto: 342000,  categoria: 'extras' },
     { id: 'caldera-12v',       nombre: 'Caldera 12V a gasoil',                                      precioNeto: 718000,  categoria: 'extras' },
     { id: 'split-3200',        nombre: 'Split frío/calor 3200 frigorías inverter',                  precioNeto: 1550000, categoria: 'extras' },
     { id: 'panel-solar-160',   nombre: 'Panel solar 160W + regulador 30A (12V)',                    precioNeto: 410000,  categoria: 'extras' },
     { id: 'sistema-solar-220', nombre: 'Sistema solar 220 off grid (inv 3kVA, 4 paneles 485W, 2 baterías litio 100Ah)', precioNeto: 6755000, categoria: 'extras' },
     { id: 'tv-32-directv',     nombre: 'TV 32" con DirecTV',                                        precioNeto: 458000,  categoria: 'extras' },
     { id: 'estereo-pioneer',   nombre: 'Mueble estéreo Pioneer + parlantes + antena',               precioNeto: 328220,  categoria: 'extras' },
     { id: 'cortinas-blackout', nombre: 'Cortinas roller black out (habitación y cocina)',           precioNeto: 483600,  categoria: 'extras' },
     { id: 'toldo',             nombre: 'Toldo',                                                     precioNeto: 496011,  categoria: 'extras' },
   ]
   ```
NO omitir ninguno, NO redondear. Son 17 entradas exactas.
  </action>
  <verify>
    <automated>node --input-type=module -e "import('./src/data/extras.js').then(m=>{const e=m.EXTRAS; if(e.length!==17){console.error('cant extras',e.length);process.exit(1)} const s=e.reduce((a,x)=>a+x.precioNeto,0); if(s!==17006023){console.error('suma extras incorrecta:',s);process.exit(1)} const cats=new Set(e.map(x=>x.categoria)); ['bano','dormitorio','cocina','extras'].forEach(c=>{if(!cats.has(c)){console.error('falta categoria',c);process.exit(1)}}); console.log('extras ok')})"</automated>
  </verify>
  <acceptance_criteria>
    - `src/data/extras.js` exporta `EXTRAS` con exactamente 17 entradas.
    - Cada entrada tiene `id`, `nombre`, `precioNeto` (Number), `categoria`.
    - Precios exactos presentes: `grep -q 495000` y 6755000, 1699000, 1690000, 172098, 496011, 328220 (los menos "redondos", para detectar typos).
    - La suma de los 17 `precioNeto` es 17.006.023 (validado por el smoke check).
    - Las 4 categorías ('bano','dormitorio','cocina','extras') están todas presentes.
  </acceptance_criteria>
  <done>`data/extras.js` exporta los 17 accesorios con precios exactos y categorías; la cantidad y la suma pasan el smoke check.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Crear utils/formato.js y wirearlo en App.jsx (smoke render)</name>
  <read_first>
    - src/App.jsx (shell de Plan 01 — se le agrega el smoke render)
    - src/data/models.js y src/data/geometry.js (para importar MODELOS e IVA)
  </read_first>
  <files>src/utils/formato.js, src/App.jsx</files>
  <behavior>
    - formatPrecio(29108976) === "$29.108.976" (formato argentino: $ adelante, punto como separador de miles, sin decimales)
    - formatPrecio(0) === "$0"
    - formatPrecio(172098) === "$172.098"
    - calcularIVA(100) === 21 (neto * 0.21)
    - calcularTotal(100) === 121 (neto * 1.21)
    - calcularTotal(29108976) === 35221860.96 (neto + IVA)
  </behavior>
  <action>
**1. `src/utils/formato.js`** — formato de precio argentino + helpers de IVA. Importar `IVA` de geometry:
   ```js
   import { IVA } from '../data/geometry.js'

   // Formato argentino: "$" adelante, punto de miles, sin decimales. Ej: 29108976 -> "$29.108.976"
   export function formatPrecio(n) {
     const entero = Math.round(Number(n) || 0)
     return '$' + entero.toLocaleString('es-AR')
   }

   export function calcularIVA(neto) {
     return (Number(neto) || 0) * IVA
   }

   export function calcularTotal(neto) {
     return (Number(neto) || 0) * (1 + IVA)
   }
   ```
   NOTA: `toLocaleString('es-AR')` usa el punto como separador de miles en Node y en navegadores
   modernos. Verificar el output con el smoke check (algunos entornos Node sin ICU completo pueden
   diferir; si `toLocaleString('es-AR')` no produjera puntos, implementar el agrupamiento a mano
   con regex `replace(/\B(?=(\d{3})+(?!\d))/g, '.')` para garantizar el formato).

**2. Actualizar `src/App.jsx`** — agregar un smoke render que importe los datos y use formatPrecio,
   probando que el wiring funciona end-to-end. Mantener el shell de Plan 01 y agregar una lista
   mínima de modelos con su precio neto formateado:
   ```jsx
   import { MODELOS } from './data/models.js'
   import { formatPrecio, calcularTotal } from './utils/formato.js'

   export default function App() {
     return (
       <div className="min-h-screen bg-impacar-fondo text-impacar-texto font-sans">
         <header className="bg-impacar-campo text-white px-6 py-4">
           <h1 className="text-2xl font-bold tracking-wide">IMPACAR</h1>
           <p className="text-sm opacity-90">Configurador de casillas rurales</p>
         </header>
         <main className="p-6">
           <p className="text-impacar-cobre font-semibold mb-4">
             Datos Lista 108 cargados — {MODELOS.length} modelos disponibles.
           </p>
           <ul className="space-y-1">
             {MODELOS.map((m) => (
               <li key={m.id}>
                 {m.nombre} · {m.largo}m · neto {formatPrecio(m.precioNeto)} · total c/IVA {formatPrecio(calcularTotal(m.precioNeto))}
               </li>
             ))}
           </ul>
         </main>
       </div>
     )
   }
   ```
   Este smoke render es temporal (se reemplaza por la landing real en Phase 3), pero prueba el
   Success Criteria de que los datos importan y formatPrecio funciona.
  </action>
  <verify>
    <automated>node --input-type=module -e "import('./src/utils/formato.js').then(f=>{const a=f.formatPrecio(29108976); if(a!=='\$29.108.976'){console.error('formatPrecio:',a);process.exit(1)} if(f.formatPrecio(0)!=='\$0')process.exit(1); if(Math.round(f.calcularTotal(100))!==121)process.exit(1); if(f.calcularIVA(100)!==21)process.exit(1); console.log('formato ok')})" && npm run build && grep -q 'formatPrecio' src/App.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/utils/formato.js` exporta `formatPrecio`, `calcularIVA`, `calcularTotal`.
    - `formatPrecio(29108976) === "$29.108.976"` (punto de miles, $ adelante) — validado por el smoke check.
    - `formatPrecio(0) === "$0"`.
    - `calcularTotal(100)` redondea a 121 y `calcularIVA(100) === 21` (usa `IVA` de geometry.js).
    - `src/App.jsx` importa `MODELOS` de `./data/models.js` y `formatPrecio` de `./utils/formato.js`: `grep -q 'formatPrecio' src/App.jsx` y `grep -q 'MODELOS' src/App.jsx`.
    - `npm run build` pasa limpio (valida que todos los imports resuelven y el JSX compila).
  </acceptance_criteria>
  <done>`formatPrecio` produce el formato argentino correcto, los helpers de IVA funcionan, y `App.jsx` renderiza los 7 modelos con sus precios formateados; el build pasa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| datos hardcodeados → app | Los precios y la geometría de la Lista 108 viven en `/data` como constantes estáticas. La única "amenaza" relevante es la integridad de los datos (typos en precios), no una superficie de seguridad: no hay input externo, ni red, ni backend en esta fase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | integridad de precios Lista 108 | mitigate | Los smoke checks automatizados validan la suma de los 7 modelos (268.579.506) y de los 17 extras (17.006.023); cualquier typo en un precio rompe la suma y falla la verificación. |
| T-01-06 | Tampering | consistencia geométrica | mitigate | Assert automatizado `anchoCama*2 + pasilloCentral === anchoInterior` (2.52) garantiza que la geometría que alimenta el plano y la validación de camas es coherente. |
| T-01-07 | Information Disclosure | datos comerciales en cliente | accept | Los precios de lista son públicos (catálogo comercial) y están pensados para mostrarse al cliente; no son secretos. Sin riesgo. |
| T-01-08 | Tampering | formato de precio dependiente de ICU | mitigate | El smoke check valida el output exacto de `formatPrecio`; si el entorno no agrupa con puntos, el fallback con regex garantiza el formato argentino. |
</threat_model>

<verification>
- `data/models.js` exporta 7 modelos con precios exactos; suma = 268.579.506.
- `data/extras.js` exporta 17 accesorios; suma = 17.006.023.
- `data/geometry.js` exporta la geometría real e IVA 0.21; la geometría cierra (2.52).
- `formatPrecio(29108976)` === "$29.108.976".
- `App.jsx` importa los datos y formatPrecio, y `npm run build` pasa limpio.
</verification>

<success_criteria>
- [ ] Cubre Success Criteria #2: `data/models.js` expone los 7 modelos reales con largo, camas base y precio neto.
- [ ] Cubre Success Criteria #3: `data/extras.js` expone los accesorios reales con precio neto y paso sugerido.
- [ ] Cubre Success Criteria #4: constantes de geometría real centralizadas en `data/geometry.js`.
- [ ] `formatPrecio()` produce formato argentino y los helpers de IVA funcionan.
- [ ] Smoke render en `App.jsx` prueba que los datos importan y formatPrecio funciona.
</success_criteria>

<output>
Tras completar, crear `.planning/phases/01-cimientos-y-datos/01-02-SUMMARY.md`
</output>
