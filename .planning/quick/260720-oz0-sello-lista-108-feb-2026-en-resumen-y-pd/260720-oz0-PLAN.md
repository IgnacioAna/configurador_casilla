---
phase: quick-260720-oz0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/geometry.js
  - src/components/resumen/PresupuestoDesglosado.jsx
  - src/utils/exportPDF.js
  - src/utils/exportWhatsApp.js
  - src/utils/exportWhatsApp.test.js
  - public/og.png
  - scripts/gen-og.ps1
autonomous: true
requirements: [QUICK-OZ0]

must_haves:
  truths:
    - "El sello de lista/fecha vive en UNA sola constante (geometry.js); ningún consumidor duplica el string literal"
    - "La pantalla de resumen muestra la leyenda 'Precios Lista 108 · Febrero 2026, sujetos a actualización.' leída de la constante"
    - "El PDF muestra la misma leyenda tras la nota orientativa y la fecha de generación (es-AR)"
    - "El mensaje de WhatsApp incluye una línea corta con Lista 108 · Febrero 2026"
    - "public/og.png tiene contenido real (>20KB, ya no es transparente)"
    - "El script generador queda versionado en scripts/gen-og.ps1"
  artifacts:
    - path: "src/data/geometry.js"
      provides: "Constante LISTA_PRECIOS (nombre + vigencia)"
      contains: "LISTA_PRECIOS"
    - path: "public/og.png"
      provides: "Imagen OG con contenido (preview de link WhatsApp)"
    - path: "scripts/gen-og.ps1"
      provides: "Script de regeneración del OG"
  key_links:
    - from: "src/components/resumen/PresupuestoDesglosado.jsx"
      to: "src/data/geometry.js"
      via: "import LISTA_PRECIOS"
      pattern: "LISTA_PRECIOS"
    - from: "src/utils/exportPDF.js"
      to: "src/data/geometry.js"
      via: "import LISTA_PRECIOS"
      pattern: "LISTA_PRECIOS"
---

<objective>
Dos mejoras pre-demo del configurador, en commits separados:

1. **Sello de Lista de Precios** — anclar de qué lista/fecha son los precios (Lista 108, Feb-2026)
   en resumen, PDF y WhatsApp, desde una constante única en geometry.js. Mitiga el riesgo comercial
   de que un PDF viejo circule con precio desactualizado sin aviso. El PDF además estampa la fecha
   de generación (es-AR).
2. **Reemplazar public/og.png** — el archivo actual es un PNG 100% transparente (WhatsApp muestra un
   recuadro blanco). Copiar la imagen nueva ya aprobada sobre public/og.png y versionar el script
   generador en scripts/gen-og.ps1.

Purpose: robustez comercial (precios fechados) + preview de link correcto en WhatsApp para la demo.
Output: constante LISTA_PRECIOS consumida en 3 salidas, og.png con contenido, script versionado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Estado actual de los ficheros a tocar. Usar directamente, sin explorar el codebase. -->

src/data/geometry.js (ya exporta IVA como constante de negocio; agregar LISTA_PRECIOS al lado):
```js
export const GEOMETRIA = { /* medidas */ }
export const IVA = 0.21 // 21%
```

src/components/resumen/PresupuestoDesglosado.jsx — la nota orientativa existente (línea final del componente):
```jsx
<p className="mt-2 text-xs text-impacar-texto/70">
  Presupuesto orientativo, sujeto a confirmación del asesor comercial.
</p>
```

src/utils/exportPDF.js — sección 3 "Presupuesto", tras el loop de la nota orientativa (~línea 93).
IMPORTANTE (constraint): la leyenda y la fecha van en construirDocPDF (browser-side, jsPDF), que NO
es testeable con node:test. nombreArchivoPDF es la única parte pura testeable y NO se toca.
Loop existente de la nota:
```js
doc.setFontSize(9)
for (const linea of doc.splitTextToSize(
  'Presupuesto orientativo, sujeto a confirmación del asesor comercial.',
  anchoUtil,
)) {
  cursorY = avanzarSiNecesario(doc, cursorY) // WR-03
  doc.text(linea, M, cursorY)
  cursorY += 5
}
cursorY += 4
```

src/utils/exportWhatsApp.js — mensajeWhatsApp arma un array .join('\n'); la nota existente es la
línea 'Presupuesto orientativo, sujeto a confirmación.' (justo después del Total c/IVA).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sello de Lista de Precios en constante única + resumen + PDF + WhatsApp</name>
  <files>src/data/geometry.js, src/components/resumen/PresupuestoDesglosado.jsx, src/utils/exportPDF.js, src/utils/exportWhatsApp.js, src/utils/exportWhatsApp.test.js</files>
  <action>
Implementa el sello de lista leyendo SIEMPRE de una constante única. NO duplicar el string en los consumidores.

**a) src/data/geometry.js** — agregar tras `export const IVA = 0.21`:
```js
// Lista de precios vigente (netos hardcodeados en models.js/extras.js). Fuente única del sello:
// resumen, PDF y WhatsApp leen de acá para evitar que un documento viejo circule sin fecha.
export const LISTA_PRECIOS = { nombre: 'Lista 108', vigencia: 'Febrero 2026' }
```

**b) src/components/resumen/PresupuestoDesglosado.jsx** — importar la constante:
`import { LISTA_PRECIOS } from '../../data/geometry.js'` y agregar una línea discreta INMEDIATAMENTE
debajo de la nota orientativa existente (misma clase tenue), leyendo de la constante:
```jsx
<p className="mt-1 text-xs text-impacar-texto/60">
  {`Precios ${LISTA_PRECIOS.nombre} · ${LISTA_PRECIOS.vigencia}, sujetos a actualización.`}
</p>
```

**c) src/utils/exportPDF.js** — importar `{ LISTA_PRECIOS }` desde `'../data/geometry.js'`. En
construirDocPDF, sección 3, JUSTO DESPUÉS del bloque `cursorY += 4` que cierra la nota orientativa
(antes de la sección 4 "Configuración"), agregar en la MISMA tipografía tenue (setFontSize 9,
setTextColor gris/texto): (1) la leyenda del sello y (2) la fecha de generación es-AR. Usar el helper
avanzarSiNecesario(doc, cursorY) por cada línea (WR-03), igual que la nota:
```js
doc.setFontSize(8)
doc.setTextColor(90, 90, 90)
const sello = `Precios ${LISTA_PRECIOS.nombre} · ${LISTA_PRECIOS.vigencia}, sujetos a actualización.`
const fechaGen = `Generado el ${new Date().toLocaleDateString('es-AR')}`
for (const linea of [sello, fechaGen]) {
  cursorY = avanzarSiNecesario(doc, cursorY)
  doc.text(linea, M, cursorY)
  cursorY += 5
}
doc.setTextColor(26, 26, 26) // restaurar color de texto por defecto
cursorY += 2
```
NOTA (constraint): esto va en construirDocPDF (browser-side, jsPDF import dinámico); NO es testeable
con node:test. No agregar tests para el PDF — se valida con `npm run build`.

**d) src/utils/exportWhatsApp.js** — importar `{ LISTA_PRECIOS }` desde `'../data/geometry.js'`. El
mensaje ya es largo: agregar UNA sola línea corta en el array de mensajeWhatsApp, JUSTO DESPUÉS de
`'Presupuesto orientativo, sujeto a confirmación.'`:
```js
`Precios ${LISTA_PRECIOS.nombre} · ${LISTA_PRECIOS.vigencia}.`,
```
Verificar que el test de longitud (<2000 chars con todos los extras) siga pasando — una línea corta
no debería acercarse al límite.

**e) src/utils/exportWhatsApp.test.js** — agregar UNA aserción dentro del test existente
'mensajeWhatsApp incluye el modelo...' (o un test nuevo corto) que verifique que el mensaje incluye
`LISTA_PRECIOS.nombre` (p.ej. `assert.ok(msg.includes('Lista 108'))`). Importar LISTA_PRECIOS si se
quiere evitar el literal en el test. NO agregar tests para exportPDF (parte no pura).

Trato de usted, tono sobrio en todo el copy nuevo.
  </action>
  <verify>
    <automated>node --test src/utils/exportWhatsApp.test.js src/utils/exportPDF.test.js</automated>
    <automated>npm run build</automated>
  </verify>
  <done>
LISTA_PRECIOS exportada en geometry.js; resumen, PDF (leyenda + fecha es-AR) y WhatsApp leen de la
constante sin duplicar el string; tests de WhatsApp pasan (incluye aserción del sello) y el build
compila. Ningún consumidor tiene el string 'Lista 108' hardcodeado (grep sólo lo encuentra en geometry.js).
  </done>
</task>

<task type="auto">
  <name>Task 2: Reemplazar public/og.png + versionar script generador</name>
  <files>public/og.png, scripts/gen-og.ps1</files>
  <action>
Reemplaza el OG transparente por la imagen nueva ya aprobada y versiona el script generador.

**a)** Copiar (sobrescribir) el PNG nuevo sobre public/og.png:
origen: `C:/Users/Usuario/AppData/Local/Temp/claude/C--Users-Usuario-OneDrive-Desktop-Proyectos-Impacar/8d55dc34-2795-49c2-abae-bee74b9890de/scratchpad/og-nuevo.png`
destino: `public/og.png`

**b)** Crear la carpeta `scripts/` (si no existe) y copiar el script generador para poder regenerar la imagen:
origen: `C:/Users/Usuario/AppData/Local/Temp/claude/C--Users-Usuario-OneDrive-Desktop-Proyectos-Impacar/8d55dc34-2795-49c2-abae-bee74b9890de/scratchpad/gen-og.ps1`
destino: `scripts/gen-og.ps1`

Usar la herramienta Bash con `cp` (rutas absolutas) — es copia binaria, NO usar Write/Edit para el PNG.
Ejemplo:
```bash
mkdir -p "C:/Users/Usuario/OneDrive/Desktop/Proyectos/Impacar/scripts"
cp "C:/Users/Usuario/AppData/Local/Temp/claude/C--Users-Usuario-OneDrive-Desktop-Proyectos-Impacar/8d55dc34-2795-49c2-abae-bee74b9890de/scratchpad/og-nuevo.png" "C:/Users/Usuario/OneDrive/Desktop/Proyectos/Impacar/public/og.png"
cp "C:/Users/Usuario/AppData/Local/Temp/claude/C--Users-Usuario-OneDrive-Desktop-Proyectos-Impacar/8d55dc34-2795-49c2-abae-bee74b9890de/scratchpad/gen-og.ps1" "C:/Users/Usuario/OneDrive/Desktop/Proyectos/Impacar/scripts/gen-og.ps1"
```
  </action>
  <verify>
    <automated>test $(stat -c%s "C:/Users/Usuario/OneDrive/Desktop/Proyectos/Impacar/public/og.png") -gt 20000 && test -f "C:/Users/Usuario/OneDrive/Desktop/Proyectos/Impacar/scripts/gen-og.ps1" && echo OK</automated>
  </verify>
  <done>
public/og.png pesa >20KB (contenido real, ya no transparente) y scripts/gen-og.ps1 existe versionado.
  </done>
</task>

</tasks>

<verification>
- `node --test src/utils/exportWhatsApp.test.js src/utils/exportPDF.test.js` → todos verdes.
- `npm run build` → compila sin errores (valida el bloque nuevo del PDF).
- `grep -rn "Lista 108" src/` → aparece SÓLO en src/data/geometry.js (no duplicado en consumidores).
- `public/og.png` >20KB; `scripts/gen-og.ps1` presente.
</verification>

<success_criteria>
- Constante LISTA_PRECIOS única en geometry.js; resumen + PDF + WhatsApp la consumen sin duplicar el string.
- PDF incluye leyenda del sello + fecha de generación es-AR (validado por build, no por test).
- Test de WhatsApp asevera la presencia del sello y sigue pasando (< 2000 chars).
- og.png con contenido real y script generador versionado.
- Dos commits separados: (1) sello de lista, (2) og.png + script.
</success_criteria>

<output>
Tras completar, crear `.planning/quick/260720-oz0-sello-lista-108-feb-2026-en-resumen-y-pd/260720-oz0-SUMMARY.md`.
Hacer DOS commits separados:
1. `feat(quick-260720-oz0): sello Lista 108 · Feb-2026 en resumen, PDF y WhatsApp` (Task 1)
2. `chore(quick-260720-oz0): reemplaza og.png transparente + versiona gen-og.ps1` (Task 2)
</output>
